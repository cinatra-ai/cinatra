# S7/M2 live render proof — listRow slot activation + un-hard-coded library glyphs (cinatra#1631)

Production-equivalent build (`next build` + `next start`, `CINATRA_E2E_SETUP_BYPASS` only), driven
with Playwright against the local verify stack (postgres:5634 / redis:6579, isolated per-lane DB
`s7_m2` cloned from the Slice B proof DB and migrated through `core__0053`). Session minted through
the real Better-Auth sign-in flow.

## Fixture world (org `s7before-org-mrp1ncos`)

| Row | mime | Active semantic assertions | Effective identity |
|---|---|---|---|
| S7 file row | image/png | default-artifact (agent) | floor |
| S7 structured row | application/octet-stream | none | plain |
| S7 claimed row | image/png | default-artifact (agent) + **email-body-artifact (user, classic)** | `@cinatra-ai/email-body-artifact` |
| S7 image-claimed row | image/png | **image-artifact (user, classic)** | `@cinatra-ai/image-artifact` |

Fixture provenance (disclosed): the artifact rows + representations were created through the real
upload route by the earlier lane run; the classic USER assertions and the ambient
`email-body-artifact` install row were seeded directly in the proof DB (the identity resolver and
every rendering path under test read them through the real stores). Identity grounding: the
effective-identity install read filters `kind='artifact'`, so only artifact-kind claimants can win —
a connector-claimed fixture can never produce an extension identity (verified live: a
gmail-connector user assertion resolves to the floor).

## BEFORE — origin/main @ bb785968 (`01-before-library-hardcoded-glyphs.png`)

The host-side hard-coded family map (`libraryGlyph`, library-mode.tsx): the email-body-artifact
winner renders the hard-coded **Mail** icon (name-substring match), the image-artifact winner the
hard-coded **Boxes** fallback; file/structured rows render the host floor split
(FileText / Braces).

## AFTER — lane/1631-m2 (`02-after-library-spine-glyphs.png`)

The glyph cell resolves through the winner-bound semantic registry at slot `listRow`
(`data-testid="artifacts-library-glyph"`, `data-glyph-source` attribute):

| Row | data-glyph-source | Rendered |
|---|---|---|
| S7 image-claimed row | **extension** | the claimant's OWN listRow module (`data-image-artifact="list-row-glyph"` svg) inside the host claimed cell |
| S7 claimed row | generic | generic claimed glyph (Boxes) — winner ships no listRow capability; the hard-coded Mail icon is GONE |
| S7 file row | generic | FileText, warning tint (host floor split intact) |
| S7 structured row | generic | Braces, muted tint (host floor split intact) |

DOM evidence captured from the live page (Playwright `evaluate`): the four glyph cells carry
exactly the sources/classes above; the extension glyph renders exactly one module (the losing /
capability-less claimants never mount — winner-binding + single-module).

## The listRow extension-channel fixture (TRANSIENT, disclosed)

No real claimant ships a `listRow` renderer yet (both M2 migrations are guarded out — see the PR
body). To prove the ACTIVATED channel end-to-end on the real surface, the LOCAL dev checkout of
`@cinatra-ai/image-artifact` (a prod-bundled system base) transiently declared
`cinatra.artifact.ui.renderers.listRow` with a static svg glyph module; the generated map +
build-config alias were regenerated by the real generators; the manifest passed the real
conformance path. The declaration/module/alias were REVERTED after capture — nothing of the
fixture is in this PR's diff. The channel it proves (manifest → conformance → generated map →
bridge registration → winner-bound resolve → SSR mount) is exactly the S1/S2 path a real claimant
will ship through.
