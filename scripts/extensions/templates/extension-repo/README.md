# {{NAME}}

{{DESCRIPTION}}

This is a standalone Cinatra extension repository, extracted from the Cinatra
monorepo. It is installed into a Cinatra workspace through the extension
marketplace and runs against the frozen `@cinatra-ai/sdk-extensions` host ABI.

## Capabilities

<!--
  Per the Cinatra extension README contract:
    - H1 = the extension display name (above)
    - one description paragraph (above)
    - a Capabilities section listing what this extension does
  Fill in the concrete capabilities this extension exposes. Examples:
    - A connector lists the providers/auth it bridges.
    - An agent lists the primitives / OAS operations it serves.
    - A workflow lists the orchestrated steps it runs.
    - A skill lists the SKILL.md-backed competencies it delivers.
-->

- _Describe a capability this extension provides._

## Kind

`{{KIND}}`

## Host ABI

Requires `@cinatra-ai/sdk-extensions` (see `peerDependencies`).

## License

See [`LICENSE`](./LICENSE).
