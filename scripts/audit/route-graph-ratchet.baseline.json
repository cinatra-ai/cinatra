{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions \u2014 a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1649,
    "/api/mcp": 1645,
    "/chat": 1723,
    "/api/a2a": 1650,
    "/api/llm-bridge": 1660
  },
  "absorbs": {
    "/sign-in": {
      "from": 1648,
      "to": 1649,
      "reason": "cinatra#2218 L1 \u2014 the atomic /api/mcp cutover to the published @modelcontextprotocol/server@2.0.0 adds exactly ONE first-party module, packages/mcp-server/src/inbound-era.ts, statically reachable from all five locked routes through @cinatra-ai/mcp-server: +1 module on each. It is irreducible, not an un-narrowed barrel. The module holds the protocol-ERA split the revision requires: the isLegacyRequest() routing that delivers inbound posture row A (2026-07-28 plus all five legacy revisions), the JSON-framed legacy leg, the createMcpHandler modern leg, CORS admission for the revision's REQUIRED Mcp-Method/Mcp-Name headers, Accept normalisation, and the per-request teardown. It cannot live in packages/mcp-server/src/index.tsx because the file-size ratchet caps that file \u2014 this change in fact leaves index.tsx BELOW its ceiling (1963 of 1967), so the two ratchets pull in opposite directions and a new module is the only resolution that is not gate-gaming. A second candidate module was avoided by folding the Standard-Schema tool admission into runtime-server.ts, where policedRegisterTool already owns the registerTool contract, so the measured delta is +1 and not +2. A lazy import would NOT change the measured graph (scripts/route-graph.mjs follows dynamic import()), so deferring it would move runtime cost while pretending the graph shrank. Re-measured on this PR head rebased onto main after #2233, with the companion extension repos cloned pinned exactly as CI does; the #2213 records this raise supersedes are retired by --write-baseline per the gate's retire-on-re-raise rule.",
      "pr": 2238
    },
    "/api/mcp": {
      "from": 1644,
      "to": 1645,
      "reason": "cinatra#2218 L1 \u2014 the atomic /api/mcp cutover to the published @modelcontextprotocol/server@2.0.0 adds exactly ONE first-party module, packages/mcp-server/src/inbound-era.ts, statically reachable from all five locked routes through @cinatra-ai/mcp-server: +1 module on each. It is irreducible, not an un-narrowed barrel. The module holds the protocol-ERA split the revision requires: the isLegacyRequest() routing that delivers inbound posture row A (2026-07-28 plus all five legacy revisions), the JSON-framed legacy leg, the createMcpHandler modern leg, CORS admission for the revision's REQUIRED Mcp-Method/Mcp-Name headers, Accept normalisation, and the per-request teardown. It cannot live in packages/mcp-server/src/index.tsx because the file-size ratchet caps that file \u2014 this change in fact leaves index.tsx BELOW its ceiling (1963 of 1967), so the two ratchets pull in opposite directions and a new module is the only resolution that is not gate-gaming. A second candidate module was avoided by folding the Standard-Schema tool admission into runtime-server.ts, where policedRegisterTool already owns the registerTool contract, so the measured delta is +1 and not +2. A lazy import would NOT change the measured graph (scripts/route-graph.mjs follows dynamic import()), so deferring it would move runtime cost while pretending the graph shrank. Re-measured on this PR head rebased onto main after #2233, with the companion extension repos cloned pinned exactly as CI does; the #2213 records this raise supersedes are retired by --write-baseline per the gate's retire-on-re-raise rule.",
      "pr": 2238
    },
    "/chat": {
      "from": 1722,
      "to": 1723,
      "reason": "cinatra#2218 L1 \u2014 the atomic /api/mcp cutover to the published @modelcontextprotocol/server@2.0.0 adds exactly ONE first-party module, packages/mcp-server/src/inbound-era.ts, statically reachable from all five locked routes through @cinatra-ai/mcp-server: +1 module on each. It is irreducible, not an un-narrowed barrel. The module holds the protocol-ERA split the revision requires: the isLegacyRequest() routing that delivers inbound posture row A (2026-07-28 plus all five legacy revisions), the JSON-framed legacy leg, the createMcpHandler modern leg, CORS admission for the revision's REQUIRED Mcp-Method/Mcp-Name headers, Accept normalisation, and the per-request teardown. It cannot live in packages/mcp-server/src/index.tsx because the file-size ratchet caps that file \u2014 this change in fact leaves index.tsx BELOW its ceiling (1963 of 1967), so the two ratchets pull in opposite directions and a new module is the only resolution that is not gate-gaming. A second candidate module was avoided by folding the Standard-Schema tool admission into runtime-server.ts, where policedRegisterTool already owns the registerTool contract, so the measured delta is +1 and not +2. A lazy import would NOT change the measured graph (scripts/route-graph.mjs follows dynamic import()), so deferring it would move runtime cost while pretending the graph shrank. Re-measured on this PR head rebased onto main after #2233, with the companion extension repos cloned pinned exactly as CI does; the #2213 records this raise supersedes are retired by --write-baseline per the gate's retire-on-re-raise rule.",
      "pr": 2238
    },
    "/api/a2a": {
      "from": 1649,
      "to": 1650,
      "reason": "cinatra#2218 L1 \u2014 the atomic /api/mcp cutover to the published @modelcontextprotocol/server@2.0.0 adds exactly ONE first-party module, packages/mcp-server/src/inbound-era.ts, statically reachable from all five locked routes through @cinatra-ai/mcp-server: +1 module on each. It is irreducible, not an un-narrowed barrel. The module holds the protocol-ERA split the revision requires: the isLegacyRequest() routing that delivers inbound posture row A (2026-07-28 plus all five legacy revisions), the JSON-framed legacy leg, the createMcpHandler modern leg, CORS admission for the revision's REQUIRED Mcp-Method/Mcp-Name headers, Accept normalisation, and the per-request teardown. It cannot live in packages/mcp-server/src/index.tsx because the file-size ratchet caps that file \u2014 this change in fact leaves index.tsx BELOW its ceiling (1963 of 1967), so the two ratchets pull in opposite directions and a new module is the only resolution that is not gate-gaming. A second candidate module was avoided by folding the Standard-Schema tool admission into runtime-server.ts, where policedRegisterTool already owns the registerTool contract, so the measured delta is +1 and not +2. A lazy import would NOT change the measured graph (scripts/route-graph.mjs follows dynamic import()), so deferring it would move runtime cost while pretending the graph shrank. Re-measured on this PR head rebased onto main after #2233, with the companion extension repos cloned pinned exactly as CI does; the #2213 records this raise supersedes are retired by --write-baseline per the gate's retire-on-re-raise rule.",
      "pr": 2238
    },
    "/api/llm-bridge": {
      "from": 1659,
      "to": 1660,
      "reason": "cinatra#2218 L1 \u2014 the atomic /api/mcp cutover to the published @modelcontextprotocol/server@2.0.0 adds exactly ONE first-party module, packages/mcp-server/src/inbound-era.ts, statically reachable from all five locked routes through @cinatra-ai/mcp-server: +1 module on each. It is irreducible, not an un-narrowed barrel. The module holds the protocol-ERA split the revision requires: the isLegacyRequest() routing that delivers inbound posture row A (2026-07-28 plus all five legacy revisions), the JSON-framed legacy leg, the createMcpHandler modern leg, CORS admission for the revision's REQUIRED Mcp-Method/Mcp-Name headers, Accept normalisation, and the per-request teardown. It cannot live in packages/mcp-server/src/index.tsx because the file-size ratchet caps that file \u2014 this change in fact leaves index.tsx BELOW its ceiling (1963 of 1967), so the two ratchets pull in opposite directions and a new module is the only resolution that is not gate-gaming. A second candidate module was avoided by folding the Standard-Schema tool admission into runtime-server.ts, where policedRegisterTool already owns the registerTool contract, so the measured delta is +1 and not +2. A lazy import would NOT change the measured graph (scripts/route-graph.mjs follows dynamic import()), so deferring it would move runtime cost while pretending the graph shrank. Re-measured on this PR head rebased onto main after #2233, with the companion extension repos cloned pinned exactly as CI does; the #2213 records this raise supersedes are retired by --write-baseline per the gate's retire-on-re-raise rule.",
      "pr": 2238
    }
  }
}
