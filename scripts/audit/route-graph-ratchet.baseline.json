{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 221,
    "/api/mcp": 1675,
    "/chat": 1771,
    "/api/a2a": 1680,
    "/api/llm-bridge": 1690
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1677,
      "to": 1680,
      "reason": "PR #2890 carries this record — it merges cinatra#2788 (S9d) forward into cinatra#2790 (S9f), so ONE record absorbs both raises on this route. cinatra#2790 S9f adds TWO first-party modules to the agents workspace package — packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision) — both shared between the run page's server-action path and the widget's API branch, which is why they are standalone modules rather than bodies inside the server-action files the API branch cannot import. cinatra#2788 S9d adds packages/agents/src/trigger-recurrence.ts — the ONE module that says what a recurrence selection means — which packages/agents/src/trigger-service.ts now imports (`buildCron`, `RecurringConfig`) instead of restating the cron vocabulary a second time. MEASURED, not computed: `analyzeRoute` was run over this merged tree and over pristine origin/main, origin/feat/2790-s9f-host-parity and origin/feat/2788-s9d-rework worktrees in the same environment; each parent branch reproduced its own committed ceiling exactly against the main control, and the merge measures the sum of the two disjoint module sets.",
      "pr": 2890
    },
    "/api/llm-bridge": {
      "from": 1687,
      "to": 1690,
      "reason": "PR #2890 carries this record — it merges cinatra#2788 (S9d) forward into cinatra#2790 (S9f), so ONE record absorbs both raises on this route. cinatra#2790 S9f adds TWO first-party modules to the agents workspace package — packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision) — both shared between the run page's server-action path and the widget's API branch, which is why they are standalone modules rather than bodies inside the server-action files the API branch cannot import. cinatra#2788 S9d adds packages/agents/src/trigger-recurrence.ts — the ONE module that says what a recurrence selection means — which packages/agents/src/trigger-service.ts now imports (`buildCron`, `RecurringConfig`) instead of restating the cron vocabulary a second time. MEASURED, not computed: `analyzeRoute` was run over this merged tree and over pristine origin/main, origin/feat/2790-s9f-host-parity and origin/feat/2788-s9d-rework worktrees in the same environment; each parent branch reproduced its own committed ceiling exactly against the main control, and the merge measures the sum of the two disjoint module sets.",
      "pr": 2890
    },
    "/api/mcp": {
      "from": 1672,
      "to": 1675,
      "reason": "PR #2890 carries this record — it merges cinatra#2788 (S9d) forward into cinatra#2790 (S9f), so ONE record absorbs both raises on this route. cinatra#2790 S9f adds TWO first-party modules to the agents workspace package — packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision) — both shared between the run page's server-action path and the widget's API branch, which is why they are standalone modules rather than bodies inside the server-action files the API branch cannot import. cinatra#2788 S9d adds packages/agents/src/trigger-recurrence.ts — the ONE module that says what a recurrence selection means — which packages/agents/src/trigger-service.ts now imports (`buildCron`, `RecurringConfig`) instead of restating the cron vocabulary a second time. MEASURED, not computed: `analyzeRoute` was run over this merged tree and over pristine origin/main, origin/feat/2790-s9f-host-parity and origin/feat/2788-s9d-rework worktrees in the same environment; each parent branch reproduced its own committed ceiling exactly against the main control, and the merge measures the sum of the two disjoint module sets.",
      "pr": 2890
    },
    "/chat": {
      "from": 1767,
      "to": 1771,
      "reason": "PR #2890 carries this record — it merges cinatra#2788 (S9d) forward into cinatra#2790 (S9f), so ONE record absorbs both raises on this route. cinatra#2790 S9f adds TWO first-party modules to the agents workspace package — packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision) — both shared between the run page's server-action path and the widget's API branch, which is why they are standalone modules rather than bodies inside the server-action files the API branch cannot import. cinatra#2788 S9d adds packages/agents/src/trigger-recurrence.ts — the ONE module that says what a recurrence selection means — which packages/agents/src/trigger-service.ts now imports (`buildCron`, `RecurringConfig`) instead of restating the cron vocabulary a second time. cinatra#2788 S9d also adds packages/agents/src/schedule-proposal-card.tsx on /chat alone: the renderable-view registry now dispatches `trigger_schedule_proposal` to the DRAWN card and the S1 placeholder shell is retired for this kind. MEASURED, not computed: `analyzeRoute` was run over this merged tree and over pristine origin/main, origin/feat/2790-s9f-host-parity and origin/feat/2788-s9d-rework worktrees in the same environment; each parent branch reproduced its own committed ceiling exactly against the main control, and the merge measures the sum of the two disjoint module sets.",
      "pr": 2890
    },
    "/sign-in": {
      "from": 220,
      "to": 221,
      "reason": "cinatra#2911 (PR #2919 carries this record): agent_runs.created_at bootstrap backfill moved to a DDL leaf imported by drizzle-store (#2911) — src/lib/agent-run-created-at-schema.ts. The unguarded whole-table `UPDATE ... SET created_at = COALESCE(started_at, completed_at, created_at)` that stood inline in buildCreateStoreSchemaQueries is replaced by three idempotent statements in their own zero-import leaf (add the column NULLABLE, backfill ONLY `created_at IS NULL`, then restore DEFAULT now() + NOT NULL), so a replay of the bootstrap can no longer recompute a run's creation time from columns populated later in its life. The leaf is reached only from src/lib/drizzle-store.ts, which sits under the store graph every locked route reaches, so it is +1 on each of them. It imports NOTHING (a pure string builder, the same leaf shape as review-island-grant-schema.ts): the edge is one module, not a barrel. Measured with the companion extension repos cloned pinned (112/112) on this branch and, as the control, on origin/main (269ceb19449dfa1ba7ed436aef132a59de28f9ae) — the control reproduced the committed main ceilings exactly and the branch measured +1 on each of the five tracked routes.",
      "pr": 2919
    }
  }
}
