{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions \u2014 a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 218,
    "/api/mcp": 1703,
    "/chat": 1775,
    "/api/a2a": 1708,
    "/api/llm-bridge": 1718
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1706,
      "pr": 2689,
      "reason": "cinatra#2687: sealing the widget OBO token to its parent sign-in and its live turn adds ONE new leaf (src/lib/widget-mcp-actor-authorization.ts) and makes ONE existing leaf reachable from the MCP graph (src/lib/widget-session-binding.ts, #2684's shared parent-session predicate, which the acceptance criteria require this path to ask rather than re-implement). +2 on each of these four routes, measured as a delta against the clean base on an identical tree; /sign-in is untouched at 218. Narrowing considered and rejected, twice. Inlining the two checks into src/lib/mcp-server.ts removes the new leaf, and removes with it the negative control the issue's acceptance demands: nothing can import that settings module in a test, which is precisely why codex round 0 found the boundary wiring unproven. Injecting the two readers as parameters keeps the graph flat and re-opens the drift seam the shared predicate exists to close, since a future caller could pass a laxer answer. The turn-activity read added NO node at all: it lives in assistant-thread-store.ts, which owns the assistant_turns table and was already on all four graphs. Both new nodes are leaves whose own imports were already reachable, so this adds two nodes and no subtree.",
      "to": 1708
    },
    "/api/llm-bridge": {
      "from": 1716,
      "pr": 2689,
      "reason": "cinatra#2687: sealing the widget OBO token to its parent sign-in and its live turn adds ONE new leaf (src/lib/widget-mcp-actor-authorization.ts) and makes ONE existing leaf reachable from the MCP graph (src/lib/widget-session-binding.ts, #2684's shared parent-session predicate, which the acceptance criteria require this path to ask rather than re-implement). +2 on each of these four routes, measured as a delta against the clean base on an identical tree; /sign-in is untouched at 218. Narrowing considered and rejected, twice. Inlining the two checks into src/lib/mcp-server.ts removes the new leaf, and removes with it the negative control the issue's acceptance demands: nothing can import that settings module in a test, which is precisely why codex round 0 found the boundary wiring unproven. Injecting the two readers as parameters keeps the graph flat and re-opens the drift seam the shared predicate exists to close, since a future caller could pass a laxer answer. The turn-activity read added NO node at all: it lives in assistant-thread-store.ts, which owns the assistant_turns table and was already on all four graphs. Both new nodes are leaves whose own imports were already reachable, so this adds two nodes and no subtree.",
      "to": 1718
    },
    "/api/mcp": {
      "from": 1701,
      "pr": 2689,
      "reason": "cinatra#2687: sealing the widget OBO token to its parent sign-in and its live turn adds ONE new leaf (src/lib/widget-mcp-actor-authorization.ts) and makes ONE existing leaf reachable from the MCP graph (src/lib/widget-session-binding.ts, #2684's shared parent-session predicate, which the acceptance criteria require this path to ask rather than re-implement). +2 on each of these four routes, measured as a delta against the clean base on an identical tree; /sign-in is untouched at 218. Narrowing considered and rejected, twice. Inlining the two checks into src/lib/mcp-server.ts removes the new leaf, and removes with it the negative control the issue's acceptance demands: nothing can import that settings module in a test, which is precisely why codex round 0 found the boundary wiring unproven. Injecting the two readers as parameters keeps the graph flat and re-opens the drift seam the shared predicate exists to close, since a future caller could pass a laxer answer. The turn-activity read added NO node at all: it lives in assistant-thread-store.ts, which owns the assistant_turns table and was already on all four graphs. Both new nodes are leaves whose own imports were already reachable, so this adds two nodes and no subtree.",
      "to": 1703
    },
    "/chat": {
      "from": 1773,
      "pr": 2689,
      "reason": "cinatra#2687: sealing the widget OBO token to its parent sign-in and its live turn adds ONE new leaf (src/lib/widget-mcp-actor-authorization.ts) and makes ONE existing leaf reachable from the MCP graph (src/lib/widget-session-binding.ts, #2684's shared parent-session predicate, which the acceptance criteria require this path to ask rather than re-implement). +2 on each of these four routes, measured as a delta against the clean base on an identical tree; /sign-in is untouched at 218. Narrowing considered and rejected, twice. Inlining the two checks into src/lib/mcp-server.ts removes the new leaf, and removes with it the negative control the issue's acceptance demands: nothing can import that settings module in a test, which is precisely why codex round 0 found the boundary wiring unproven. Injecting the two readers as parameters keeps the graph flat and re-opens the drift seam the shared predicate exists to close, since a future caller could pass a laxer answer. The turn-activity read added NO node at all: it lives in assistant-thread-store.ts, which owns the assistant_turns table and was already on all four graphs. Both new nodes are leaves whose own imports were already reachable, so this adds two nodes and no subtree.",
      "to": 1775
    }
  }
}
