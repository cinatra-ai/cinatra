{
 "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
 "routes": {
  "/sign-in": 219,
  "/api/mcp": 1672,
  "/chat": 1764,
  "/api/a2a": 1677,
  "/api/llm-bridge": 1687
 },
 "absorbs": {
  "/sign-in": {
   "from": 218,
   "to": 219,
   "pr": 2895,
   "reason": "cinatra#2754 (PR #2895 carries this record; the ruling on the issue names the mechanism): the single-use island-grant ledger gains ONE module, and it sits on the shared store surface, so it is reachable from every locked route that mounts the store: +1 on all five routes, including /sign-in (the auth surface reads the same store). src/lib/lifecycle/review-island-grant-store.ts — the hash-keyed grant ledger whose atomic spend semantics are unit-assertable against a ledger stand-in without loading the server-only store graph; inlining it into the store was rejected because the spend-once property must be testable in isolation and the file-size ratchet holds the store's ceiling flat. Measured with the pinned extension tree."
  },
  "/api/mcp": {
   "from": 1670,
   "to": 1672,
   "pr": 2790,
   "reason": "cinatra#2790 (PR #2890 carries this record): the recommendation-hold slice adds TWO core modules to the agents workspace package - packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision). Both are shared between the run page's server-action path (run-recommendation-actions.ts, run-actions.ts, server-actions.ts) and the widget's API branch (src/app/api/lifecycle-views/recommendation-hold/*, recommendation-hold-widget-branch.ts), which is why they exist as standalone modules rather than inside the server-action files the API branch cannot import. They sit on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on these four routes, /sign-in unchanged. Measured with the pinned extension tree against an origin/main control measured in the same environment (control reproduced main's committed baseline exactly)."
  },
  "/chat": {
   "from": 1762,
   "to": 1764,
   "pr": 2790,
   "reason": "cinatra#2790 (PR #2890 carries this record): the recommendation-hold slice adds TWO core modules to the agents workspace package - packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision). Both are shared between the run page's server-action path (run-recommendation-actions.ts, run-actions.ts, server-actions.ts) and the widget's API branch (src/app/api/lifecycle-views/recommendation-hold/*, recommendation-hold-widget-branch.ts), which is why they exist as standalone modules rather than inside the server-action files the API branch cannot import. They sit on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on these four routes, /sign-in unchanged. Measured with the pinned extension tree against an origin/main control measured in the same environment (control reproduced main's committed baseline exactly)."
  },
  "/api/a2a": {
   "from": 1675,
   "to": 1677,
   "pr": 2790,
   "reason": "cinatra#2790 (PR #2890 carries this record): the recommendation-hold slice adds TWO core modules to the agents workspace package - packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision). Both are shared between the run page's server-action path (run-recommendation-actions.ts, run-actions.ts, server-actions.ts) and the widget's API branch (src/app/api/lifecycle-views/recommendation-hold/*, recommendation-hold-widget-branch.ts), which is why they exist as standalone modules rather than inside the server-action files the API branch cannot import. They sit on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on these four routes, /sign-in unchanged. Measured with the pinned extension tree against an origin/main control measured in the same environment (control reproduced main's committed baseline exactly)."
  },
  "/api/llm-bridge": {
   "from": 1685,
   "to": 1687,
   "pr": 2790,
   "reason": "cinatra#2790 (PR #2890 carries this record): the recommendation-hold slice adds TWO core modules to the agents workspace package - packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision). Both are shared between the run page's server-action path (run-recommendation-actions.ts, run-actions.ts, server-actions.ts) and the widget's API branch (src/app/api/lifecycle-views/recommendation-hold/*, recommendation-hold-widget-branch.ts), which is why they exist as standalone modules rather than inside the server-action files the API branch cannot import. They sit on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on these four routes, /sign-in unchanged. Measured with the pinned extension tree against an origin/main control measured in the same environment (control reproduced main's committed baseline exactly)."
  }
 }
}
