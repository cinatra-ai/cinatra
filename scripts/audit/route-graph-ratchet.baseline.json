{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1545,
    "/api/mcp": 1541,
    "/chat": 1603,
    "/api/a2a": 1546,
    "/api/llm-bridge": 1553
  },
  "absorbs": {
    "/sign-in": {
      "from": 1539,
      "to": 1545,
      "reason": "#1625 (PR #1946): the run-scoped test-delivery send PORT is wired FAIL-CLOSED at boot per the converged design — the host send impl (test-delivery-send-port-impl.ts, which imports the email-send/auth/objects send graph the email_test_delivery_run_send primitive needs) must be registered present-or-error before any send so a run-scoped test send never silently no-ops. Reached via BOTH boot chains that hit the agents primitive handlers: the native MCP transport (src/lib/mcp-server.ts) and the deterministic /api/agents/passthrough primitive-handlers path. CI-measured +6 per tracked route (the run-scoped test-delivery send graph the email_test_delivery_run_send primitive needs); eager by the ratified fail-closed design, not deferred. RE-ANCHORED onto origin/main post-#1915 during the reconcile rebase: from = origin/main current ceiling, to = from + 6; the +6 test-delivery delta is disjoint from and additive to #1915's assistant-registry modules already in the base, and the CI route-graph-ratchet gate re-verifies the reachable counts with the companion extension repos cloned pinned. Retires/replaces the base branch's carried #1915 record via this annotated re-raise. codex-converged MERGE-SAFE.",
      "pr": 1946
    },
    "/api/mcp": {
      "from": 1535,
      "to": 1541,
      "reason": "#1625 (PR #1946): the run-scoped test-delivery send PORT is wired FAIL-CLOSED at boot per the converged design — the host send impl (test-delivery-send-port-impl.ts, which imports the email-send/auth/objects send graph the email_test_delivery_run_send primitive needs) must be registered present-or-error before any send so a run-scoped test send never silently no-ops. Reached via BOTH boot chains that hit the agents primitive handlers: the native MCP transport (src/lib/mcp-server.ts) and the deterministic /api/agents/passthrough primitive-handlers path. CI-measured +6 per tracked route (the run-scoped test-delivery send graph the email_test_delivery_run_send primitive needs); eager by the ratified fail-closed design, not deferred. RE-ANCHORED onto origin/main post-#1915 during the reconcile rebase: from = origin/main current ceiling, to = from + 6; the +6 test-delivery delta is disjoint from and additive to #1915's assistant-registry modules already in the base, and the CI route-graph-ratchet gate re-verifies the reachable counts with the companion extension repos cloned pinned. Retires/replaces the base branch's carried #1915 record via this annotated re-raise. codex-converged MERGE-SAFE.",
      "pr": 1946
    },
    "/chat": {
      "from": 1597,
      "to": 1603,
      "reason": "#1625 (PR #1946): the run-scoped test-delivery send PORT is wired FAIL-CLOSED at boot per the converged design — the host send impl (test-delivery-send-port-impl.ts, which imports the email-send/auth/objects send graph the email_test_delivery_run_send primitive needs) must be registered present-or-error before any send so a run-scoped test send never silently no-ops. Reached via BOTH boot chains that hit the agents primitive handlers: the native MCP transport (src/lib/mcp-server.ts) and the deterministic /api/agents/passthrough primitive-handlers path. CI-measured +6 per tracked route (the run-scoped test-delivery send graph the email_test_delivery_run_send primitive needs); eager by the ratified fail-closed design, not deferred. RE-ANCHORED onto origin/main post-#1915 during the reconcile rebase: from = origin/main current ceiling, to = from + 6; the +6 test-delivery delta is disjoint from and additive to #1915's assistant-registry modules already in the base, and the CI route-graph-ratchet gate re-verifies the reachable counts with the companion extension repos cloned pinned. Retires/replaces the base branch's carried #1915 record via this annotated re-raise. codex-converged MERGE-SAFE.",
      "pr": 1946
    },
    "/api/a2a": {
      "from": 1540,
      "to": 1546,
      "reason": "#1625 (PR #1946): the run-scoped test-delivery send PORT is wired FAIL-CLOSED at boot per the converged design — the host send impl (test-delivery-send-port-impl.ts, which imports the email-send/auth/objects send graph the email_test_delivery_run_send primitive needs) must be registered present-or-error before any send so a run-scoped test send never silently no-ops. Reached via BOTH boot chains that hit the agents primitive handlers: the native MCP transport (src/lib/mcp-server.ts) and the deterministic /api/agents/passthrough primitive-handlers path. CI-measured +6 per tracked route (the run-scoped test-delivery send graph the email_test_delivery_run_send primitive needs); eager by the ratified fail-closed design, not deferred. RE-ANCHORED onto origin/main post-#1915 during the reconcile rebase: from = origin/main current ceiling, to = from + 6; the +6 test-delivery delta is disjoint from and additive to #1915's assistant-registry modules already in the base, and the CI route-graph-ratchet gate re-verifies the reachable counts with the companion extension repos cloned pinned. Retires/replaces the base branch's carried #1915 record via this annotated re-raise. codex-converged MERGE-SAFE.",
      "pr": 1946
    },
    "/api/llm-bridge": {
      "from": 1547,
      "to": 1553,
      "reason": "#1625 (PR #1946): the run-scoped test-delivery send PORT is wired FAIL-CLOSED at boot per the converged design — the host send impl (test-delivery-send-port-impl.ts, which imports the email-send/auth/objects send graph the email_test_delivery_run_send primitive needs) must be registered present-or-error before any send so a run-scoped test send never silently no-ops. Reached via BOTH boot chains that hit the agents primitive handlers: the native MCP transport (src/lib/mcp-server.ts) and the deterministic /api/agents/passthrough primitive-handlers path. CI-measured +6 per tracked route (the run-scoped test-delivery send graph the email_test_delivery_run_send primitive needs); eager by the ratified fail-closed design, not deferred. RE-ANCHORED onto origin/main post-#1915 during the reconcile rebase: from = origin/main current ceiling, to = from + 6; the +6 test-delivery delta is disjoint from and additive to #1915's assistant-registry modules already in the base, and the CI route-graph-ratchet gate re-verifies the reachable counts with the companion extension repos cloned pinned. Retires/replaces the base branch's carried #1915 record via this annotated re-raise. codex-converged MERGE-SAFE.",
      "pr": 1946
    }
  }
}
