{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1499,
    "/api/mcp": 1495,
    "/chat": 1555,
    "/api/a2a": 1500,
    "/api/llm-bridge": 1507
  },
  "absorbs": {
    "/sign-in": {
      "from": 1497,
      "to": 1499,
      "reason": "exec-plane S3 A2/A3 (cinatra#1708, PR #1843) makes exactly TWO lightweight first-party modules reachable from THIS tracked route via the shared drizzle-store / background-jobs bootstrap graph: (1) src/lib/execution/environment-layer-schema.ts — the Drizzle table schema for the durable environment-layer store, aggregated by the globally-reachable src/lib/drizzle-store.ts (the store imports every table schema, so the schema is reachable wherever the store is); (2) src/lib/execution/register-execution-environment-service.ts — the globalThis DI-slot accessor, reachable via the background-jobs registry / boot wiring. Both add exactly themselves: the heavy environment-execution-service impl, its pg store (environment-layer-store.pg.ts), the service construct, the boot phase, and the @cinatra-ai/execution-plane environment builder/cache graph all stay behind the DI slot and the boot-only dynamic import and are NOT route-reachable (verified: none appear in any tracked route's reachable set; moduleCount grew by exactly +2). A uniform +2 on every tracked route. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0) in CI.",
      "pr": 1843
    },
    "/api/mcp": {
      "from": 1493,
      "to": 1495,
      "reason": "exec-plane S3 A2/A3 (cinatra#1708, PR #1843) makes exactly TWO lightweight first-party modules reachable from THIS tracked route via the shared drizzle-store / background-jobs bootstrap graph: (1) src/lib/execution/environment-layer-schema.ts — the Drizzle table schema for the durable environment-layer store, aggregated by the globally-reachable src/lib/drizzle-store.ts (the store imports every table schema, so the schema is reachable wherever the store is); (2) src/lib/execution/register-execution-environment-service.ts — the globalThis DI-slot accessor, reachable via the background-jobs registry / boot wiring. Both add exactly themselves: the heavy environment-execution-service impl, its pg store (environment-layer-store.pg.ts), the service construct, the boot phase, and the @cinatra-ai/execution-plane environment builder/cache graph all stay behind the DI slot and the boot-only dynamic import and are NOT route-reachable (verified: none appear in any tracked route's reachable set; moduleCount grew by exactly +2). A uniform +2 on every tracked route. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0) in CI.",
      "pr": 1843
    },
    "/chat": {
      "from": 1553,
      "to": 1555,
      "reason": "exec-plane S3 A2/A3 (cinatra#1708, PR #1843) makes exactly TWO lightweight first-party modules reachable from THIS tracked route via the shared drizzle-store / background-jobs bootstrap graph: (1) src/lib/execution/environment-layer-schema.ts — the Drizzle table schema for the durable environment-layer store, aggregated by the globally-reachable src/lib/drizzle-store.ts (the store imports every table schema, so the schema is reachable wherever the store is); (2) src/lib/execution/register-execution-environment-service.ts — the globalThis DI-slot accessor, reachable via the background-jobs registry / boot wiring. Both add exactly themselves: the heavy environment-execution-service impl, its pg store (environment-layer-store.pg.ts), the service construct, the boot phase, and the @cinatra-ai/execution-plane environment builder/cache graph all stay behind the DI slot and the boot-only dynamic import and are NOT route-reachable (verified: none appear in any tracked route's reachable set; moduleCount grew by exactly +2). A uniform +2 on every tracked route. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0) in CI.",
      "pr": 1843
    },
    "/api/a2a": {
      "from": 1498,
      "to": 1500,
      "reason": "exec-plane S3 A2/A3 (cinatra#1708, PR #1843) makes exactly TWO lightweight first-party modules reachable from THIS tracked route via the shared drizzle-store / background-jobs bootstrap graph: (1) src/lib/execution/environment-layer-schema.ts — the Drizzle table schema for the durable environment-layer store, aggregated by the globally-reachable src/lib/drizzle-store.ts (the store imports every table schema, so the schema is reachable wherever the store is); (2) src/lib/execution/register-execution-environment-service.ts — the globalThis DI-slot accessor, reachable via the background-jobs registry / boot wiring. Both add exactly themselves: the heavy environment-execution-service impl, its pg store (environment-layer-store.pg.ts), the service construct, the boot phase, and the @cinatra-ai/execution-plane environment builder/cache graph all stay behind the DI slot and the boot-only dynamic import and are NOT route-reachable (verified: none appear in any tracked route's reachable set; moduleCount grew by exactly +2). A uniform +2 on every tracked route. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0) in CI.",
      "pr": 1843
    },
    "/api/llm-bridge": {
      "from": 1504,
      "to": 1507,
      "reason": "exec-plane S3 A2/A3 (cinatra#1708, PR #1843) makes exactly TWO lightweight first-party modules reachable from THIS tracked route via the shared drizzle-store / background-jobs bootstrap graph: (1) src/lib/execution/environment-layer-schema.ts — the Drizzle table schema for the durable environment-layer store, aggregated by the globally-reachable src/lib/drizzle-store.ts (the store imports every table schema, so the schema is reachable wherever the store is); (2) src/lib/execution/register-execution-environment-service.ts — the globalThis DI-slot accessor, reachable via the background-jobs registry / boot wiring. Both add exactly themselves: the heavy environment-execution-service impl, its pg store (environment-layer-store.pg.ts), the service construct, the boot phase, and the @cinatra-ai/execution-plane environment builder/cache graph all stay behind the DI slot and the boot-only dynamic import and are NOT route-reachable (verified: none appear in any tracked route's reachable set; moduleCount grew by exactly +2). A uniform +2 on every tracked route. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0) in CI. PLUS one route-specific module reachable from /api/llm-bridge ONLY: src/lib/execution/resolve-run-execution-binding.ts — the fail-closed run-seam decision matrix imported by src/app/api/llm-bridge/route.ts (its only value import, resolveRunExecutionEnvironment from @cinatra-ai/agents, is already route-reachable, so it adds only itself). Net +3 on /api/llm-bridge (1504 -> 1507).",
      "pr": 1843
    }
  }
}
