{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1483,
    "/api/mcp": 1479,
    "/chat": 1539,
    "/api/a2a": 1484,
    "/api/llm-bridge": 1490
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1481,
      "to": 1484,
      "reason": "cinatra#1708 exec-plane S3 (PR #1754) L1 declared environments: three net-new leaf modules become reachable on this route. (1) packages/sdk-extensions/src/execution-environment.ts — canonical ExecutionEnvironmentSpec parser + identity-bearing canonicalization; ZERO first-party imports — consumed by the snapshot capture and re-exported from the already-reachable sdk-extensions barrel. (2) packages/agents/src/execution-environment.ts — project-agent env normalization + pinned-run snapshot-first resolution (the S3 public API the app-layer wiring slice consumes next); imports only the already-reachable sdk-extensions barrel plus a type-only ./store import. (3) packages/agents/src/template-snapshot.ts — the snapshot build/hash/diff seam EXTRACTED out of packages/agents/src/store.ts because store.ts is a file-size-ratcheted bottleneck sitting AT its line ceiling (no absorb mechanism exists there by design; extraction is the sanctioned remedy), carrying the S3 execution-environment snapshot capture; imports only already-reachable modules. All three are true leaves pulling no further subtree, so the route grows by exactly +3. NOT a narrowable barrel import: store.ts genuinely consumes the parser at snapshot-save time and the module split is forced by the file-size ratchet. Re-measured on the S3 branch merged onto final main d1d8f2ff, with the pinned companion-extension clones present at missingCount 0 (scripts/audit/route-graph-ratchet.mjs --report): /api/a2a 1481 -> 1484 (NET +3). Retires the PR #1750 record by re-raising the ceiling.",
      "pr": 1754
    },
    "/api/llm-bridge": {
      "from": 1487,
      "to": 1490,
      "reason": "cinatra#1708 exec-plane S3 (PR #1754) L1 declared environments: three net-new leaf modules become reachable on this route. (1) packages/sdk-extensions/src/execution-environment.ts — canonical ExecutionEnvironmentSpec parser + identity-bearing canonicalization; ZERO first-party imports — consumed by the snapshot capture and re-exported from the already-reachable sdk-extensions barrel. (2) packages/agents/src/execution-environment.ts — project-agent env normalization + pinned-run snapshot-first resolution (the S3 public API the app-layer wiring slice consumes next); imports only the already-reachable sdk-extensions barrel plus a type-only ./store import. (3) packages/agents/src/template-snapshot.ts — the snapshot build/hash/diff seam EXTRACTED out of packages/agents/src/store.ts because store.ts is a file-size-ratcheted bottleneck sitting AT its line ceiling (no absorb mechanism exists there by design; extraction is the sanctioned remedy), carrying the S3 execution-environment snapshot capture; imports only already-reachable modules. All three are true leaves pulling no further subtree, so the route grows by exactly +3. NOT a narrowable barrel import: store.ts genuinely consumes the parser at snapshot-save time and the module split is forced by the file-size ratchet. Re-measured on the S3 branch merged onto final main d1d8f2ff, with the pinned companion-extension clones present at missingCount 0 (scripts/audit/route-graph-ratchet.mjs --report): /api/llm-bridge 1487 -> 1490 (NET +3). Retires the PR #1750 record by re-raising the ceiling.",
      "pr": 1754
    },
    "/api/mcp": {
      "from": 1476,
      "to": 1479,
      "reason": "cinatra#1708 exec-plane S3 (PR #1754) L1 declared environments: three net-new leaf modules become reachable on this route. (1) packages/sdk-extensions/src/execution-environment.ts — canonical ExecutionEnvironmentSpec parser + identity-bearing canonicalization; ZERO first-party imports — consumed by the snapshot capture and re-exported from the already-reachable sdk-extensions barrel. (2) packages/agents/src/execution-environment.ts — project-agent env normalization + pinned-run snapshot-first resolution (the S3 public API the app-layer wiring slice consumes next); imports only the already-reachable sdk-extensions barrel plus a type-only ./store import. (3) packages/agents/src/template-snapshot.ts — the snapshot build/hash/diff seam EXTRACTED out of packages/agents/src/store.ts because store.ts is a file-size-ratcheted bottleneck sitting AT its line ceiling (no absorb mechanism exists there by design; extraction is the sanctioned remedy), carrying the S3 execution-environment snapshot capture; imports only already-reachable modules. All three are true leaves pulling no further subtree, so the route grows by exactly +3. NOT a narrowable barrel import: store.ts genuinely consumes the parser at snapshot-save time and the module split is forced by the file-size ratchet. Re-measured on the S3 branch merged onto final main d1d8f2ff, with the pinned companion-extension clones present at missingCount 0 (scripts/audit/route-graph-ratchet.mjs --report): /api/mcp 1476 -> 1479 (NET +3). Retires the PR #1750 record by re-raising the ceiling.",
      "pr": 1754
    },
    "/chat": {
      "from": 1536,
      "to": 1539,
      "reason": "cinatra#1708 exec-plane S3 (PR #1754) L1 declared environments: three net-new leaf modules become reachable on this route. (1) packages/sdk-extensions/src/execution-environment.ts — canonical ExecutionEnvironmentSpec parser + identity-bearing canonicalization; ZERO first-party imports — consumed by the snapshot capture and re-exported from the already-reachable sdk-extensions barrel. (2) packages/agents/src/execution-environment.ts — project-agent env normalization + pinned-run snapshot-first resolution (the S3 public API the app-layer wiring slice consumes next); imports only the already-reachable sdk-extensions barrel plus a type-only ./store import. (3) packages/agents/src/template-snapshot.ts — the snapshot build/hash/diff seam EXTRACTED out of packages/agents/src/store.ts because store.ts is a file-size-ratcheted bottleneck sitting AT its line ceiling (no absorb mechanism exists there by design; extraction is the sanctioned remedy), carrying the S3 execution-environment snapshot capture; imports only already-reachable modules. All three are true leaves pulling no further subtree, so the route grows by exactly +3. NOT a narrowable barrel import: store.ts genuinely consumes the parser at snapshot-save time and the module split is forced by the file-size ratchet. Re-measured on the S3 branch merged onto final main d1d8f2ff, with the pinned companion-extension clones present at missingCount 0 (scripts/audit/route-graph-ratchet.mjs --report): /chat 1536 -> 1539 (NET +3). Retires the PR #1750 record by re-raising the ceiling.",
      "pr": 1754
    },
    "/sign-in": {
      "from": 1480,
      "to": 1483,
      "reason": "cinatra#1708 exec-plane S3 (PR #1754) L1 declared environments: three net-new leaf modules become reachable on this route. (1) packages/sdk-extensions/src/execution-environment.ts — canonical ExecutionEnvironmentSpec parser + identity-bearing canonicalization; ZERO first-party imports — consumed by the snapshot capture and re-exported from the already-reachable sdk-extensions barrel. (2) packages/agents/src/execution-environment.ts — project-agent env normalization + pinned-run snapshot-first resolution (the S3 public API the app-layer wiring slice consumes next); imports only the already-reachable sdk-extensions barrel plus a type-only ./store import. (3) packages/agents/src/template-snapshot.ts — the snapshot build/hash/diff seam EXTRACTED out of packages/agents/src/store.ts because store.ts is a file-size-ratcheted bottleneck sitting AT its line ceiling (no absorb mechanism exists there by design; extraction is the sanctioned remedy), carrying the S3 execution-environment snapshot capture; imports only already-reachable modules. All three are true leaves pulling no further subtree, so the route grows by exactly +3. NOT a narrowable barrel import: store.ts genuinely consumes the parser at snapshot-save time and the module split is forced by the file-size ratchet. Re-measured on the S3 branch merged onto final main d1d8f2ff, with the pinned companion-extension clones present at missingCount 0 (scripts/audit/route-graph-ratchet.mjs --report): /sign-in 1480 -> 1483 (NET +3). Retires the PR #1750 record by re-raising the ceiling.",
      "pr": 1754
    }
  }
}
