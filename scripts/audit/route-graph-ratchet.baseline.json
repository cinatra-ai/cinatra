{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 142,
    "/api/mcp": 1421,
    "/chat": 1459,
    "/api/a2a": 1426,
    "/api/llm-bridge": 1432
  },
  "absorbs": {
    "/sign-in": {
      "from": 141,
      "to": 142,
      "reason": "cinatra#1427 (PR #1475): src/lib/graphiti-projection-policy-schema.ts — the projection-policy epoch + durable rebuild-journal bootstrap DDL held in a pure-strings zero-import leaf STATICALLY imported by drizzle-store.ts (the #1425 artifact-claim-schema / #1426 semantic-assertion-schema extract-leaf -> route-graph-absorb pattern). /sign-in reaches the store via auth -> DB (drizzle-store) but NOT the background-jobs projection loop, so it gains EXACTLY this 1 schema leaf and neither packages/objects rebuild module. Measured by the CI ratchet on this PR's tree with the companion extension repos cloned pinned (missingCount 0, delta exact +1 vs the base ceiling). Absorbed raise 141->142.",
      "pr": 1475
    },
    "/api/mcp": {
      "from": 1418,
      "to": 1421,
      "reason": "cinatra#1427 (PR #1475) faceted claimed-row projection + projection-policy epoch + fenced rebuild. This route gains EXACTLY 3 net-new first-party modules: (1) src/lib/graphiti-projection-policy-schema.ts — the bootstrap DDL leaf statically imported by drizzle-store.ts (reached by every store route, incl. /sign-in); plus (2) packages/objects/src/graphiti-rebuild.ts (the epoch-bump/rebuild-journal driver) and (3) packages/objects/src/graphiti-projection-policy.ts (the shared epoch-read leaf), both pulled in by the GRAPHITI_PROJECTION_REPAIR handler in background-jobs-registry.ts (which swapped its dynamic import from graphiti-projector to graphiti-rebuild). The four API/chat routes reach the background-jobs registry surface and the CI ratchet attributed exactly +3 to each; /sign-in reaches the store but not the projection loop and gains only the +1 schema leaf. Absorbed raise 1418->1421.",
      "pr": 1475
    },
    "/chat": {
      "from": 1456,
      "to": 1459,
      "reason": "cinatra#1427 (PR #1475) faceted claimed-row projection + projection-policy epoch + fenced rebuild. This route gains EXACTLY 3 net-new first-party modules: (1) src/lib/graphiti-projection-policy-schema.ts — the bootstrap DDL leaf statically imported by drizzle-store.ts (reached by every store route, incl. /sign-in); plus (2) packages/objects/src/graphiti-rebuild.ts (the epoch-bump/rebuild-journal driver) and (3) packages/objects/src/graphiti-projection-policy.ts (the shared epoch-read leaf), both pulled in by the GRAPHITI_PROJECTION_REPAIR handler in background-jobs-registry.ts (which swapped its dynamic import from graphiti-projector to graphiti-rebuild). The four API/chat routes reach the background-jobs registry surface and the CI ratchet attributed exactly +3 to each; /sign-in reaches the store but not the projection loop and gains only the +1 schema leaf. Absorbed raise 1456->1459.",
      "pr": 1475
    },
    "/api/a2a": {
      "from": 1423,
      "to": 1426,
      "reason": "cinatra#1427 (PR #1475) faceted claimed-row projection + projection-policy epoch + fenced rebuild. This route gains EXACTLY 3 net-new first-party modules: (1) src/lib/graphiti-projection-policy-schema.ts — the bootstrap DDL leaf statically imported by drizzle-store.ts (reached by every store route, incl. /sign-in); plus (2) packages/objects/src/graphiti-rebuild.ts (the epoch-bump/rebuild-journal driver) and (3) packages/objects/src/graphiti-projection-policy.ts (the shared epoch-read leaf), both pulled in by the GRAPHITI_PROJECTION_REPAIR handler in background-jobs-registry.ts (which swapped its dynamic import from graphiti-projector to graphiti-rebuild). The four API/chat routes reach the background-jobs registry surface and the CI ratchet attributed exactly +3 to each; /sign-in reaches the store but not the projection loop and gains only the +1 schema leaf. Absorbed raise 1423->1426.",
      "pr": 1475
    },
    "/api/llm-bridge": {
      "from": 1429,
      "to": 1432,
      "reason": "cinatra#1427 (PR #1475) faceted claimed-row projection + projection-policy epoch + fenced rebuild. This route gains EXACTLY 3 net-new first-party modules: (1) src/lib/graphiti-projection-policy-schema.ts — the bootstrap DDL leaf statically imported by drizzle-store.ts (reached by every store route, incl. /sign-in); plus (2) packages/objects/src/graphiti-rebuild.ts (the epoch-bump/rebuild-journal driver) and (3) packages/objects/src/graphiti-projection-policy.ts (the shared epoch-read leaf), both pulled in by the GRAPHITI_PROJECTION_REPAIR handler in background-jobs-registry.ts (which swapped its dynamic import from graphiti-projector to graphiti-rebuild). The four API/chat routes reach the background-jobs registry surface and the CI ratchet attributed exactly +3 to each; /sign-in reaches the store but not the projection loop and gains only the +1 schema leaf. Absorbed raise 1429->1432.",
      "pr": 1475
    }
  }
}
