{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1591,
    "/api/mcp": 1587,
    "/chat": 1661,
    "/api/a2a": 1592,
    "/api/llm-bridge": 1602
  },
  "absorbs": {
    "/sign-in": {
      "from": 1590,
      "to": 1591,
      "reason": "#2088 S1 (epic #2086): the bundle-aware skill content authority adds exactly ONE new first-party module, src/lib/skill-bundle-store.ts — the store for skill_bundle_blobs / skill_revision_files / skill_bundle_heads. It is reachable from every locked route because src/lib/skill-lifecycle-store.ts appends its query builder to the SAME transaction as the custom/personal upsertSkill catalog write (the manifest MUST commit atomically with the revision, so the builder cannot be deferred behind a boot-phase slot), and src/lib/database.ts resolves a rollback target's bundle identity through it. Narrowing was applied as far as it goes and is already reflected here: the module imports NOTHING but node builtins, server-only and @/lib leaves — the pure bundle-digest and router-lint helpers are INLINED rather than split into a second leaf (which would have cost +2 per route) and are deliberately not imported from @cinatra-ai/llm (that barrel re-exports packages/llm/src/registry.ts, which imports @/lib/database — a circular import through the store hub that broke prod boot). A guard test in src/lib/__tests__/skill-bundle-digest.test.ts pins the import surface so this +1 cannot silently become more. Retires the #2101 record for this route (its raise is baked into the base ceiling).",
      "pr": 2102
    },
    "/api/mcp": {
      "from": 1586,
      "to": 1587,
      "reason": "#2088 S1 (epic #2086): the bundle-aware skill content authority adds exactly ONE new first-party module, src/lib/skill-bundle-store.ts — the store for skill_bundle_blobs / skill_revision_files / skill_bundle_heads. It is reachable from every locked route because src/lib/skill-lifecycle-store.ts appends its query builder to the SAME transaction as the custom/personal upsertSkill catalog write (the manifest MUST commit atomically with the revision, so the builder cannot be deferred behind a boot-phase slot), and src/lib/database.ts resolves a rollback target's bundle identity through it. Narrowing was applied as far as it goes and is already reflected here: the module imports NOTHING but node builtins, server-only and @/lib leaves — the pure bundle-digest and router-lint helpers are INLINED rather than split into a second leaf (which would have cost +2 per route) and are deliberately not imported from @cinatra-ai/llm (that barrel re-exports packages/llm/src/registry.ts, which imports @/lib/database — a circular import through the store hub that broke prod boot). A guard test in src/lib/__tests__/skill-bundle-digest.test.ts pins the import surface so this +1 cannot silently become more. Retires the #2101 record for this route (its raise is baked into the base ceiling).",
      "pr": 2102
    },
    "/chat": {
      "from": 1660,
      "to": 1661,
      "reason": "#2088 S1 (epic #2086): the bundle-aware skill content authority adds exactly ONE new first-party module, src/lib/skill-bundle-store.ts — the store for skill_bundle_blobs / skill_revision_files / skill_bundle_heads. It is reachable from every locked route because src/lib/skill-lifecycle-store.ts appends its query builder to the SAME transaction as the custom/personal upsertSkill catalog write (the manifest MUST commit atomically with the revision, so the builder cannot be deferred behind a boot-phase slot), and src/lib/database.ts resolves a rollback target's bundle identity through it. Narrowing was applied as far as it goes and is already reflected here: the module imports NOTHING but node builtins, server-only and @/lib leaves — the pure bundle-digest and router-lint helpers are INLINED rather than split into a second leaf (which would have cost +2 per route) and are deliberately not imported from @cinatra-ai/llm (that barrel re-exports packages/llm/src/registry.ts, which imports @/lib/database — a circular import through the store hub that broke prod boot). A guard test in src/lib/__tests__/skill-bundle-digest.test.ts pins the import surface so this +1 cannot silently become more. Retires the #2101 record for this route (its raise is baked into the base ceiling).",
      "pr": 2102
    },
    "/api/a2a": {
      "from": 1591,
      "to": 1592,
      "reason": "#2088 S1 (epic #2086): the bundle-aware skill content authority adds exactly ONE new first-party module, src/lib/skill-bundle-store.ts — the store for skill_bundle_blobs / skill_revision_files / skill_bundle_heads. It is reachable from every locked route because src/lib/skill-lifecycle-store.ts appends its query builder to the SAME transaction as the custom/personal upsertSkill catalog write (the manifest MUST commit atomically with the revision, so the builder cannot be deferred behind a boot-phase slot), and src/lib/database.ts resolves a rollback target's bundle identity through it. Narrowing was applied as far as it goes and is already reflected here: the module imports NOTHING but node builtins, server-only and @/lib leaves — the pure bundle-digest and router-lint helpers are INLINED rather than split into a second leaf (which would have cost +2 per route) and are deliberately not imported from @cinatra-ai/llm (that barrel re-exports packages/llm/src/registry.ts, which imports @/lib/database — a circular import through the store hub that broke prod boot). A guard test in src/lib/__tests__/skill-bundle-digest.test.ts pins the import surface so this +1 cannot silently become more. Retires the #2101 record for this route (its raise is baked into the base ceiling).",
      "pr": 2102
    },
    "/api/llm-bridge": {
      "from": 1601,
      "to": 1602,
      "reason": "#2088 S1 (epic #2086): the bundle-aware skill content authority adds exactly ONE new first-party module, src/lib/skill-bundle-store.ts — the store for skill_bundle_blobs / skill_revision_files / skill_bundle_heads. It is reachable from every locked route because src/lib/skill-lifecycle-store.ts appends its query builder to the SAME transaction as the custom/personal upsertSkill catalog write (the manifest MUST commit atomically with the revision, so the builder cannot be deferred behind a boot-phase slot), and src/lib/database.ts resolves a rollback target's bundle identity through it. Narrowing was applied as far as it goes and is already reflected here: the module imports NOTHING but node builtins, server-only and @/lib leaves — the pure bundle-digest and router-lint helpers are INLINED rather than split into a second leaf (which would have cost +2 per route) and are deliberately not imported from @cinatra-ai/llm (that barrel re-exports packages/llm/src/registry.ts, which imports @/lib/database — a circular import through the store hub that broke prod boot). A guard test in src/lib/__tests__/skill-bundle-digest.test.ts pins the import surface so this +1 cannot silently become more. Retires the #2101 record for this route (its raise is baked into the base ceiling).",
      "pr": 2102
    }
  }
}
