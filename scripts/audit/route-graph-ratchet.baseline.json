{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1667,
    "/api/mcp": 1663,
    "/chat": 1741,
    "/api/a2a": 1668,
    "/api/llm-bridge": 1678
  },
  "absorbs": {
    "/sign-in": {
      "from": 1666,
      "to": 1667,
      "reason": "cinatra#2359 (PR #2377): the post-login redirect-target fix adds ONE new pure module, src/lib/auth-redirect-target.ts (the single open-redirect validator + ?next= builder shared by the app-wide auth gate, the proxy, and the sign-in page), reached via the PRE-EXISTING edge into src/lib/auth-session.ts (already in every locked route's graph) — folding the validator directly into auth-session.ts to avoid the new module was considered and rejected: doing so for the proxy consumer (src/lib/auth-route-guard.ts) would require it to import auth-session.ts, which transitively pulls in src/lib/auth.ts's eager better-auth/DB-pool bootstrap — a real behavioral regression to the guard's documented cookie-only, no-DB-round-trip design — and duplicating the validator instead would violate the single-copy requirement. One shared module, one extra edge, +1 on every locked route.",
      "pr": 2377
    },
    "/api/mcp": {
      "from": 1662,
      "to": 1663,
      "reason": "cinatra#2359 (PR #2377): same shared open-redirect validator module (src/lib/auth-redirect-target.ts) reached via the pre-existing auth-session.ts edge — see the /sign-in record for the full rationale (folding into auth-session.ts was rejected because it would force the proxy to import the heavy DB-bootstrapping auth.ts graph; a second copy was rejected as duplication).",
      "pr": 2377
    },
    "/chat": {
      "from": 1740,
      "to": 1741,
      "reason": "cinatra#2359 (PR #2377): same shared open-redirect validator module (src/lib/auth-redirect-target.ts) reached via the pre-existing auth-session.ts edge — see the /sign-in record for the full rationale (folding into auth-session.ts was rejected because it would force the proxy to import the heavy DB-bootstrapping auth.ts graph; a second copy was rejected as duplication).",
      "pr": 2377
    },
    "/api/a2a": {
      "from": 1667,
      "to": 1668,
      "reason": "cinatra#2359 (PR #2377): same shared open-redirect validator module (src/lib/auth-redirect-target.ts) reached via the pre-existing auth-session.ts edge — see the /sign-in record for the full rationale (folding into auth-session.ts was rejected because it would force the proxy to import the heavy DB-bootstrapping auth.ts graph; a second copy was rejected as duplication).",
      "pr": 2377
    },
    "/api/llm-bridge": {
      "from": 1677,
      "to": 1678,
      "reason": "cinatra#2359 (PR #2377): same shared open-redirect validator module (src/lib/auth-redirect-target.ts) reached via the pre-existing auth-session.ts edge — see the /sign-in record for the full rationale (folding into auth-session.ts was rejected because it would force the proxy to import the heavy DB-bootstrapping auth.ts graph; a second copy was rejected as duplication).",
      "pr": 2377
    }
  }
}
