{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions \u2014 a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 218,
    "/api/mcp": 1715,
    "/chat": 1794,
    "/api/a2a": 1720,
    "/api/llm-bridge": 1730
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1718,
      "to": 1720,
      "pr": 2834,
      "reason": "cinatra#2771 (PR #2834 carries this record; the reason names the issue): the chat turn's per-turn input prefix gains TWO pure modules, and both are on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on these four routes, /sign-in unchanged. (1) packages/mcp-server/src/delegated-chat-tool-exposure.ts \u2014 the deterministic, zero-model-call tool-exposure resolver that narrows the hosted self-MCP reference to the tools a turn can plausibly use. It has to sit BESIDE the chat tool policy and stay dependency-free, because it consumes the policy's own accessors and must never become an authorization decision; folding it INTO the policy file was rejected because that file's allow-list source is being replaced by versioned admission, and mixing exposure into it would couple the two. (2) src/lib/assistant-runtime/chat-system-prefix.ts \u2014 the pure system-string composer that orders stable fragments ahead of volatile ones. It is a separate module precisely so the byte-stability property is unit-assertable without loading the server-only runtime graph; inlining it into runtime.ts was rejected because the test would then have to mock the entire turn producer to exercise four lines of concatenation. Measured with the pinned extension tree."
    },
    "/api/llm-bridge": {
      "from": 1728,
      "to": 1730,
      "pr": 2834,
      "reason": "cinatra#2771 (PR #2834 carries this record; the reason names the issue): the chat turn's per-turn input prefix gains TWO pure modules, and both are on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on these four routes, /sign-in unchanged. (1) packages/mcp-server/src/delegated-chat-tool-exposure.ts \u2014 the deterministic, zero-model-call tool-exposure resolver that narrows the hosted self-MCP reference to the tools a turn can plausibly use. It has to sit BESIDE the chat tool policy and stay dependency-free, because it consumes the policy's own accessors and must never become an authorization decision; folding it INTO the policy file was rejected because that file's allow-list source is being replaced by versioned admission, and mixing exposure into it would couple the two. (2) src/lib/assistant-runtime/chat-system-prefix.ts \u2014 the pure system-string composer that orders stable fragments ahead of volatile ones. It is a separate module precisely so the byte-stability property is unit-assertable without loading the server-only runtime graph; inlining it into runtime.ts was rejected because the test would then have to mock the entire turn producer to exercise four lines of concatenation. Measured with the pinned extension tree."
    },
    "/api/mcp": {
      "from": 1713,
      "to": 1715,
      "pr": 2834,
      "reason": "cinatra#2771 (PR #2834 carries this record; the reason names the issue): the chat turn's per-turn input prefix gains TWO pure modules, and both are on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on these four routes, /sign-in unchanged. (1) packages/mcp-server/src/delegated-chat-tool-exposure.ts \u2014 the deterministic, zero-model-call tool-exposure resolver that narrows the hosted self-MCP reference to the tools a turn can plausibly use. It has to sit BESIDE the chat tool policy and stay dependency-free, because it consumes the policy's own accessors and must never become an authorization decision; folding it INTO the policy file was rejected because that file's allow-list source is being replaced by versioned admission, and mixing exposure into it would couple the two. (2) src/lib/assistant-runtime/chat-system-prefix.ts \u2014 the pure system-string composer that orders stable fragments ahead of volatile ones. It is a separate module precisely so the byte-stability property is unit-assertable without loading the server-only runtime graph; inlining it into runtime.ts was rejected because the test would then have to mock the entire turn producer to exercise four lines of concatenation. Measured with the pinned extension tree."
    },
    "/chat": {
      "from": 1792,
      "to": 1794,
      "pr": 2834,
      "reason": "cinatra#2771 (PR #2834 carries this record; the reason names the issue): the chat turn's per-turn input prefix gains TWO pure modules, and both are on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on these four routes, /sign-in unchanged. (1) packages/mcp-server/src/delegated-chat-tool-exposure.ts \u2014 the deterministic, zero-model-call tool-exposure resolver that narrows the hosted self-MCP reference to the tools a turn can plausibly use. It has to sit BESIDE the chat tool policy and stay dependency-free, because it consumes the policy's own accessors and must never become an authorization decision; folding it INTO the policy file was rejected because that file's allow-list source is being replaced by versioned admission, and mixing exposure into it would couple the two. (2) src/lib/assistant-runtime/chat-system-prefix.ts \u2014 the pure system-string composer that orders stable fragments ahead of volatile ones. It is a separate module precisely so the byte-stability property is unit-assertable without loading the server-only runtime graph; inlining it into runtime.ts was rejected because the test would then have to mock the entire turn producer to exercise four lines of concatenation. Measured with the pinned extension tree."
    }
  }
}
