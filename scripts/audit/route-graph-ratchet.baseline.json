{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions \u2014 a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1673,
    "/api/mcp": 1669,
    "/chat": 1747,
    "/api/a2a": 1674,
    "/api/llm-bridge": 1684
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1673,
      "to": 1674,
      "reason": "cinatra#2578 usage-ledger choke point: ONE new first-party module, packages/llm/src/usage-metering.ts, reached by every locked route through the PRE-EXISTING route -> @cinatra-ai/llm barrel edge they all already carry (no new cross-package edge); +1 per locked route, uniformly. The module is the seam that makes the LLM spend ledger structural: resolveProviderAdapter \u2014 the single place a connector adapter is minted \u2014 now returns a metering proxy, so every provider call is recorded no matter which call site made it. Before this, every usage_events row came from a hand-written emit next to an adapter call, and any path that reached a provider some other way was billed and invisible (measured: ~36 gpt-5.5 requests, 1 ledger row). The +1 cannot be narrowed away: the seam must be importable by both packages/llm/src/index.ts and the two route modules that reach a provider without an adapter, so it has to be its own leaf. Folding it into an already-counted module (registry.ts, telemetry.ts) would merge unrelated concerns purely to avoid the counter. Counts measured with the companion extension repos cloned pinned.",
      "pr": 2588
    },
    "/api/llm-bridge": {
      "from": 1683,
      "to": 1684,
      "reason": "cinatra#2578 usage-ledger choke point: ONE new first-party module, packages/llm/src/usage-metering.ts, reached by every locked route through the PRE-EXISTING route -> @cinatra-ai/llm barrel edge they all already carry (no new cross-package edge); +1 per locked route, uniformly. The module is the seam that makes the LLM spend ledger structural: resolveProviderAdapter \u2014 the single place a connector adapter is minted \u2014 now returns a metering proxy, so every provider call is recorded no matter which call site made it. Before this, every usage_events row came from a hand-written emit next to an adapter call, and any path that reached a provider some other way was billed and invisible (measured: ~36 gpt-5.5 requests, 1 ledger row). The +1 cannot be narrowed away: the seam must be importable by both packages/llm/src/index.ts and the two route modules that reach a provider without an adapter, so it has to be its own leaf. Folding it into an already-counted module (registry.ts, telemetry.ts) would merge unrelated concerns purely to avoid the counter. Counts measured with the companion extension repos cloned pinned.",
      "pr": 2588
    },
    "/api/mcp": {
      "from": 1668,
      "to": 1669,
      "reason": "cinatra#2578 usage-ledger choke point: ONE new first-party module, packages/llm/src/usage-metering.ts, reached by every locked route through the PRE-EXISTING route -> @cinatra-ai/llm barrel edge they all already carry (no new cross-package edge); +1 per locked route, uniformly. The module is the seam that makes the LLM spend ledger structural: resolveProviderAdapter \u2014 the single place a connector adapter is minted \u2014 now returns a metering proxy, so every provider call is recorded no matter which call site made it. Before this, every usage_events row came from a hand-written emit next to an adapter call, and any path that reached a provider some other way was billed and invisible (measured: ~36 gpt-5.5 requests, 1 ledger row). The +1 cannot be narrowed away: the seam must be importable by both packages/llm/src/index.ts and the two route modules that reach a provider without an adapter, so it has to be its own leaf. Folding it into an already-counted module (registry.ts, telemetry.ts) would merge unrelated concerns purely to avoid the counter. Counts measured with the companion extension repos cloned pinned.",
      "pr": 2588
    },
    "/chat": {
      "from": 1746,
      "to": 1747,
      "reason": "cinatra#2578 usage-ledger choke point: ONE new first-party module, packages/llm/src/usage-metering.ts, reached by every locked route through the PRE-EXISTING route -> @cinatra-ai/llm barrel edge they all already carry (no new cross-package edge); +1 per locked route, uniformly. The module is the seam that makes the LLM spend ledger structural: resolveProviderAdapter \u2014 the single place a connector adapter is minted \u2014 now returns a metering proxy, so every provider call is recorded no matter which call site made it. Before this, every usage_events row came from a hand-written emit next to an adapter call, and any path that reached a provider some other way was billed and invisible (measured: ~36 gpt-5.5 requests, 1 ledger row). The +1 cannot be narrowed away: the seam must be importable by both packages/llm/src/index.ts and the two route modules that reach a provider without an adapter, so it has to be its own leaf. Folding it into an already-counted module (registry.ts, telemetry.ts) would merge unrelated concerns purely to avoid the counter. Counts measured with the companion extension repos cloned pinned.",
      "pr": 2588
    },
    "/sign-in": {
      "from": 1672,
      "to": 1673,
      "reason": "cinatra#2578 usage-ledger choke point: ONE new first-party module, packages/llm/src/usage-metering.ts, reached by every locked route through the PRE-EXISTING route -> @cinatra-ai/llm barrel edge they all already carry (no new cross-package edge); +1 per locked route, uniformly. The module is the seam that makes the LLM spend ledger structural: resolveProviderAdapter \u2014 the single place a connector adapter is minted \u2014 now returns a metering proxy, so every provider call is recorded no matter which call site made it. Before this, every usage_events row came from a hand-written emit next to an adapter call, and any path that reached a provider some other way was billed and invisible (measured: ~36 gpt-5.5 requests, 1 ledger row). The +1 cannot be narrowed away: the seam must be importable by both packages/llm/src/index.ts and the two route modules that reach a provider without an adapter, so it has to be its own leaf. Folding it into an already-counted module (registry.ts, telemetry.ts) would merge unrelated concerns purely to avoid the counter. Counts measured with the companion extension repos cloned pinned.",
      "pr": 2588
    }
  }
}
