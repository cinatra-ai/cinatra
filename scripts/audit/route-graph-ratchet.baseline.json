{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1490,
    "/api/mcp": 1486,
    "/chat": 1546,
    "/api/a2a": 1491,
    "/api/llm-bridge": 1497
  },
  "absorbs": {
    "/sign-in": {
      "from": 1487,
      "to": 1490,
      "reason": "linkedin host-wiring (cinatra#1457) wires the draftable mutability lock into the objects_save/objects_update content-write paths (packages/objects/src/mcp/handlers.ts enforceDraftableLock), which lazy-imports src/lib/objects/draftable-lock-gate.ts. route-graph counts dynamic import() reachability, so the lock gate plus its three app-layer first-party deps (src/lib/objects/artifact-claim-store.ts, src/lib/artifacts/publication-ledger.ts, src/lib/artifacts/publication-ledger-types.ts) become first-party-reachable from every route whose graph reaches the objects MCP handler tree — a uniform +3 vs the committed ceiling. The lazy import keeps the STATIC handler edge out; the write-path lock genuinely must consult the publication ledger, so this is intrinsic feature growth, not a barrel over-import. Measured A/B with the companion extension repos cloned pinned (missingCount 0): a clean +3 (1487->1490).",
      "pr": 1831
    },
    "/api/mcp": {
      "from": 1483,
      "to": 1486,
      "reason": "linkedin host-wiring (cinatra#1457) wires the draftable mutability lock into the objects_save/objects_update content-write paths (packages/objects/src/mcp/handlers.ts enforceDraftableLock), which lazy-imports src/lib/objects/draftable-lock-gate.ts. route-graph counts dynamic import() reachability, so the lock gate plus its three app-layer first-party deps (src/lib/objects/artifact-claim-store.ts, src/lib/artifacts/publication-ledger.ts, src/lib/artifacts/publication-ledger-types.ts) become first-party-reachable from every route whose graph reaches the objects MCP handler tree — a uniform +3 vs the committed ceiling. The lazy import keeps the STATIC handler edge out; the write-path lock genuinely must consult the publication ledger, so this is intrinsic feature growth, not a barrel over-import. Measured A/B with the companion extension repos cloned pinned (missingCount 0): a clean +3 (1483->1486).",
      "pr": 1831
    },
    "/chat": {
      "from": 1543,
      "to": 1546,
      "reason": "linkedin host-wiring (cinatra#1457) wires the draftable mutability lock into the objects_save/objects_update content-write paths (packages/objects/src/mcp/handlers.ts enforceDraftableLock), which lazy-imports src/lib/objects/draftable-lock-gate.ts. route-graph counts dynamic import() reachability, so the lock gate plus its three app-layer first-party deps (src/lib/objects/artifact-claim-store.ts, src/lib/artifacts/publication-ledger.ts, src/lib/artifacts/publication-ledger-types.ts) become first-party-reachable from every route whose graph reaches the objects MCP handler tree — a uniform +3 vs the committed ceiling. The lazy import keeps the STATIC handler edge out; the write-path lock genuinely must consult the publication ledger, so this is intrinsic feature growth, not a barrel over-import. Measured A/B with the companion extension repos cloned pinned (missingCount 0): a clean +3 (1543->1546).",
      "pr": 1831
    },
    "/api/a2a": {
      "from": 1488,
      "to": 1491,
      "reason": "linkedin host-wiring (cinatra#1457) wires the draftable mutability lock into the objects_save/objects_update content-write paths (packages/objects/src/mcp/handlers.ts enforceDraftableLock), which lazy-imports src/lib/objects/draftable-lock-gate.ts. route-graph counts dynamic import() reachability, so the lock gate plus its three app-layer first-party deps (src/lib/objects/artifact-claim-store.ts, src/lib/artifacts/publication-ledger.ts, src/lib/artifacts/publication-ledger-types.ts) become first-party-reachable from every route whose graph reaches the objects MCP handler tree — a uniform +3 vs the committed ceiling. The lazy import keeps the STATIC handler edge out; the write-path lock genuinely must consult the publication ledger, so this is intrinsic feature growth, not a barrel over-import. Measured A/B with the companion extension repos cloned pinned (missingCount 0): a clean +3 (1488->1491).",
      "pr": 1831
    },
    "/api/llm-bridge": {
      "from": 1494,
      "to": 1497,
      "reason": "linkedin host-wiring (cinatra#1457) wires the draftable mutability lock into the objects_save/objects_update content-write paths (packages/objects/src/mcp/handlers.ts enforceDraftableLock), which lazy-imports src/lib/objects/draftable-lock-gate.ts. route-graph counts dynamic import() reachability, so the lock gate plus its three app-layer first-party deps (src/lib/objects/artifact-claim-store.ts, src/lib/artifacts/publication-ledger.ts, src/lib/artifacts/publication-ledger-types.ts) become first-party-reachable from every route whose graph reaches the objects MCP handler tree — a uniform +3 vs the committed ceiling. The lazy import keeps the STATIC handler edge out; the write-path lock genuinely must consult the publication ledger, so this is intrinsic feature growth, not a barrel over-import. Measured A/B with the companion extension repos cloned pinned (missingCount 0): a clean +3 (1494->1497).",
      "pr": 1831
    }
  }
}
