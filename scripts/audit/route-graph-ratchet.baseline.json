{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions \u2014 a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1612,
    "/api/mcp": 1608,
    "/chat": 1682,
    "/api/a2a": 1613,
    "/api/llm-bridge": 1623
  },
  "absorbs": {
    "/sign-in": {
      "from": 1611,
      "to": 1612,
      "reason": "#2089 S2 (epic #2086): the skill packaging + structure enforcement seam adds exactly ONE new first-party module, src/lib/skill-packaging-install-gate.ts \u2014 the pre-finalize install gate that applies the shared packaging verdict to a materialized extension. It is reachable from every locked route because it runs INSIDE src/lib/extension-install-pipeline.ts's inert window (after the SRI-verified materialization, BEFORE beginInstallOp), so it cannot be deferred behind a boot-phase slot without giving up the property that makes a refusal safe: nothing durable has mutated yet. NARROWING WAS APPLIED AND IS ALREADY REFLECTED HERE. The verdict itself \u2014 every rule, the frontmatter reader, the one-hop router lint, the glob matcher \u2014 lives in scripts/audit/_lib/skill-packaging-verdict.mjs, a dependency-free module OUTSIDE the first-party roots, so it costs ZERO modules; a TypeScript twin under src/lib would have cost +2 per route and would have let CI and store install drift apart. Its two policy documents (config/skill-fixture-allowlist.json, config/skill-packaging-legacy-exceptions.json) are JSON imports, not modules. The gate module itself imports nothing but node builtins, those two JSON documents and that one .mjs \u2014 no @cinatra-ai barrel, no store, no pg. Same shape and same justification as this epic's #2088 record one slice earlier.",
      "pr": 2108
    },
    "/api/mcp": {
      "from": 1607,
      "to": 1608,
      "reason": "#2089 S2 (epic #2086): the skill packaging + structure enforcement seam adds exactly ONE new first-party module, src/lib/skill-packaging-install-gate.ts \u2014 the pre-finalize install gate that applies the shared packaging verdict to a materialized extension. It is reachable from every locked route because it runs INSIDE src/lib/extension-install-pipeline.ts's inert window (after the SRI-verified materialization, BEFORE beginInstallOp), so it cannot be deferred behind a boot-phase slot without giving up the property that makes a refusal safe: nothing durable has mutated yet. NARROWING WAS APPLIED AND IS ALREADY REFLECTED HERE. The verdict itself \u2014 every rule, the frontmatter reader, the one-hop router lint, the glob matcher \u2014 lives in scripts/audit/_lib/skill-packaging-verdict.mjs, a dependency-free module OUTSIDE the first-party roots, so it costs ZERO modules; a TypeScript twin under src/lib would have cost +2 per route and would have let CI and store install drift apart. Its two policy documents (config/skill-fixture-allowlist.json, config/skill-packaging-legacy-exceptions.json) are JSON imports, not modules. The gate module itself imports nothing but node builtins, those two JSON documents and that one .mjs \u2014 no @cinatra-ai barrel, no store, no pg. Same shape and same justification as this epic's #2088 record one slice earlier.",
      "pr": 2108
    },
    "/chat": {
      "from": 1681,
      "to": 1682,
      "reason": "#2089 S2 (epic #2086): the skill packaging + structure enforcement seam adds exactly ONE new first-party module, src/lib/skill-packaging-install-gate.ts \u2014 the pre-finalize install gate that applies the shared packaging verdict to a materialized extension. It is reachable from every locked route because it runs INSIDE src/lib/extension-install-pipeline.ts's inert window (after the SRI-verified materialization, BEFORE beginInstallOp), so it cannot be deferred behind a boot-phase slot without giving up the property that makes a refusal safe: nothing durable has mutated yet. NARROWING WAS APPLIED AND IS ALREADY REFLECTED HERE. The verdict itself \u2014 every rule, the frontmatter reader, the one-hop router lint, the glob matcher \u2014 lives in scripts/audit/_lib/skill-packaging-verdict.mjs, a dependency-free module OUTSIDE the first-party roots, so it costs ZERO modules; a TypeScript twin under src/lib would have cost +2 per route and would have let CI and store install drift apart. Its two policy documents (config/skill-fixture-allowlist.json, config/skill-packaging-legacy-exceptions.json) are JSON imports, not modules. The gate module itself imports nothing but node builtins, those two JSON documents and that one .mjs \u2014 no @cinatra-ai barrel, no store, no pg. Same shape and same justification as this epic's #2088 record one slice earlier.",
      "pr": 2108
    },
    "/api/a2a": {
      "from": 1612,
      "to": 1613,
      "reason": "#2089 S2 (epic #2086): the skill packaging + structure enforcement seam adds exactly ONE new first-party module, src/lib/skill-packaging-install-gate.ts \u2014 the pre-finalize install gate that applies the shared packaging verdict to a materialized extension. It is reachable from every locked route because it runs INSIDE src/lib/extension-install-pipeline.ts's inert window (after the SRI-verified materialization, BEFORE beginInstallOp), so it cannot be deferred behind a boot-phase slot without giving up the property that makes a refusal safe: nothing durable has mutated yet. NARROWING WAS APPLIED AND IS ALREADY REFLECTED HERE. The verdict itself \u2014 every rule, the frontmatter reader, the one-hop router lint, the glob matcher \u2014 lives in scripts/audit/_lib/skill-packaging-verdict.mjs, a dependency-free module OUTSIDE the first-party roots, so it costs ZERO modules; a TypeScript twin under src/lib would have cost +2 per route and would have let CI and store install drift apart. Its two policy documents (config/skill-fixture-allowlist.json, config/skill-packaging-legacy-exceptions.json) are JSON imports, not modules. The gate module itself imports nothing but node builtins, those two JSON documents and that one .mjs \u2014 no @cinatra-ai barrel, no store, no pg. Same shape and same justification as this epic's #2088 record one slice earlier.",
      "pr": 2108
    },
    "/api/llm-bridge": {
      "from": 1622,
      "to": 1623,
      "reason": "#2089 S2 (epic #2086): the skill packaging + structure enforcement seam adds exactly ONE new first-party module, src/lib/skill-packaging-install-gate.ts \u2014 the pre-finalize install gate that applies the shared packaging verdict to a materialized extension. It is reachable from every locked route because it runs INSIDE src/lib/extension-install-pipeline.ts's inert window (after the SRI-verified materialization, BEFORE beginInstallOp), so it cannot be deferred behind a boot-phase slot without giving up the property that makes a refusal safe: nothing durable has mutated yet. NARROWING WAS APPLIED AND IS ALREADY REFLECTED HERE. The verdict itself \u2014 every rule, the frontmatter reader, the one-hop router lint, the glob matcher \u2014 lives in scripts/audit/_lib/skill-packaging-verdict.mjs, a dependency-free module OUTSIDE the first-party roots, so it costs ZERO modules; a TypeScript twin under src/lib would have cost +2 per route and would have let CI and store install drift apart. Its two policy documents (config/skill-fixture-allowlist.json, config/skill-packaging-legacy-exceptions.json) are JSON imports, not modules. The gate module itself imports nothing but node builtins, those two JSON documents and that one .mjs \u2014 no @cinatra-ai barrel, no store, no pg. Same shape and same justification as this epic's #2088 record one slice earlier.",
      "pr": 2108
    }
  }
}
