{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 223,
    "/api/mcp": 1746,
    "/chat": 1847,
    "/api/a2a": 1751,
    "/api/llm-bridge": 1761
  },
  "absorbs": {
    "/sign-in": {
      "from": 222,
      "to": 223,
      "reason": "cinatra#2988 (lifecycle-b W5a, cinatra#2932) carries this record — this merge brings origin/main forward into the W5a branch, so `from` (222) is main's OWN committed ceiling at 1fb86826b078b7031c422cfab7c60c0182d9b8f3. Main adds NOTHING to this route: its trigger-claim leaf (PR #2987, cinatra#2981) and its two recommendation-hold cores (PR #2890, cinatra#2790 S9f host parity, which also carries the cinatra#2997 placeholder review slot) are not reachable from the sign-in entry, and main measures 222 here unchanged. The whole +1 is this branch's own: ONE leaf of the W5a slice reachable from the auth plugins — the six leaves the slice adds (the bound-reference resolver, the bound-turn actor, the bound-card binding, the grant codec, its single-use store and the store's DDL leaf) are reached from `runAssistantTurn`, which mints the grant at send time, and only one of them lands on this route's graph. MEASURED THE WAY CI MEASURES, not computed: with the companion extension fleet cloned PINNED (112/112 at their lock SHAs, missingCount 0) and an identical scripts/route-graph.mjs, THREE trees were analysed in ONE environment — origin/main at 1fb86826b078b7031c422cfab7c60c0182d9b8f3 measures 222, this branch's pre-merge head at f84dc8a9758e9d2647e9d47acdc3e463346a23a1 reproduces this branch's own committed ceiling EXACTLY (223), and the merge measures 223: the branch's ceiling unchanged, so nothing of main's composed onto it. The carried-forward PR #2928 record for this route is RETIRED BY THIS RAISE, which is the one lifecycle the gate allows for an annotation whose ceiling moves.",
      "pr": 2988
    },
    "/api/mcp": {
      "from": 1738,
      "to": 1746,
      "reason": "Pull request #2998 carries this record - cinatra#2934 (epic #2926 W5c, filling a form by asking and the review page's typed road), MERGED with main at 8028fef4effd80d5c0c1625f2b114dd345f66c9b, so `from` (1738) is MAIN'S OWN committed ceiling at that commit and nothing main already absorbed is re-absorbed here: the memory promotion slice (pull request #3038, cinatra#1381), the per-run prompt window (pull request #2995, cinatra#2933), the HITL screen's card on every host (pull request #3014, cinatra#2930), the artifact renderer dispatch (pull request #2983, cinatra#2931), the W5d start (pull request #2996, cinatra#2935), the declaration-bound delegated-chat admission (pull request #2914, cinatra#2817), the lifecycle-b W5a raise (pull request #2988, cinatra#2932) and the recommendation-hold cores (pull request #2890, cinatra#2790 S9f host parity) are ALL ALREADY INSIDE 1738. The record this raise replaces for this route is the pre-merge record this same pull request carried, RETIRED BY THIS RAISE - the one lifecycle the gate allows for an annotation whose ceiling moves.\n\nWHAT THE RAISE IS, measured as a module SET and not only as a number: exactly SIX modules enter this route's graph and none leaves, and it is the SAME six on every route this record covers.\nFOUR are the app's own src - the fill road itself:\n  src/lib/lifecycle/bound-screen-fill.ts      - the fill road's core,\n  src/lib/lifecycle/bound-screen-fill-mcp.ts  - the one self-MCP primitive that exposes it,\n  src/lib/lifecycle/bound-screen-controls.ts  - the pure projection that says which controls a screen DRAWS and which values each of them can hold,\n  src/lib/lifecycle/schedule-form-screen.ts   - the scheduler form's own rows as a bound screen.\nThey enter through an edge this route's graph already contained, src/lib/mcp-server.ts, which registers the fill beside the lent action W5a already registered there.\nTWO are the renderer this slice MOVED, in the workspace package:\n  packages/agents/src/markdown-render-core.ts - the one definition of the markup and of the escaping the untrusted-source rule depends on,\n  packages/agents/src/run-window-markdown.ts  - the window's one-line reading of it.\nThey exist because the per-run window drew the assistant's markdown raw and the graded picture leg caught it; the renderer could not be imported from @cinatra-ai/chat because that package DEPENDS ON @cinatra-ai/agents, so it moved DOWN to the package both sides may reach. marked was already an agents dependency and nothing new was added to make the move.\nPER OWNER the whole delta on every one of the four routes is TWO owners and nothing else: (app) src +4 and @cinatra-ai/agents +2. Every other owner of every one of these graphs measures exactly the same count in the control and at this head. No barrel import is opened and no new package edge is created: the agents package was already on each of these graphs.\n\nWHY THE COMMITTED CEILING RISES BY 5 WHILE THE MODULE SET GROWS BY 6, stated rather than left to be noticed: main's committed ceiling for this route carries ONE of headroom over main's own measurement - main measures 1737 against a committed 1738 - and this record sets the ceiling AT the merged head's measurement, headroom 0. 1737 + 6 = 1743, and 1743 - 1738 = 5 is the raise the gate compares against `from`/`to`.\n\nWHAT THE RETIRED ROUTE TAKES AWAY does not show here, and that is stated rather than left to be noticed: this slice DELETES src/app/api/agents/builder/[templateId]/hitl-assist/route.ts, which is its own route entry and is reachable from none of the five tracked ones, so retiring it moves no tracked count.\n\nHOW IT WAS MEASURED, the way CI measures, and re-measured at THIS merge: the companion extension fleet cloned PINNED (112/112 at their lock SHAs) in BOTH trees, missingCount 0 on EVERY tracked route of both, and a byte-identical scripts/route-graph.mjs. TWO trees analysed in ONE environment. A CONTROL WORKTREE at main's own tip 8028fef4effd80d5c0c1625f2b114dd345f66c9b measures /api/mcp 1737, /chat 1838, /api/a2a 1742, /api/llm-bridge 1752 and /sign-in 161; this merged head measures /api/mcp 1743, /chat 1844, /api/a2a 1748, /api/llm-bridge 1758 and /sign-in 161 - the same +6 on each of the four, in the two owners named above, and nothing on /sign-in.\n\n/sign-in KEEPS ITS CEILING AND ITS CARRIED-FORWARD RECORD, byte-identical to main's. It measures 161 in the control and 161 at this head against a committed 223, and locking that shrink in is main's own change to make, not this slice's: retiring an annotation is reserved for the change that MOVES the ceiling.\n\nSECOND RAISE ON THIS SAME RECORD, +3 ON EACH OF THE FOUR (cinatra#2934, the armed-trigger tab). The maintainer's CHANGES REQUESTED reading on this pull request refuses its Deviation 1: an armed schedule must be changeable from the window on the agent's Schedule tab for as long as the form can still be edited. Serving it puts the ARMED scheduler form on the one road as a bound screen of its own, and the whole point of the change is that the window uses THE FORM'S OWN predicate and THE BUTTON'S OWN save rather than a parallel pair - so exactly the three modules that own those two things become reachable from a bound turn, and no others.\n\nMEASURED AS A MODULE SET, per owner, on one tracked route and identically on the other three: (app) src +1 and @cinatra-ai/agents +2, every other owner unchanged.\n  src/lib/lifecycle/trigger-schedule-proposal-card.ts        - the card's own decide entry. `decideTriggerScheduleProposal` with the card's `save` op is the function `/api/lifecycle-views/decide` calls when the person presses Save changes; the ask takes it with the same ref and the same actor, which is what \"one road for the press and for the ask\" means.\n  packages/agents/src/trigger-schedule-proposal-service.ts   - `resolveProposalForRun`, the call that computes the settled card's `body.canSave` (`canSaveInstalled`). The window reads that boolean rather than deriving one, so the box and the button cannot disagree about which schedules are still changeable.\n  packages/agents/src/trigger-service.ts                     - `saveScheduleRefusalFor`, a two-line wrapper over `saveScheduleGuardRefusal`, the guard `updateRunTriggerScheduleForActor` asks before it delegates and again inside the setter. It is what lets a refusal be said in the server's own words instead of a blank one.\n\nTHE SET IS MINIMAL AND CANNOT BE NARROWED FURTHER without abandoning the requirement: the decide entry reaches both services on its own, so any road that presses the card's own button carries all three, and any road that avoided them would be the parallel predicate the review refuses. All three are reached through LITERAL dynamic imports resolved at the call, so nothing new is pulled in at module load; the ratchet counts reachability, which is why the count moves at all. The placement rule the fill needs on both sides of the wire was deliberately put in the recurrence VOCABULARY (`packages/agents/src/trigger-recurrence.ts`), which every one of these graphs already reached, rather than in a leaf of its own - that is why this raise is 3 and not 4.\n\nHOW IT WAS MEASURED: with the companion extension fleet cloned PINNED (112/112 at their lock SHAs, missingCount 0) and a byte-identical scripts/route-graph.mjs, TWO trees in ONE environment - this pull request's own head as it stood before this change, which reproduces the ceilings committed above EXACTLY (/api/mcp 1743, /chat 1844, /api/a2a 1748, /api/llm-bridge 1758), and this head, which measures /api/mcp 1746, /chat 1847, /api/a2a 1751 and /api/llm-bridge 1761. The same +3 on each of the four, in the two owners named above. /sign-in is untouched by this change and keeps its ceiling and its carried-forward record.",
      "pr": 2998
    },
    "/chat": {
      "from": 1839,
      "to": 1847,
      "reason": "Pull request #2998 carries this record - cinatra#2934 (epic #2926 W5c, filling a form by asking and the review page's typed road), MERGED with main at 8028fef4effd80d5c0c1625f2b114dd345f66c9b, so `from` (1839) is MAIN'S OWN committed ceiling at that commit and nothing main already absorbed is re-absorbed here: the memory promotion slice (pull request #3038, cinatra#1381), the per-run prompt window (pull request #2995, cinatra#2933), the HITL screen's card on every host (pull request #3014, cinatra#2930), the artifact renderer dispatch (pull request #2983, cinatra#2931), the W5d start (pull request #2996, cinatra#2935), the declaration-bound delegated-chat admission (pull request #2914, cinatra#2817), the lifecycle-b W5a raise (pull request #2988, cinatra#2932) and the recommendation-hold cores (pull request #2890, cinatra#2790 S9f host parity) are ALL ALREADY INSIDE 1839. The record this raise replaces for this route is the pre-merge record this same pull request carried, RETIRED BY THIS RAISE - the one lifecycle the gate allows for an annotation whose ceiling moves.\n\nWHAT THE RAISE IS, measured as a module SET and not only as a number: exactly SIX modules enter this route's graph and none leaves, and it is the SAME six on every route this record covers.\nFOUR are the app's own src - the fill road itself:\n  src/lib/lifecycle/bound-screen-fill.ts      - the fill road's core,\n  src/lib/lifecycle/bound-screen-fill-mcp.ts  - the one self-MCP primitive that exposes it,\n  src/lib/lifecycle/bound-screen-controls.ts  - the pure projection that says which controls a screen DRAWS and which values each of them can hold,\n  src/lib/lifecycle/schedule-form-screen.ts   - the scheduler form's own rows as a bound screen.\nThey enter through an edge this route's graph already contained, src/lib/mcp-server.ts, which registers the fill beside the lent action W5a already registered there.\nTWO are the renderer this slice MOVED, in the workspace package:\n  packages/agents/src/markdown-render-core.ts - the one definition of the markup and of the escaping the untrusted-source rule depends on,\n  packages/agents/src/run-window-markdown.ts  - the window's one-line reading of it.\nThey exist because the per-run window drew the assistant's markdown raw and the graded picture leg caught it; the renderer could not be imported from @cinatra-ai/chat because that package DEPENDS ON @cinatra-ai/agents, so it moved DOWN to the package both sides may reach. marked was already an agents dependency and nothing new was added to make the move.\nPER OWNER the whole delta on every one of the four routes is TWO owners and nothing else: (app) src +4 and @cinatra-ai/agents +2. Every other owner of every one of these graphs measures exactly the same count in the control and at this head. No barrel import is opened and no new package edge is created: the agents package was already on each of these graphs.\n\nWHY THE COMMITTED CEILING RISES BY 5 WHILE THE MODULE SET GROWS BY 6, stated rather than left to be noticed: main's committed ceiling for this route carries ONE of headroom over main's own measurement - main measures 1838 against a committed 1839 - and this record sets the ceiling AT the merged head's measurement, headroom 0. 1838 + 6 = 1844, and 1844 - 1839 = 5 is the raise the gate compares against `from`/`to`.\n\nWHAT THE RETIRED ROUTE TAKES AWAY does not show here, and that is stated rather than left to be noticed: this slice DELETES src/app/api/agents/builder/[templateId]/hitl-assist/route.ts, which is its own route entry and is reachable from none of the five tracked ones, so retiring it moves no tracked count.\n\nHOW IT WAS MEASURED, the way CI measures, and re-measured at THIS merge: the companion extension fleet cloned PINNED (112/112 at their lock SHAs) in BOTH trees, missingCount 0 on EVERY tracked route of both, and a byte-identical scripts/route-graph.mjs. TWO trees analysed in ONE environment. A CONTROL WORKTREE at main's own tip 8028fef4effd80d5c0c1625f2b114dd345f66c9b measures /api/mcp 1737, /chat 1838, /api/a2a 1742, /api/llm-bridge 1752 and /sign-in 161; this merged head measures /api/mcp 1743, /chat 1844, /api/a2a 1748, /api/llm-bridge 1758 and /sign-in 161 - the same +6 on each of the four, in the two owners named above, and nothing on /sign-in.\n\n/sign-in KEEPS ITS CEILING AND ITS CARRIED-FORWARD RECORD, byte-identical to main's. It measures 161 in the control and 161 at this head against a committed 223, and locking that shrink in is main's own change to make, not this slice's: retiring an annotation is reserved for the change that MOVES the ceiling.\n\nSECOND RAISE ON THIS SAME RECORD, +3 ON EACH OF THE FOUR (cinatra#2934, the armed-trigger tab). The maintainer's CHANGES REQUESTED reading on this pull request refuses its Deviation 1: an armed schedule must be changeable from the window on the agent's Schedule tab for as long as the form can still be edited. Serving it puts the ARMED scheduler form on the one road as a bound screen of its own, and the whole point of the change is that the window uses THE FORM'S OWN predicate and THE BUTTON'S OWN save rather than a parallel pair - so exactly the three modules that own those two things become reachable from a bound turn, and no others.\n\nMEASURED AS A MODULE SET, per owner, on one tracked route and identically on the other three: (app) src +1 and @cinatra-ai/agents +2, every other owner unchanged.\n  src/lib/lifecycle/trigger-schedule-proposal-card.ts        - the card's own decide entry. `decideTriggerScheduleProposal` with the card's `save` op is the function `/api/lifecycle-views/decide` calls when the person presses Save changes; the ask takes it with the same ref and the same actor, which is what \"one road for the press and for the ask\" means.\n  packages/agents/src/trigger-schedule-proposal-service.ts   - `resolveProposalForRun`, the call that computes the settled card's `body.canSave` (`canSaveInstalled`). The window reads that boolean rather than deriving one, so the box and the button cannot disagree about which schedules are still changeable.\n  packages/agents/src/trigger-service.ts                     - `saveScheduleRefusalFor`, a two-line wrapper over `saveScheduleGuardRefusal`, the guard `updateRunTriggerScheduleForActor` asks before it delegates and again inside the setter. It is what lets a refusal be said in the server's own words instead of a blank one.\n\nTHE SET IS MINIMAL AND CANNOT BE NARROWED FURTHER without abandoning the requirement: the decide entry reaches both services on its own, so any road that presses the card's own button carries all three, and any road that avoided them would be the parallel predicate the review refuses. All three are reached through LITERAL dynamic imports resolved at the call, so nothing new is pulled in at module load; the ratchet counts reachability, which is why the count moves at all. The placement rule the fill needs on both sides of the wire was deliberately put in the recurrence VOCABULARY (`packages/agents/src/trigger-recurrence.ts`), which every one of these graphs already reached, rather than in a leaf of its own - that is why this raise is 3 and not 4.\n\nHOW IT WAS MEASURED: with the companion extension fleet cloned PINNED (112/112 at their lock SHAs, missingCount 0) and a byte-identical scripts/route-graph.mjs, TWO trees in ONE environment - this pull request's own head as it stood before this change, which reproduces the ceilings committed above EXACTLY (/api/mcp 1743, /chat 1844, /api/a2a 1748, /api/llm-bridge 1758), and this head, which measures /api/mcp 1746, /chat 1847, /api/a2a 1751 and /api/llm-bridge 1761. The same +3 on each of the four, in the two owners named above. /sign-in is untouched by this change and keeps its ceiling and its carried-forward record.",
      "pr": 2998
    },
    "/api/a2a": {
      "from": 1743,
      "to": 1751,
      "reason": "Pull request #2998 carries this record - cinatra#2934 (epic #2926 W5c, filling a form by asking and the review page's typed road), MERGED with main at 8028fef4effd80d5c0c1625f2b114dd345f66c9b, so `from` (1743) is MAIN'S OWN committed ceiling at that commit and nothing main already absorbed is re-absorbed here: the memory promotion slice (pull request #3038, cinatra#1381), the per-run prompt window (pull request #2995, cinatra#2933), the HITL screen's card on every host (pull request #3014, cinatra#2930), the artifact renderer dispatch (pull request #2983, cinatra#2931), the W5d start (pull request #2996, cinatra#2935), the declaration-bound delegated-chat admission (pull request #2914, cinatra#2817), the lifecycle-b W5a raise (pull request #2988, cinatra#2932) and the recommendation-hold cores (pull request #2890, cinatra#2790 S9f host parity) are ALL ALREADY INSIDE 1743. The record this raise replaces for this route is the pre-merge record this same pull request carried, RETIRED BY THIS RAISE - the one lifecycle the gate allows for an annotation whose ceiling moves.\n\nWHAT THE RAISE IS, measured as a module SET and not only as a number: exactly SIX modules enter this route's graph and none leaves, and it is the SAME six on every route this record covers.\nFOUR are the app's own src - the fill road itself:\n  src/lib/lifecycle/bound-screen-fill.ts      - the fill road's core,\n  src/lib/lifecycle/bound-screen-fill-mcp.ts  - the one self-MCP primitive that exposes it,\n  src/lib/lifecycle/bound-screen-controls.ts  - the pure projection that says which controls a screen DRAWS and which values each of them can hold,\n  src/lib/lifecycle/schedule-form-screen.ts   - the scheduler form's own rows as a bound screen.\nThey enter through an edge this route's graph already contained, src/lib/mcp-server.ts, which registers the fill beside the lent action W5a already registered there.\nTWO are the renderer this slice MOVED, in the workspace package:\n  packages/agents/src/markdown-render-core.ts - the one definition of the markup and of the escaping the untrusted-source rule depends on,\n  packages/agents/src/run-window-markdown.ts  - the window's one-line reading of it.\nThey exist because the per-run window drew the assistant's markdown raw and the graded picture leg caught it; the renderer could not be imported from @cinatra-ai/chat because that package DEPENDS ON @cinatra-ai/agents, so it moved DOWN to the package both sides may reach. marked was already an agents dependency and nothing new was added to make the move.\nPER OWNER the whole delta on every one of the four routes is TWO owners and nothing else: (app) src +4 and @cinatra-ai/agents +2. Every other owner of every one of these graphs measures exactly the same count in the control and at this head. No barrel import is opened and no new package edge is created: the agents package was already on each of these graphs.\n\nWHY THE COMMITTED CEILING RISES BY 5 WHILE THE MODULE SET GROWS BY 6, stated rather than left to be noticed: main's committed ceiling for this route carries ONE of headroom over main's own measurement - main measures 1742 against a committed 1743 - and this record sets the ceiling AT the merged head's measurement, headroom 0. 1742 + 6 = 1748, and 1748 - 1743 = 5 is the raise the gate compares against `from`/`to`.\n\nWHAT THE RETIRED ROUTE TAKES AWAY does not show here, and that is stated rather than left to be noticed: this slice DELETES src/app/api/agents/builder/[templateId]/hitl-assist/route.ts, which is its own route entry and is reachable from none of the five tracked ones, so retiring it moves no tracked count.\n\nHOW IT WAS MEASURED, the way CI measures, and re-measured at THIS merge: the companion extension fleet cloned PINNED (112/112 at their lock SHAs) in BOTH trees, missingCount 0 on EVERY tracked route of both, and a byte-identical scripts/route-graph.mjs. TWO trees analysed in ONE environment. A CONTROL WORKTREE at main's own tip 8028fef4effd80d5c0c1625f2b114dd345f66c9b measures /api/mcp 1737, /chat 1838, /api/a2a 1742, /api/llm-bridge 1752 and /sign-in 161; this merged head measures /api/mcp 1743, /chat 1844, /api/a2a 1748, /api/llm-bridge 1758 and /sign-in 161 - the same +6 on each of the four, in the two owners named above, and nothing on /sign-in.\n\n/sign-in KEEPS ITS CEILING AND ITS CARRIED-FORWARD RECORD, byte-identical to main's. It measures 161 in the control and 161 at this head against a committed 223, and locking that shrink in is main's own change to make, not this slice's: retiring an annotation is reserved for the change that MOVES the ceiling.\n\nSECOND RAISE ON THIS SAME RECORD, +3 ON EACH OF THE FOUR (cinatra#2934, the armed-trigger tab). The maintainer's CHANGES REQUESTED reading on this pull request refuses its Deviation 1: an armed schedule must be changeable from the window on the agent's Schedule tab for as long as the form can still be edited. Serving it puts the ARMED scheduler form on the one road as a bound screen of its own, and the whole point of the change is that the window uses THE FORM'S OWN predicate and THE BUTTON'S OWN save rather than a parallel pair - so exactly the three modules that own those two things become reachable from a bound turn, and no others.\n\nMEASURED AS A MODULE SET, per owner, on one tracked route and identically on the other three: (app) src +1 and @cinatra-ai/agents +2, every other owner unchanged.\n  src/lib/lifecycle/trigger-schedule-proposal-card.ts        - the card's own decide entry. `decideTriggerScheduleProposal` with the card's `save` op is the function `/api/lifecycle-views/decide` calls when the person presses Save changes; the ask takes it with the same ref and the same actor, which is what \"one road for the press and for the ask\" means.\n  packages/agents/src/trigger-schedule-proposal-service.ts   - `resolveProposalForRun`, the call that computes the settled card's `body.canSave` (`canSaveInstalled`). The window reads that boolean rather than deriving one, so the box and the button cannot disagree about which schedules are still changeable.\n  packages/agents/src/trigger-service.ts                     - `saveScheduleRefusalFor`, a two-line wrapper over `saveScheduleGuardRefusal`, the guard `updateRunTriggerScheduleForActor` asks before it delegates and again inside the setter. It is what lets a refusal be said in the server's own words instead of a blank one.\n\nTHE SET IS MINIMAL AND CANNOT BE NARROWED FURTHER without abandoning the requirement: the decide entry reaches both services on its own, so any road that presses the card's own button carries all three, and any road that avoided them would be the parallel predicate the review refuses. All three are reached through LITERAL dynamic imports resolved at the call, so nothing new is pulled in at module load; the ratchet counts reachability, which is why the count moves at all. The placement rule the fill needs on both sides of the wire was deliberately put in the recurrence VOCABULARY (`packages/agents/src/trigger-recurrence.ts`), which every one of these graphs already reached, rather than in a leaf of its own - that is why this raise is 3 and not 4.\n\nHOW IT WAS MEASURED: with the companion extension fleet cloned PINNED (112/112 at their lock SHAs, missingCount 0) and a byte-identical scripts/route-graph.mjs, TWO trees in ONE environment - this pull request's own head as it stood before this change, which reproduces the ceilings committed above EXACTLY (/api/mcp 1743, /chat 1844, /api/a2a 1748, /api/llm-bridge 1758), and this head, which measures /api/mcp 1746, /chat 1847, /api/a2a 1751 and /api/llm-bridge 1761. The same +3 on each of the four, in the two owners named above. /sign-in is untouched by this change and keeps its ceiling and its carried-forward record.",
      "pr": 2998
    },
    "/api/llm-bridge": {
      "from": 1753,
      "to": 1761,
      "reason": "Pull request #2998 carries this record - cinatra#2934 (epic #2926 W5c, filling a form by asking and the review page's typed road), MERGED with main at 8028fef4effd80d5c0c1625f2b114dd345f66c9b, so `from` (1753) is MAIN'S OWN committed ceiling at that commit and nothing main already absorbed is re-absorbed here: the memory promotion slice (pull request #3038, cinatra#1381), the per-run prompt window (pull request #2995, cinatra#2933), the HITL screen's card on every host (pull request #3014, cinatra#2930), the artifact renderer dispatch (pull request #2983, cinatra#2931), the W5d start (pull request #2996, cinatra#2935), the declaration-bound delegated-chat admission (pull request #2914, cinatra#2817), the lifecycle-b W5a raise (pull request #2988, cinatra#2932) and the recommendation-hold cores (pull request #2890, cinatra#2790 S9f host parity) are ALL ALREADY INSIDE 1753. The record this raise replaces for this route is the pre-merge record this same pull request carried, RETIRED BY THIS RAISE - the one lifecycle the gate allows for an annotation whose ceiling moves.\n\nWHAT THE RAISE IS, measured as a module SET and not only as a number: exactly SIX modules enter this route's graph and none leaves, and it is the SAME six on every route this record covers.\nFOUR are the app's own src - the fill road itself:\n  src/lib/lifecycle/bound-screen-fill.ts      - the fill road's core,\n  src/lib/lifecycle/bound-screen-fill-mcp.ts  - the one self-MCP primitive that exposes it,\n  src/lib/lifecycle/bound-screen-controls.ts  - the pure projection that says which controls a screen DRAWS and which values each of them can hold,\n  src/lib/lifecycle/schedule-form-screen.ts   - the scheduler form's own rows as a bound screen.\nThey enter through an edge this route's graph already contained, src/lib/mcp-server.ts, which registers the fill beside the lent action W5a already registered there.\nTWO are the renderer this slice MOVED, in the workspace package:\n  packages/agents/src/markdown-render-core.ts - the one definition of the markup and of the escaping the untrusted-source rule depends on,\n  packages/agents/src/run-window-markdown.ts  - the window's one-line reading of it.\nThey exist because the per-run window drew the assistant's markdown raw and the graded picture leg caught it; the renderer could not be imported from @cinatra-ai/chat because that package DEPENDS ON @cinatra-ai/agents, so it moved DOWN to the package both sides may reach. marked was already an agents dependency and nothing new was added to make the move.\nPER OWNER the whole delta on every one of the four routes is TWO owners and nothing else: (app) src +4 and @cinatra-ai/agents +2. Every other owner of every one of these graphs measures exactly the same count in the control and at this head. No barrel import is opened and no new package edge is created: the agents package was already on each of these graphs.\n\nWHY THE COMMITTED CEILING RISES BY 5 WHILE THE MODULE SET GROWS BY 6, stated rather than left to be noticed: main's committed ceiling for this route carries ONE of headroom over main's own measurement - main measures 1752 against a committed 1753 - and this record sets the ceiling AT the merged head's measurement, headroom 0. 1752 + 6 = 1758, and 1758 - 1753 = 5 is the raise the gate compares against `from`/`to`.\n\nWHAT THE RETIRED ROUTE TAKES AWAY does not show here, and that is stated rather than left to be noticed: this slice DELETES src/app/api/agents/builder/[templateId]/hitl-assist/route.ts, which is its own route entry and is reachable from none of the five tracked ones, so retiring it moves no tracked count.\n\nHOW IT WAS MEASURED, the way CI measures, and re-measured at THIS merge: the companion extension fleet cloned PINNED (112/112 at their lock SHAs) in BOTH trees, missingCount 0 on EVERY tracked route of both, and a byte-identical scripts/route-graph.mjs. TWO trees analysed in ONE environment. A CONTROL WORKTREE at main's own tip 8028fef4effd80d5c0c1625f2b114dd345f66c9b measures /api/mcp 1737, /chat 1838, /api/a2a 1742, /api/llm-bridge 1752 and /sign-in 161; this merged head measures /api/mcp 1743, /chat 1844, /api/a2a 1748, /api/llm-bridge 1758 and /sign-in 161 - the same +6 on each of the four, in the two owners named above, and nothing on /sign-in.\n\n/sign-in KEEPS ITS CEILING AND ITS CARRIED-FORWARD RECORD, byte-identical to main's. It measures 161 in the control and 161 at this head against a committed 223, and locking that shrink in is main's own change to make, not this slice's: retiring an annotation is reserved for the change that MOVES the ceiling.\n\nSECOND RAISE ON THIS SAME RECORD, +3 ON EACH OF THE FOUR (cinatra#2934, the armed-trigger tab). The maintainer's CHANGES REQUESTED reading on this pull request refuses its Deviation 1: an armed schedule must be changeable from the window on the agent's Schedule tab for as long as the form can still be edited. Serving it puts the ARMED scheduler form on the one road as a bound screen of its own, and the whole point of the change is that the window uses THE FORM'S OWN predicate and THE BUTTON'S OWN save rather than a parallel pair - so exactly the three modules that own those two things become reachable from a bound turn, and no others.\n\nMEASURED AS A MODULE SET, per owner, on one tracked route and identically on the other three: (app) src +1 and @cinatra-ai/agents +2, every other owner unchanged.\n  src/lib/lifecycle/trigger-schedule-proposal-card.ts        - the card's own decide entry. `decideTriggerScheduleProposal` with the card's `save` op is the function `/api/lifecycle-views/decide` calls when the person presses Save changes; the ask takes it with the same ref and the same actor, which is what \"one road for the press and for the ask\" means.\n  packages/agents/src/trigger-schedule-proposal-service.ts   - `resolveProposalForRun`, the call that computes the settled card's `body.canSave` (`canSaveInstalled`). The window reads that boolean rather than deriving one, so the box and the button cannot disagree about which schedules are still changeable.\n  packages/agents/src/trigger-service.ts                     - `saveScheduleRefusalFor`, a two-line wrapper over `saveScheduleGuardRefusal`, the guard `updateRunTriggerScheduleForActor` asks before it delegates and again inside the setter. It is what lets a refusal be said in the server's own words instead of a blank one.\n\nTHE SET IS MINIMAL AND CANNOT BE NARROWED FURTHER without abandoning the requirement: the decide entry reaches both services on its own, so any road that presses the card's own button carries all three, and any road that avoided them would be the parallel predicate the review refuses. All three are reached through LITERAL dynamic imports resolved at the call, so nothing new is pulled in at module load; the ratchet counts reachability, which is why the count moves at all. The placement rule the fill needs on both sides of the wire was deliberately put in the recurrence VOCABULARY (`packages/agents/src/trigger-recurrence.ts`), which every one of these graphs already reached, rather than in a leaf of its own - that is why this raise is 3 and not 4.\n\nHOW IT WAS MEASURED: with the companion extension fleet cloned PINNED (112/112 at their lock SHAs, missingCount 0) and a byte-identical scripts/route-graph.mjs, TWO trees in ONE environment - this pull request's own head as it stood before this change, which reproduces the ceilings committed above EXACTLY (/api/mcp 1743, /chat 1844, /api/a2a 1748, /api/llm-bridge 1758), and this head, which measures /api/mcp 1746, /chat 1847, /api/a2a 1751 and /api/llm-bridge 1761. The same +3 on each of the four, in the two owners named above. /sign-in is untouched by this change and keeps its ceiling and its carried-forward record.",
      "pr": 2998
    }
  }
}
