{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions \u2014 a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 133,
    "/api/mcp": 1384,
    "/chat": 1422,
    "/api/a2a": 1389,
    "/api/llm-bridge": 1395
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1383,
      "to": 1389,
      "reason": "cinatra#1283 (PR #1227): the approvals sidebar badge OOM fix splits each ApprovalSource into a LIGHT *.contract.ts (id/availability/appliesTo/counts[/inboxActionable]) consumed by a nav-only registry the root layout imports, so the layout stops dragging the decide/render surface (../decision-helpers -> @cinatra-ai/agents/mcp-handlers + the React decision-action components) into every route (1372 vs 141 root-layout modules, the CI build OOM). Each heavy source SPREADS its contract to share the SAME count/visibility function references (registry-parity, drift-proof) instead of duplicating them, so the six contract modules become reachable wherever the heavy registry is (approvals-mcp -> registry -> the six sources), i.e. this locked agent/MCP/chat route gains exactly the six new first-party contract leaves. Net a massive shrink elsewhere (the layout drops ~1200 modules); the +6 here is the honest reference-sharing cost on the routes that already reach the agents runtime. Measured vs origin/main with the companion extension repos cloned pinned in CI (missingCount 0, delta exact +6). Absorbed raise 1383->1389.",
      "pr": 1227
    },
    "/api/llm-bridge": {
      "from": 1389,
      "to": 1395,
      "reason": "cinatra#1283 (PR #1227): the approvals sidebar badge OOM fix splits each ApprovalSource into a LIGHT *.contract.ts (id/availability/appliesTo/counts[/inboxActionable]) consumed by a nav-only registry the root layout imports, so the layout stops dragging the decide/render surface (../decision-helpers -> @cinatra-ai/agents/mcp-handlers + the React decision-action components) into every route (1372 vs 141 root-layout modules, the CI build OOM). Each heavy source SPREADS its contract to share the SAME count/visibility function references (registry-parity, drift-proof) instead of duplicating them, so the six contract modules become reachable wherever the heavy registry is (approvals-mcp -> registry -> the six sources), i.e. this locked agent/MCP/chat route gains exactly the six new first-party contract leaves. Net a massive shrink elsewhere (the layout drops ~1200 modules); the +6 here is the honest reference-sharing cost on the routes that already reach the agents runtime. Measured vs origin/main with the companion extension repos cloned pinned in CI (missingCount 0, delta exact +6). Absorbed raise 1389->1395.",
      "pr": 1227
    },
    "/api/mcp": {
      "from": 1378,
      "to": 1384,
      "reason": "cinatra#1283 (PR #1227): the approvals sidebar badge OOM fix splits each ApprovalSource into a LIGHT *.contract.ts (id/availability/appliesTo/counts[/inboxActionable]) consumed by a nav-only registry the root layout imports, so the layout stops dragging the decide/render surface (../decision-helpers -> @cinatra-ai/agents/mcp-handlers + the React decision-action components) into every route (1372 vs 141 root-layout modules, the CI build OOM). Each heavy source SPREADS its contract to share the SAME count/visibility function references (registry-parity, drift-proof) instead of duplicating them, so the six contract modules become reachable wherever the heavy registry is (approvals-mcp -> registry -> the six sources), i.e. this locked agent/MCP/chat route gains exactly the six new first-party contract leaves. Net a massive shrink elsewhere (the layout drops ~1200 modules); the +6 here is the honest reference-sharing cost on the routes that already reach the agents runtime. Measured vs origin/main with the companion extension repos cloned pinned in CI (missingCount 0, delta exact +6). Absorbed raise 1378->1384.",
      "pr": 1227
    },
    "/chat": {
      "from": 1416,
      "to": 1422,
      "reason": "cinatra#1283 (PR #1227): the approvals sidebar badge OOM fix splits each ApprovalSource into a LIGHT *.contract.ts (id/availability/appliesTo/counts[/inboxActionable]) consumed by a nav-only registry the root layout imports, so the layout stops dragging the decide/render surface (../decision-helpers -> @cinatra-ai/agents/mcp-handlers + the React decision-action components) into every route (1372 vs 141 root-layout modules, the CI build OOM). Each heavy source SPREADS its contract to share the SAME count/visibility function references (registry-parity, drift-proof) instead of duplicating them, so the six contract modules become reachable wherever the heavy registry is (approvals-mcp -> registry -> the six sources), i.e. this locked agent/MCP/chat route gains exactly the six new first-party contract leaves. Net a massive shrink elsewhere (the layout drops ~1200 modules); the +6 here is the honest reference-sharing cost on the routes that already reach the agents runtime. Measured vs origin/main with the companion extension repos cloned pinned in CI (missingCount 0, delta exact +6). Absorbed raise 1416->1422.",
      "pr": 1227
    },
    "/sign-in": {
      "from": 132,
      "to": 133,
      "reason": "cinatra#1135 (PR #1137): the one-shot initial-admin bootstrap decision was extracted into a new dependency-free policy module src/lib/initial-admin-bootstrap-policy.ts (pure decision core, unit-testable without the server-only auth graph; same pattern as closed-registration-gate.ts), imported by src/lib/auth.ts \u2014 one new reachable first-party leaf on every route that reaches the auth core. Delta verified locally vs origin/main with the companion extension repos cloned pinned (missingCount 0, so the +1 is exact). Absorbed raise 132->133.",
      "pr": 1137
    }
  }
}
