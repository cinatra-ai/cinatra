{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1487,
    "/api/mcp": 1483,
    "/chat": 1543,
    "/api/a2a": 1488,
    "/api/llm-bridge": 1494
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1487,
      "to": 1488,
      "reason": "store.ts file-size-ratchet main-red repair (post-#1803): the agent_run_hitl_prompts persistence seam — writeHitlPrompt, the excluded-flag mutators (updateHitlPromptExcluded + the #1794 run/agent-scoped batch updateHitlPromptsExcludedForRunAgent), and the run-scoped readers (readHitlPromptsForRun / readAllHitlPromptsForRun / readNonExcludedAgentIdsForRun) — was extracted VERBATIM out of packages/agents/src/store.ts into the new leaf module packages/agents/src/agent-run-hitl-prompts.ts so store.ts drops back under its file-size ceiling (store.ts hit 4465 vs the 4448 ceiling after #1803 added updateHitlPromptsExcludedForRunAgent inline with no extraction; the file-size ratchet is shrink-only). ./store re-exports the whole surface, so the new module is first-party-reachable wherever ./store is; it pulls NO new subtree (its only first-party imports — db from ./db, agentRunHitlPrompts from ./schema — are already reachable from ./store), so it adds exactly itself (+1 module) to this route's graph, exactly like the ./template-snapshot precedent (PR #1754). MEASURED via an A/B differential with the companion extension repos cloned pinned (missingCount 0): a clean +1 (1487->1488).",
      "pr": 1813
    },
    "/api/llm-bridge": {
      "from": 1493,
      "to": 1494,
      "reason": "store.ts file-size-ratchet main-red repair (post-#1803): the agent_run_hitl_prompts persistence seam — writeHitlPrompt, the excluded-flag mutators (updateHitlPromptExcluded + the #1794 run/agent-scoped batch updateHitlPromptsExcludedForRunAgent), and the run-scoped readers (readHitlPromptsForRun / readAllHitlPromptsForRun / readNonExcludedAgentIdsForRun) — was extracted VERBATIM out of packages/agents/src/store.ts into the new leaf module packages/agents/src/agent-run-hitl-prompts.ts so store.ts drops back under its file-size ceiling (store.ts hit 4465 vs the 4448 ceiling after #1803 added updateHitlPromptsExcludedForRunAgent inline with no extraction; the file-size ratchet is shrink-only). ./store re-exports the whole surface, so the new module is first-party-reachable wherever ./store is; it pulls NO new subtree (its only first-party imports — db from ./db, agentRunHitlPrompts from ./schema — are already reachable from ./store), so it adds exactly itself (+1 module) to this route's graph, exactly like the ./template-snapshot precedent (PR #1754). MEASURED via an A/B differential with the companion extension repos cloned pinned (missingCount 0): a clean +1 (1493->1494).",
      "pr": 1813
    },
    "/api/mcp": {
      "from": 1482,
      "to": 1483,
      "reason": "store.ts file-size-ratchet main-red repair (post-#1803): the agent_run_hitl_prompts persistence seam — writeHitlPrompt, the excluded-flag mutators (updateHitlPromptExcluded + the #1794 run/agent-scoped batch updateHitlPromptsExcludedForRunAgent), and the run-scoped readers (readHitlPromptsForRun / readAllHitlPromptsForRun / readNonExcludedAgentIdsForRun) — was extracted VERBATIM out of packages/agents/src/store.ts into the new leaf module packages/agents/src/agent-run-hitl-prompts.ts so store.ts drops back under its file-size ceiling (store.ts hit 4465 vs the 4448 ceiling after #1803 added updateHitlPromptsExcludedForRunAgent inline with no extraction; the file-size ratchet is shrink-only). ./store re-exports the whole surface, so the new module is first-party-reachable wherever ./store is; it pulls NO new subtree (its only first-party imports — db from ./db, agentRunHitlPrompts from ./schema — are already reachable from ./store), so it adds exactly itself (+1 module) to this route's graph, exactly like the ./template-snapshot precedent (PR #1754). MEASURED via an A/B differential with the companion extension repos cloned pinned (missingCount 0): a clean +1 (1482->1483).",
      "pr": 1813
    },
    "/chat": {
      "from": 1542,
      "to": 1543,
      "reason": "store.ts file-size-ratchet main-red repair (post-#1803): the agent_run_hitl_prompts persistence seam — writeHitlPrompt, the excluded-flag mutators (updateHitlPromptExcluded + the #1794 run/agent-scoped batch updateHitlPromptsExcludedForRunAgent), and the run-scoped readers (readHitlPromptsForRun / readAllHitlPromptsForRun / readNonExcludedAgentIdsForRun) — was extracted VERBATIM out of packages/agents/src/store.ts into the new leaf module packages/agents/src/agent-run-hitl-prompts.ts so store.ts drops back under its file-size ceiling (store.ts hit 4465 vs the 4448 ceiling after #1803 added updateHitlPromptsExcludedForRunAgent inline with no extraction; the file-size ratchet is shrink-only). ./store re-exports the whole surface, so the new module is first-party-reachable wherever ./store is; it pulls NO new subtree (its only first-party imports — db from ./db, agentRunHitlPrompts from ./schema — are already reachable from ./store), so it adds exactly itself (+1 module) to this route's graph, exactly like the ./template-snapshot precedent (PR #1754). MEASURED via an A/B differential with the companion extension repos cloned pinned (missingCount 0): a clean +1 (1542->1543).",
      "pr": 1813
    },
    "/sign-in": {
      "from": 1486,
      "to": 1487,
      "reason": "store.ts file-size-ratchet main-red repair (post-#1803): the agent_run_hitl_prompts persistence seam — writeHitlPrompt, the excluded-flag mutators (updateHitlPromptExcluded + the #1794 run/agent-scoped batch updateHitlPromptsExcludedForRunAgent), and the run-scoped readers (readHitlPromptsForRun / readAllHitlPromptsForRun / readNonExcludedAgentIdsForRun) — was extracted VERBATIM out of packages/agents/src/store.ts into the new leaf module packages/agents/src/agent-run-hitl-prompts.ts so store.ts drops back under its file-size ceiling (store.ts hit 4465 vs the 4448 ceiling after #1803 added updateHitlPromptsExcludedForRunAgent inline with no extraction; the file-size ratchet is shrink-only). ./store re-exports the whole surface, so the new module is first-party-reachable wherever ./store is; it pulls NO new subtree (its only first-party imports — db from ./db, agentRunHitlPrompts from ./schema — are already reachable from ./store), so it adds exactly itself (+1 module) to this route's graph, exactly like the ./template-snapshot precedent (PR #1754). MEASURED via an A/B differential with the companion extension repos cloned pinned (missingCount 0): a clean +1 (1486->1487).",
      "pr": 1813
    }
  }
}
