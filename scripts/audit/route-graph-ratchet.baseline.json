{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1649,
    "/api/mcp": 1645,
    "/chat": 1723,
    "/api/a2a": 1650,
    "/api/llm-bridge": 1660
  },
  "absorbs": {
    "/sign-in": {
      "from": 1648,
      "to": 1649,
      "reason": "cinatra#2022 S7-gamma post-merge repair (PR #2230): the freshness adapter's lazy invoker-stack resolution collapses from four independent `Promise.all`-batched `import()` call sites (in wordpress-adapter.ts) into ONE dynamic entry point (the new src/lib/object-history/freshness/invoker-deps-lazy.ts), to remove a Turbopack production-build chunk-naming collision ('Two or more assets with different content were emitted to the same output path') caused by those four split points reaching a module graph also statically reached elsewhere (S5 pending-call executor, blog WordPress client). The new file is exactly +1 first-party module reachable from all five locked routes (wordpress-adapter.ts's dynamic-import chain was already counted; only the new wrapper node is net-new). RE-MEASURED locally via before/after route-graph.mjs diff on the unmodified vs fixed tree (companion extensions NOT cloned on this box per workspace policy, so the local absolute counts undercount vs CI's cloned-extensions baseline, but the delta itself is extension-repo-independent — the new node's only edges are to already-existing first-party specifiers). Cannot be narrowed further without reintroducing the four-way split this PR removes. Re-anchored during merge-conflict resolution against origin/main, which had independently absorbed cinatra#1940 P4's own +1/route (PR #2224, `from` 1647 `to` 1648 on this route) since this branch's merge-base — this record's `from` is main's current committed ceiling (already including #2224's raise), stacking this PR's +1 on top of it rather than re-deriving from the pre-#2224 base.",
      "pr": 2230
    },
    "/api/mcp": {
      "from": 1644,
      "to": 1645,
      "reason": "cinatra#2022 S7-gamma post-merge repair (PR #2230): the freshness adapter's lazy invoker-stack resolution collapses from four independent `Promise.all`-batched `import()` call sites (in wordpress-adapter.ts) into ONE dynamic entry point (the new src/lib/object-history/freshness/invoker-deps-lazy.ts), to remove a Turbopack production-build chunk-naming collision ('Two or more assets with different content were emitted to the same output path') caused by those four split points reaching a module graph also statically reached elsewhere (S5 pending-call executor, blog WordPress client). The new file is exactly +1 first-party module reachable from all five locked routes (wordpress-adapter.ts's dynamic-import chain was already counted; only the new wrapper node is net-new). RE-MEASURED locally via before/after route-graph.mjs diff on the unmodified vs fixed tree (companion extensions NOT cloned on this box per workspace policy, so the local absolute counts undercount vs CI's cloned-extensions baseline, but the delta itself is extension-repo-independent — the new node's only edges are to already-existing first-party specifiers). Cannot be narrowed further without reintroducing the four-way split this PR removes. Re-anchored during merge-conflict resolution against origin/main, which had independently absorbed cinatra#1940 P4's own +1/route (PR #2224, `from` 1643 `to` 1644 on this route) since this branch's merge-base — this record's `from` is main's current committed ceiling (already including #2224's raise), stacking this PR's +1 on top of it rather than re-deriving from the pre-#2224 base.",
      "pr": 2230
    },
    "/chat": {
      "from": 1722,
      "to": 1723,
      "reason": "cinatra#2022 S7-gamma post-merge repair (PR #2230): the freshness adapter's lazy invoker-stack resolution collapses from four independent `Promise.all`-batched `import()` call sites (in wordpress-adapter.ts) into ONE dynamic entry point (the new src/lib/object-history/freshness/invoker-deps-lazy.ts), to remove a Turbopack production-build chunk-naming collision ('Two or more assets with different content were emitted to the same output path') caused by those four split points reaching a module graph also statically reached elsewhere (S5 pending-call executor, blog WordPress client). The new file is exactly +1 first-party module reachable from all five locked routes (wordpress-adapter.ts's dynamic-import chain was already counted; only the new wrapper node is net-new). RE-MEASURED locally via before/after route-graph.mjs diff on the unmodified vs fixed tree (companion extensions NOT cloned on this box per workspace policy, so the local absolute counts undercount vs CI's cloned-extensions baseline, but the delta itself is extension-repo-independent — the new node's only edges are to already-existing first-party specifiers). Cannot be narrowed further without reintroducing the four-way split this PR removes. Re-anchored during merge-conflict resolution against origin/main, which had independently absorbed cinatra#1940 P4's own +1/route (PR #2224, `from` 1721 `to` 1722 on this route) since this branch's merge-base — this record's `from` is main's current committed ceiling (already including #2224's raise), stacking this PR's +1 on top of it rather than re-deriving from the pre-#2224 base.",
      "pr": 2230
    },
    "/api/a2a": {
      "from": 1649,
      "to": 1650,
      "reason": "cinatra#2022 S7-gamma post-merge repair (PR #2230): the freshness adapter's lazy invoker-stack resolution collapses from four independent `Promise.all`-batched `import()` call sites (in wordpress-adapter.ts) into ONE dynamic entry point (the new src/lib/object-history/freshness/invoker-deps-lazy.ts), to remove a Turbopack production-build chunk-naming collision ('Two or more assets with different content were emitted to the same output path') caused by those four split points reaching a module graph also statically reached elsewhere (S5 pending-call executor, blog WordPress client). The new file is exactly +1 first-party module reachable from all five locked routes (wordpress-adapter.ts's dynamic-import chain was already counted; only the new wrapper node is net-new). RE-MEASURED locally via before/after route-graph.mjs diff on the unmodified vs fixed tree (companion extensions NOT cloned on this box per workspace policy, so the local absolute counts undercount vs CI's cloned-extensions baseline, but the delta itself is extension-repo-independent — the new node's only edges are to already-existing first-party specifiers). Cannot be narrowed further without reintroducing the four-way split this PR removes. Re-anchored during merge-conflict resolution against origin/main, which had independently absorbed cinatra#1940 P4's own +1/route (PR #2224, `from` 1648 `to` 1649 on this route) since this branch's merge-base — this record's `from` is main's current committed ceiling (already including #2224's raise), stacking this PR's +1 on top of it rather than re-deriving from the pre-#2224 base.",
      "pr": 2230
    },
    "/api/llm-bridge": {
      "from": 1659,
      "to": 1660,
      "reason": "cinatra#2022 S7-gamma post-merge repair (PR #2230): the freshness adapter's lazy invoker-stack resolution collapses from four independent `Promise.all`-batched `import()` call sites (in wordpress-adapter.ts) into ONE dynamic entry point (the new src/lib/object-history/freshness/invoker-deps-lazy.ts), to remove a Turbopack production-build chunk-naming collision ('Two or more assets with different content were emitted to the same output path') caused by those four split points reaching a module graph also statically reached elsewhere (S5 pending-call executor, blog WordPress client). The new file is exactly +1 first-party module reachable from all five locked routes (wordpress-adapter.ts's dynamic-import chain was already counted; only the new wrapper node is net-new). RE-MEASURED locally via before/after route-graph.mjs diff on the unmodified vs fixed tree (companion extensions NOT cloned on this box per workspace policy, so the local absolute counts undercount vs CI's cloned-extensions baseline, but the delta itself is extension-repo-independent — the new node's only edges are to already-existing first-party specifiers). Cannot be narrowed further without reintroducing the four-way split this PR removes. Re-anchored during merge-conflict resolution against origin/main, which had independently absorbed cinatra#1940 P4's own +1/route (PR #2224, `from` 1658 `to` 1659 on this route) since this branch's merge-base — this record's `from` is main's current committed ceiling (already including #2224's raise), stacking this PR's +1 on top of it rather than re-deriving from the pre-#2224 base.",
      "pr": 2230
    }
  }
}
