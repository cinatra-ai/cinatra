{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions \u2014 a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 150,
    "/api/mcp": 1468,
    "/chat": 1528,
    "/api/a2a": 1473,
    "/api/llm-bridge": 1479
  },
  "absorbs": {
    "/chat": {
      "from": 1527,
      "to": 1528,
      "reason": "cinatra#1707 (PR #1755) exec-plane S2 singular-native-shell: the net-new leaf module packages/llm/src/tools/skill-read-tool.ts \u2014 the restricted NAMED skill-read function-tool contract (SKILL_FILE_READ_TOOL_NAME + its JSON schema), kept deliberately dependency-free so the provider adapter (providers/openai.ts), the orchestration barrel (index.ts), and app-side surfaces share ONE tool name without dragging provider SDK imports. Every route already reaching the @cinatra-ai/llm package grows by exactly +1 with no further subtree pulled (the @cinatra-ai/execution-plane package itself stays route-unreachable: owner-count 0 before and after). NOT a narrowable barrel import: the name/schema must be statically shared by the adapter emitting the tool and the delivery layer dispatching it \u2014 duplicating the constant re-opens the drifted-name catalog-bypass class the retired read_skill ban (scripts/audit/read-skill-function-tool-banned.mjs) closed. MEASURED by CI's authoritative merge-ref run (Actions run 29601512374, missingCount 0): /chat 1527 -> 1528 (NET +1); cross-checked by a local three-tree differential (scripts/route-graph.mjs analyzeRoute at main tips fbfe6bcb and da4d7cd0 vs the PR head): the +1 is a single net-new @cinatra-ai/llm module on every locked route (owner-count 38 -> 39) and the current main tip da4d7cd0 adds zero modules to these routes. Layers on the PR #1741 breadcrumb raise (1525->1527), whose record this retires by re-raising the ceiling.",
      "pr": 1755
    },
    "/api/a2a": {
      "from": 1472,
      "to": 1473,
      "reason": "cinatra#1707 (PR #1755) exec-plane S2 singular-native-shell: the net-new leaf module packages/llm/src/tools/skill-read-tool.ts \u2014 the restricted NAMED skill-read function-tool contract (SKILL_FILE_READ_TOOL_NAME + its JSON schema), kept deliberately dependency-free so the provider adapter (providers/openai.ts), the orchestration barrel (index.ts), and app-side surfaces share ONE tool name without dragging provider SDK imports. Every route already reaching the @cinatra-ai/llm package grows by exactly +1 with no further subtree pulled (the @cinatra-ai/execution-plane package itself stays route-unreachable: owner-count 0 before and after). NOT a narrowable barrel import: the name/schema must be statically shared by the adapter emitting the tool and the delivery layer dispatching it \u2014 duplicating the constant re-opens the drifted-name catalog-bypass class the retired read_skill ban (scripts/audit/read-skill-function-tool-banned.mjs) closed. MEASURED by CI's authoritative merge-ref run (Actions run 29601512374, missingCount 0): /api/a2a 1472 -> 1473 (NET +1); cross-checked by a local three-tree differential (scripts/route-graph.mjs analyzeRoute at main tips fbfe6bcb and da4d7cd0 vs the PR head): the +1 is a single net-new @cinatra-ai/llm module on every locked route (owner-count 38 -> 39) and the current main tip da4d7cd0 adds zero modules to these routes. Layers on the PR #1741 breadcrumb raise (1470->1472), whose record this retires by re-raising the ceiling.",
      "pr": 1755
    },
    "/api/llm-bridge": {
      "from": 1478,
      "to": 1479,
      "reason": "cinatra#1707 (PR #1755) exec-plane S2 singular-native-shell: the net-new leaf module packages/llm/src/tools/skill-read-tool.ts \u2014 the restricted NAMED skill-read function-tool contract (SKILL_FILE_READ_TOOL_NAME + its JSON schema), kept deliberately dependency-free so the provider adapter (providers/openai.ts), the orchestration barrel (index.ts), and app-side surfaces share ONE tool name without dragging provider SDK imports. Every route already reaching the @cinatra-ai/llm package grows by exactly +1 with no further subtree pulled (the @cinatra-ai/execution-plane package itself stays route-unreachable: owner-count 0 before and after). NOT a narrowable barrel import: the name/schema must be statically shared by the adapter emitting the tool and the delivery layer dispatching it \u2014 duplicating the constant re-opens the drifted-name catalog-bypass class the retired read_skill ban (scripts/audit/read-skill-function-tool-banned.mjs) closed. MEASURED by CI's authoritative merge-ref run (Actions run 29601512374, missingCount 0): /api/llm-bridge 1478 -> 1479 (NET +1); cross-checked by a local three-tree differential (scripts/route-graph.mjs analyzeRoute at main tips fbfe6bcb and da4d7cd0 vs the PR head): the +1 is a single net-new @cinatra-ai/llm module on every locked route (owner-count 38 -> 39) and the current main tip da4d7cd0 adds zero modules to these routes. Layers on the PR #1741 breadcrumb raise (1476->1478), whose record this retires by re-raising the ceiling.",
      "pr": 1755
    },
    "/api/mcp": {
      "from": 1467,
      "to": 1468,
      "reason": "cinatra#1707 (PR #1755) exec-plane S2 singular-native-shell: the net-new leaf module packages/llm/src/tools/skill-read-tool.ts \u2014 the restricted NAMED skill-read function-tool contract (SKILL_FILE_READ_TOOL_NAME + its JSON schema), kept deliberately dependency-free so the provider adapter (providers/openai.ts), the orchestration barrel (index.ts), and app-side surfaces share ONE tool name without dragging provider SDK imports. Every route already reaching the @cinatra-ai/llm package grows by exactly +1 with no further subtree pulled (the @cinatra-ai/execution-plane package itself stays route-unreachable: owner-count 0 before and after). NOT a narrowable barrel import: the name/schema must be statically shared by the adapter emitting the tool and the delivery layer dispatching it \u2014 duplicating the constant re-opens the drifted-name catalog-bypass class the retired read_skill ban (scripts/audit/read-skill-function-tool-banned.mjs) closed. MEASURED by CI's authoritative merge-ref run (Actions run 29601512374, missingCount 0): /api/mcp 1467 -> 1468 (NET +1); cross-checked by a local three-tree differential (scripts/route-graph.mjs analyzeRoute at main tips fbfe6bcb and da4d7cd0 vs the PR head): the +1 is a single net-new @cinatra-ai/llm module on every locked route (owner-count 38 -> 39) and the current main tip da4d7cd0 adds zero modules to these routes. Layers on the PR #1741 breadcrumb raise (1465->1467), whose record this retires by re-raising the ceiling.",
      "pr": 1755
    },
    "/sign-in": {
      "from": 149,
      "to": 150,
      "reason": "cinatra#1565 (PR #1579) org member-invitation email: src/lib/auth.ts (reachable from every tracked route) gains the new leaf module src/lib/org-invitation-email.ts \u2014 the pure accept-link/subject/body builder wired into the organization.sendInvitationEmail callback (Better Auth ships NO default invitation sender, so inviteMember created an invitation row but sent nothing). MEASURED via an A/B differential using scripts/route-graph.mjs analyzeRoute: with the module present vs moved aside, EVERY tracked route drops by exactly 1, so the raise is a single net-new fully-resolvable leaf, invariant to the missing companion-extension modules. /sign-in measured 150 locally at missingCount 0; the four companion routes measured the identical -1 delta (their absolute count is CI-measured with the extensions cloned pinned, this base+1). Absorbed raise 149->150 (NET +1). RE-BASED 2026-07-15 after main-side baseline rewrite (objects/memory merges); from/to re-derived on the new base, same measured NET +1 \u2014 CI re-verifies on the merged tree.",
      "pr": 1579
    }
  }
}
