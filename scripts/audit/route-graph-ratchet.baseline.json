{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1474,
    "/api/mcp": 1470,
    "/chat": 1530,
    "/api/a2a": 1475,
    "/api/llm-bridge": 1481
  },
  "absorbs": {
    "/chat": {
      "from": 1527,
      "to": 1530,
      "reason": "cinatra#1037 P1.3/P1.4 (PR #1674) assistant-agent registration + run-status extraction: this PR's net-new modules reachable from the always-reachable auth/agents-store surface. MEASURED on the PR merge tree (origin/main fbfe6bcb merged into the lane) with the companion extension repos cloned pinned (missingCount 0, exactly as CI does via clone-extensions): the route gains EXACTLY three modules — packages/agents/src/run-status.ts (the run-status state machine extracted out of the agents store for the file-size ratchet; a fully-resolvable leaf), src/lib/assistant-agent-registration.ts (the assistant-agent registration module; statically imported by src/lib/auth.ts since #1747, and auth.ts is reachable from EVERY tracked route), and src/lib/assistant-users.ts (the re-plumbed principal-mint primitive the registration module is the sole caller of). An A/B module-list diff of this route against the origin/main tree (same pinned extensions, both at missingCount 0) shows exactly these three additions and zero removals. Not a narrowable barrel import: the auth.ts edge is the registration bootstrap seam itself (I3). Retires the PR #1741 record by re-raising the ceiling (re-raise precedent: a re-raise replaces the record it layers on). Absorbed raise 1527->1530 (NET +3).",
      "pr": 1674
    },
    "/api/a2a": {
      "from": 1472,
      "to": 1475,
      "reason": "cinatra#1037 P1.3/P1.4 (PR #1674) assistant-agent registration + run-status extraction: this PR's net-new modules reachable from the always-reachable auth/agents-store surface. MEASURED on the PR merge tree (origin/main fbfe6bcb merged into the lane) with the companion extension repos cloned pinned (missingCount 0, exactly as CI does via clone-extensions): the route gains EXACTLY three modules — packages/agents/src/run-status.ts (the run-status state machine extracted out of the agents store for the file-size ratchet; a fully-resolvable leaf), src/lib/assistant-agent-registration.ts (the assistant-agent registration module; statically imported by src/lib/auth.ts since #1747, and auth.ts is reachable from EVERY tracked route), and src/lib/assistant-users.ts (the re-plumbed principal-mint primitive the registration module is the sole caller of). An A/B module-list diff of this route against the origin/main tree (same pinned extensions, both at missingCount 0) shows exactly these three additions and zero removals. Not a narrowable barrel import: the auth.ts edge is the registration bootstrap seam itself (I3). Retires the PR #1741 record by re-raising the ceiling (re-raise precedent: a re-raise replaces the record it layers on). Absorbed raise 1472->1475 (NET +3).",
      "pr": 1674
    },
    "/api/llm-bridge": {
      "from": 1478,
      "to": 1481,
      "reason": "cinatra#1037 P1.3/P1.4 (PR #1674) assistant-agent registration + run-status extraction: this PR's net-new modules reachable from the always-reachable auth/agents-store surface. MEASURED on the PR merge tree (origin/main fbfe6bcb merged into the lane) with the companion extension repos cloned pinned (missingCount 0, exactly as CI does via clone-extensions): the route gains EXACTLY three modules — packages/agents/src/run-status.ts (the run-status state machine extracted out of the agents store for the file-size ratchet; a fully-resolvable leaf), src/lib/assistant-agent-registration.ts (the assistant-agent registration module; statically imported by src/lib/auth.ts since #1747, and auth.ts is reachable from EVERY tracked route), and src/lib/assistant-users.ts (the re-plumbed principal-mint primitive the registration module is the sole caller of). An A/B module-list diff of this route against the origin/main tree (same pinned extensions, both at missingCount 0) shows exactly these three additions and zero removals. Not a narrowable barrel import: the auth.ts edge is the registration bootstrap seam itself (I3). Retires the PR #1741 record by re-raising the ceiling (re-raise precedent: a re-raise replaces the record it layers on). Absorbed raise 1478->1481 (NET +3).",
      "pr": 1674
    },
    "/api/mcp": {
      "from": 1467,
      "to": 1470,
      "reason": "cinatra#1037 P1.3/P1.4 (PR #1674) assistant-agent registration + run-status extraction: this PR's net-new modules reachable from the always-reachable auth/agents-store surface. MEASURED on the PR merge tree (origin/main fbfe6bcb merged into the lane) with the companion extension repos cloned pinned (missingCount 0, exactly as CI does via clone-extensions): the route gains EXACTLY three modules — packages/agents/src/run-status.ts (the run-status state machine extracted out of the agents store for the file-size ratchet; a fully-resolvable leaf), src/lib/assistant-agent-registration.ts (the assistant-agent registration module; statically imported by src/lib/auth.ts since #1747, and auth.ts is reachable from EVERY tracked route), and src/lib/assistant-users.ts (the re-plumbed principal-mint primitive the registration module is the sole caller of). An A/B module-list diff of this route against the origin/main tree (same pinned extensions, both at missingCount 0) shows exactly these three additions and zero removals. Not a narrowable barrel import: the auth.ts edge is the registration bootstrap seam itself (I3). Retires the PR #1741 record by re-raising the ceiling (re-raise precedent: a re-raise replaces the record it layers on). Absorbed raise 1467->1470 (NET +3).",
      "pr": 1674
    },
    "/sign-in": {
      "from": 150,
      "to": 1474,
      "reason": "cinatra#1037 P1.3/P1.4 built-in assistant principal bootstrap (PR #1674). src/lib/auth.ts ensureAssistantBootstrap reaches src/lib/assistant-agent-registration.ts (this PR), which mints+links the built-in Cinatra assistant principal via upsertBuiltInAssistantAgentTemplate from the @cinatra-ai/agents store. That store's transitive graph pulls the full host connector/agent registry (store -> auth-policy -> connector-authority -> src/lib/generated/extensions.server.ts, and the agents index barrel -> server-actions -> register-host-connector-services). auth.ts is reachable from EVERY tracked route through @cinatra-ai/permissions/pages, so /sign-in — previously a thin pre-auth page that never reached the registry (150) — now reaches it. The registration import in auth.ts was originally DYNAMIC (await import) to code-split this graph off the eager bundle; #1747 made it STATIC (the dynamic form detonated Turbopack's linux CI build). For THIS ratchet the import form is irrelevant either way: the static reachable-first-party-module counter traverses dynamic imports too (documented limitation), so the count is identical under both forms. No narrow-import fix exists: upsertBuiltInAssistantAgentTemplate lives in the agents store, which intrinsically reaches the connector registry. HISTORY: the previously approved ceiling 1444 was measured 1432 at origin/main 43399f55 plus +12 main-churn headroom; main-side store-graph growth since then consumed the buffer (this route rides the actively-growing agents-store/connector-registry graph that main's own thin /sign-in never reaches, so the growth surfaces only here). RE-MEASURED at the update-branch: 150->1474 on the PR merge tree (origin/main fbfe6bcb merged into the lane) with the companion extension repos cloned pinned (missingCount 0, exactly as CI does via clone-extensions), deterministic across repeated runs. The ceiling is now pinned EXACT with NO headroom buffer: the four companion routes are exact-bound by their own PR #1674 absorb records, so ANY later main-side store-graph growth already forces a pre-merge update-branch re-measure on those routes — a /sign-in-only buffer cannot avoid that re-run and would only blur what this PR itself contributes. The coordinator re-measures on the pre-merge update-branch (the up-to-date merge rule). Absorbed raise 150->1474.",
      "pr": 1674
    }
  }
}
