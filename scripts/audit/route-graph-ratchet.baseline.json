{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions \u2014 a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 223,
    "/api/mcp": 1787,
    "/chat": 1889,
    "/api/a2a": 1795,
    "/api/llm-bridge": 1805
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1794,
      "to": 1795,
      "reason": "cinatra#3358, the account-scope step's new-run road. ONE leaf, and no other module: src/components/ui/empty.tsx, which packages/agents/src/list-picker-renderer.tsx now imports because the ratified drawing states the zero-content reading of a list in one line \u2014 centred, a dashed circle icon, a 14px headline over a 12px helper, a single primary action \u2014 and names that pattern as the shared primitive at that path. The step drew a plain card carrying one grey sentence instead, which is the bare empty text the same section forbids. The renderer is reached from the field-renderer registry, which all four of these routes already reach, so the one leaf lands on all four and on neither /sign-in nor any other tracked entry. The change adds no other first-party module to any tracked graph: the rest of it edits files already in them (the run screen, the stepper panel, the shared gate-submit rules and the agent-path grammar) and its other new files are tests, which the analyzer does not walk. MEASURED, NOT COMPUTED, and not hand-merged \u2014 the baseline was regenerated by `node scripts/audit/route-graph-ratchet.mjs --write-baseline` on this tree: with the companion extension fleet cloned PINNED exactly as CI clones it (116/116, missingCount 0 on every route) TWO trees were analysed in ONE environment by the same scripts/route-graph.mjs. This tree measures /api/mcp 1787, /chat 1889, /api/a2a 1795, /api/llm-bridge 1805 and /sign-in 163. The second tree is this tree with that ONE import edge removed and nothing else changed, and it lands EXACTLY on the four ceilings the base branch committed (/api/mcp 1786, /chat 1888, /api/a2a 1794, /api/llm-bridge 1804) \u2014 which isolates the leaf at +1 per route and proves nothing of the base branch is folded into this raise. /sign-in measures 163 on both trees, far under its 223 ceiling, so its ceiling and its carried-forward record are left exactly as the base branch committed them. Narrowing is unavailable: scripts/route-graph.mjs counts a dynamic import() specifier as exactly the same first-party graph pressure as a static one (the metric is static reachability by design), so making the edge lazy would not lower the count; and the pattern is a shared design-system primitive every zero-content reading in the product must resolve to ONE of, so it can be neither duplicated inside this renderer nor split behind a route-specific entry point. The records these four routes carried are RETIRED BY THIS RAISE, the one lifecycle the gate allows for an annotation whose ceiling moves.",
      "pr": 0
    },
    "/api/llm-bridge": {
      "from": 1804,
      "to": 1805,
      "reason": "cinatra#3358, the account-scope step's new-run road. ONE leaf, and no other module: src/components/ui/empty.tsx, which packages/agents/src/list-picker-renderer.tsx now imports because the ratified drawing states the zero-content reading of a list in one line \u2014 centred, a dashed circle icon, a 14px headline over a 12px helper, a single primary action \u2014 and names that pattern as the shared primitive at that path. The step drew a plain card carrying one grey sentence instead, which is the bare empty text the same section forbids. The renderer is reached from the field-renderer registry, which all four of these routes already reach, so the one leaf lands on all four and on neither /sign-in nor any other tracked entry. The change adds no other first-party module to any tracked graph: the rest of it edits files already in them (the run screen, the stepper panel, the shared gate-submit rules and the agent-path grammar) and its other new files are tests, which the analyzer does not walk. MEASURED, NOT COMPUTED, and not hand-merged \u2014 the baseline was regenerated by `node scripts/audit/route-graph-ratchet.mjs --write-baseline` on this tree: with the companion extension fleet cloned PINNED exactly as CI clones it (116/116, missingCount 0 on every route) TWO trees were analysed in ONE environment by the same scripts/route-graph.mjs. This tree measures /api/mcp 1787, /chat 1889, /api/a2a 1795, /api/llm-bridge 1805 and /sign-in 163. The second tree is this tree with that ONE import edge removed and nothing else changed, and it lands EXACTLY on the four ceilings the base branch committed (/api/mcp 1786, /chat 1888, /api/a2a 1794, /api/llm-bridge 1804) \u2014 which isolates the leaf at +1 per route and proves nothing of the base branch is folded into this raise. /sign-in measures 163 on both trees, far under its 223 ceiling, so its ceiling and its carried-forward record are left exactly as the base branch committed them. Narrowing is unavailable: scripts/route-graph.mjs counts a dynamic import() specifier as exactly the same first-party graph pressure as a static one (the metric is static reachability by design), so making the edge lazy would not lower the count; and the pattern is a shared design-system primitive every zero-content reading in the product must resolve to ONE of, so it can be neither duplicated inside this renderer nor split behind a route-specific entry point. The records these four routes carried are RETIRED BY THIS RAISE, the one lifecycle the gate allows for an annotation whose ceiling moves.",
      "pr": 0
    },
    "/api/mcp": {
      "from": 1786,
      "to": 1787,
      "reason": "cinatra#3358, the account-scope step's new-run road. ONE leaf, and no other module: src/components/ui/empty.tsx, which packages/agents/src/list-picker-renderer.tsx now imports because the ratified drawing states the zero-content reading of a list in one line \u2014 centred, a dashed circle icon, a 14px headline over a 12px helper, a single primary action \u2014 and names that pattern as the shared primitive at that path. The step drew a plain card carrying one grey sentence instead, which is the bare empty text the same section forbids. The renderer is reached from the field-renderer registry, which all four of these routes already reach, so the one leaf lands on all four and on neither /sign-in nor any other tracked entry. The change adds no other first-party module to any tracked graph: the rest of it edits files already in them (the run screen, the stepper panel, the shared gate-submit rules and the agent-path grammar) and its other new files are tests, which the analyzer does not walk. MEASURED, NOT COMPUTED, and not hand-merged \u2014 the baseline was regenerated by `node scripts/audit/route-graph-ratchet.mjs --write-baseline` on this tree: with the companion extension fleet cloned PINNED exactly as CI clones it (116/116, missingCount 0 on every route) TWO trees were analysed in ONE environment by the same scripts/route-graph.mjs. This tree measures /api/mcp 1787, /chat 1889, /api/a2a 1795, /api/llm-bridge 1805 and /sign-in 163. The second tree is this tree with that ONE import edge removed and nothing else changed, and it lands EXACTLY on the four ceilings the base branch committed (/api/mcp 1786, /chat 1888, /api/a2a 1794, /api/llm-bridge 1804) \u2014 which isolates the leaf at +1 per route and proves nothing of the base branch is folded into this raise. /sign-in measures 163 on both trees, far under its 223 ceiling, so its ceiling and its carried-forward record are left exactly as the base branch committed them. Narrowing is unavailable: scripts/route-graph.mjs counts a dynamic import() specifier as exactly the same first-party graph pressure as a static one (the metric is static reachability by design), so making the edge lazy would not lower the count; and the pattern is a shared design-system primitive every zero-content reading in the product must resolve to ONE of, so it can be neither duplicated inside this renderer nor split behind a route-specific entry point. The records these four routes carried are RETIRED BY THIS RAISE, the one lifecycle the gate allows for an annotation whose ceiling moves.",
      "pr": 0
    },
    "/chat": {
      "from": 1888,
      "to": 1889,
      "reason": "cinatra#3358, the account-scope step's new-run road. ONE leaf, and no other module: src/components/ui/empty.tsx, which packages/agents/src/list-picker-renderer.tsx now imports because the ratified drawing states the zero-content reading of a list in one line \u2014 centred, a dashed circle icon, a 14px headline over a 12px helper, a single primary action \u2014 and names that pattern as the shared primitive at that path. The step drew a plain card carrying one grey sentence instead, which is the bare empty text the same section forbids. The renderer is reached from the field-renderer registry, which all four of these routes already reach, so the one leaf lands on all four and on neither /sign-in nor any other tracked entry. The change adds no other first-party module to any tracked graph: the rest of it edits files already in them (the run screen, the stepper panel, the shared gate-submit rules and the agent-path grammar) and its other new files are tests, which the analyzer does not walk. MEASURED, NOT COMPUTED, and not hand-merged \u2014 the baseline was regenerated by `node scripts/audit/route-graph-ratchet.mjs --write-baseline` on this tree: with the companion extension fleet cloned PINNED exactly as CI clones it (116/116, missingCount 0 on every route) TWO trees were analysed in ONE environment by the same scripts/route-graph.mjs. This tree measures /api/mcp 1787, /chat 1889, /api/a2a 1795, /api/llm-bridge 1805 and /sign-in 163. The second tree is this tree with that ONE import edge removed and nothing else changed, and it lands EXACTLY on the four ceilings the base branch committed (/api/mcp 1786, /chat 1888, /api/a2a 1794, /api/llm-bridge 1804) \u2014 which isolates the leaf at +1 per route and proves nothing of the base branch is folded into this raise. /sign-in measures 163 on both trees, far under its 223 ceiling, so its ceiling and its carried-forward record are left exactly as the base branch committed them. Narrowing is unavailable: scripts/route-graph.mjs counts a dynamic import() specifier as exactly the same first-party graph pressure as a static one (the metric is static reachability by design), so making the edge lazy would not lower the count; and the pattern is a shared design-system primitive every zero-content reading in the product must resolve to ONE of, so it can be neither duplicated inside this renderer nor split behind a route-specific entry point. The records these four routes carried are RETIRED BY THIS RAISE, the one lifecycle the gate allows for an annotation whose ceiling moves.",
      "pr": 0
    },
    "/sign-in": {
      "from": 222,
      "to": 223,
      "reason": "cinatra#2988 (lifecycle-b W5a, cinatra#2932) carries this record \u2014 this merge brings origin/main forward into the W5a branch, so `from` (222) is main's OWN committed ceiling at 1fb86826b078b7031c422cfab7c60c0182d9b8f3. Main adds NOTHING to this route: its trigger-claim leaf (PR #2987, cinatra#2981) and its two recommendation-hold cores (PR #2890, cinatra#2790 S9f host parity, which also carries the cinatra#2997 placeholder review slot) are not reachable from the sign-in entry, and main measures 222 here unchanged. The whole +1 is this branch's own: ONE leaf of the W5a slice reachable from the auth plugins \u2014 the six leaves the slice adds (the bound-reference resolver, the bound-turn actor, the bound-card binding, the grant codec, its single-use store and the store's DDL leaf) are reached from `runAssistantTurn`, which mints the grant at send time, and only one of them lands on this route's graph. MEASURED THE WAY CI MEASURES, not computed: with the companion extension fleet cloned PINNED (112/112 at their lock SHAs, missingCount 0) and an identical scripts/route-graph.mjs, THREE trees were analysed in ONE environment \u2014 origin/main at 1fb86826b078b7031c422cfab7c60c0182d9b8f3 measures 222, this branch's pre-merge head at f84dc8a9758e9d2647e9d47acdc3e463346a23a1 reproduces this branch's own committed ceiling EXACTLY (223), and the merge measures 223: the branch's ceiling unchanged, so nothing of main's composed onto it. The carried-forward PR #2928 record for this route is RETIRED BY THIS RAISE, which is the one lifecycle the gate allows for an annotation whose ceiling moves.",
      "pr": 2988
    }
  }
}
