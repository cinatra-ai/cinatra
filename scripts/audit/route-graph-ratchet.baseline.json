{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1645,
    "/api/mcp": 1641,
    "/chat": 1719,
    "/api/a2a": 1646,
    "/api/llm-bridge": 1656
  },
  "absorbs": {
    "/sign-in": {
      "from": 1642,
      "to": 1645,
      "reason": "cinatra#1927 declaration-driven protected-extension enforcement: +3 first-party modules on every locked route, one per seam of the shared gate — packages/sdk-extensions/src/extension-protection.ts (the `protected` declaration-domain parser, referenced BY VALUE from both file-level parsers of cinatra/config.json so the accepted shape cannot drift), packages/extensions/src/protected-extension.ts (the single refusal kernel BOTH removal paths run, so the generic dispatcher and the direct agent-registry gate can never diverge), and src/lib/extension-protection-host.ts (the host reader behind the gate's injectable seam — the only piece that touches the extension store). All five locked routes reach the sdk-extensions and extensions barrels, hence the uniform +3. The raise cannot be narrowed without either duplicating the refusal predicate across the two removal paths (the exact drift this slice exists to prevent) or moving store IO into the isomorphic SDK; each module is one seam, none is a barrel widening. Stacks on the raises already baked into base (cinatra#2021 S6 via PR #2205, then the org-write raises of PR #2201 and PR #2203). Measured with the companion extensions cloned pinned.",
      "pr": 2208
    },
    "/api/mcp": {
      "from": 1638,
      "to": 1641,
      "reason": "cinatra#1927 declaration-driven protected-extension enforcement: +3 first-party modules on every locked route, one per seam of the shared gate — packages/sdk-extensions/src/extension-protection.ts (the `protected` declaration-domain parser, referenced BY VALUE from both file-level parsers of cinatra/config.json so the accepted shape cannot drift), packages/extensions/src/protected-extension.ts (the single refusal kernel BOTH removal paths run, so the generic dispatcher and the direct agent-registry gate can never diverge), and src/lib/extension-protection-host.ts (the host reader behind the gate's injectable seam — the only piece that touches the extension store). All five locked routes reach the sdk-extensions and extensions barrels, hence the uniform +3. The raise cannot be narrowed without either duplicating the refusal predicate across the two removal paths (the exact drift this slice exists to prevent) or moving store IO into the isomorphic SDK; each module is one seam, none is a barrel widening. Stacks on the raises already baked into base (cinatra#2021 S6 via PR #2205, then the org-write raises of PR #2201 and PR #2203). Measured with the companion extensions cloned pinned.",
      "pr": 2208
    },
    "/chat": {
      "from": 1716,
      "to": 1719,
      "reason": "cinatra#1927 declaration-driven protected-extension enforcement: +3 first-party modules on every locked route, one per seam of the shared gate — packages/sdk-extensions/src/extension-protection.ts (the `protected` declaration-domain parser, referenced BY VALUE from both file-level parsers of cinatra/config.json so the accepted shape cannot drift), packages/extensions/src/protected-extension.ts (the single refusal kernel BOTH removal paths run, so the generic dispatcher and the direct agent-registry gate can never diverge), and src/lib/extension-protection-host.ts (the host reader behind the gate's injectable seam — the only piece that touches the extension store). All five locked routes reach the sdk-extensions and extensions barrels, hence the uniform +3. The raise cannot be narrowed without either duplicating the refusal predicate across the two removal paths (the exact drift this slice exists to prevent) or moving store IO into the isomorphic SDK; each module is one seam, none is a barrel widening. Stacks on the raises already baked into base (cinatra#2021 S6 via PR #2205, then the org-write raises of PR #2201 and PR #2203). Measured with the companion extensions cloned pinned.",
      "pr": 2208
    },
    "/api/a2a": {
      "from": 1643,
      "to": 1646,
      "reason": "cinatra#1927 declaration-driven protected-extension enforcement: +3 first-party modules on every locked route, one per seam of the shared gate — packages/sdk-extensions/src/extension-protection.ts (the `protected` declaration-domain parser, referenced BY VALUE from both file-level parsers of cinatra/config.json so the accepted shape cannot drift), packages/extensions/src/protected-extension.ts (the single refusal kernel BOTH removal paths run, so the generic dispatcher and the direct agent-registry gate can never diverge), and src/lib/extension-protection-host.ts (the host reader behind the gate's injectable seam — the only piece that touches the extension store). All five locked routes reach the sdk-extensions and extensions barrels, hence the uniform +3. The raise cannot be narrowed without either duplicating the refusal predicate across the two removal paths (the exact drift this slice exists to prevent) or moving store IO into the isomorphic SDK; each module is one seam, none is a barrel widening. Stacks on the raises already baked into base (cinatra#2021 S6 via PR #2205, then the org-write raises of PR #2201 and PR #2203). Measured with the companion extensions cloned pinned.",
      "pr": 2208
    },
    "/api/llm-bridge": {
      "from": 1653,
      "to": 1656,
      "reason": "cinatra#1927 declaration-driven protected-extension enforcement: +3 first-party modules on every locked route, one per seam of the shared gate — packages/sdk-extensions/src/extension-protection.ts (the `protected` declaration-domain parser, referenced BY VALUE from both file-level parsers of cinatra/config.json so the accepted shape cannot drift), packages/extensions/src/protected-extension.ts (the single refusal kernel BOTH removal paths run, so the generic dispatcher and the direct agent-registry gate can never diverge), and src/lib/extension-protection-host.ts (the host reader behind the gate's injectable seam — the only piece that touches the extension store). All five locked routes reach the sdk-extensions and extensions barrels, hence the uniform +3. The raise cannot be narrowed without either duplicating the refusal predicate across the two removal paths (the exact drift this slice exists to prevent) or moving store IO into the isomorphic SDK; each module is one seam, none is a barrel widening. Stacks on the raises already baked into base (cinatra#2021 S6 via PR #2205, then the org-write raises of PR #2201 and PR #2203). Measured with the companion extensions cloned pinned.",
      "pr": 2208
    }
  }
}
