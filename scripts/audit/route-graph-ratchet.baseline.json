{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 149,
    "/api/mcp": 1450,
    "/chat": 1488,
    "/api/a2a": 1455,
    "/api/llm-bridge": 1461
  },
  "absorbs": {
    "/sign-in": {
      "from": 147,
      "to": 149,
      "reason": "cinatra#1494 (PR #1517) Better Auth org-creation default-team slug hook: the auth config (src/lib/auth.ts, reachable from /sign-in) gains src/lib/better-auth-org-hooks.ts plus the shared team-slug deriver it imports (src/app/teams/new/team-slug) — MEASURED in the PR worktree at merge commit 15640bba vs origin/main 53ab28b7; /sign-in's first-party graph resolved fully in the measurement (the four companion-extension-dependent routes are untouched by this PR). NET +2 (149 = measured reachable count, not arithmetic). RE-BASED 2026-07-14 after main-side baseline rewrite (assistants/artifacts merges raised the companion routes; from/to re-derived on the new base, same measured NET +2 — CI re-verifies on the merged tree).",
      "pr": 1517
    },
    "/api/mcp": {
      "from": 1449,
      "to": 1450,
      "reason": "cinatra#1430 (PR #1537) capture content snapshots on the MCP context_resolve path, MEASURED in the PR worktree merged on origin/main 532874da (base ceilings set by #1517, which raised every tracked route +2) with the companion extension repos cloned pinned (missingCount 0) — never arithmetic (measured NET reachable-module-set diff via ablation: restoring origin/main's src/lib/artifacts/context-mcp.ts, which drops ONLY the new capture-wiring import edge, returns all four grown routes to EXACTLY their base ceilings and holds /sign-in at 147, so the entire growth is attributable to this feature and nothing else). Exactly one first-party module becomes reachable on each grown route: src/lib/artifacts/object-content-snapshot.ts, pulled onto the resolution path by context-mcp.ts's new static import of captureSnapshotsForContextSlot (the cinatra#1430 \"capture at resolution time\" wiring in registerContextPrimitives). It is the single net-new node — object-content-snapshot's own transitive imports (./context-resolver, ./resource-store, ./local-disk-blob-store, the @/lib/postgres-sync* leaves, @cinatra-ai/objects/claims and @/lib/derived-store-ownership) were already reachable via the existing context-mcp / artifact-service graph, which is why the NET is +1 and not larger. /sign-in is flat at 147 — it does not reach the MCP tool-module registry in mcp-server.ts. Absorbed raise 1449->1450.",
      "pr": 1537
    },
    "/chat": {
      "from": 1487,
      "to": 1488,
      "reason": "cinatra#1430 (PR #1537) capture content snapshots on the MCP context_resolve path, MEASURED in the PR worktree merged on origin/main 532874da (base ceilings set by #1517, which raised every tracked route +2) with the companion extension repos cloned pinned (missingCount 0) — never arithmetic (measured NET reachable-module-set diff via ablation: restoring origin/main's src/lib/artifacts/context-mcp.ts, which drops ONLY the new capture-wiring import edge, returns all four grown routes to EXACTLY their base ceilings and holds /sign-in at 147, so the entire growth is attributable to this feature and nothing else). Exactly one first-party module becomes reachable on each grown route: src/lib/artifacts/object-content-snapshot.ts, pulled onto the resolution path by context-mcp.ts's new static import of captureSnapshotsForContextSlot (the cinatra#1430 \"capture at resolution time\" wiring in registerContextPrimitives). It is the single net-new node — object-content-snapshot's own transitive imports (./context-resolver, ./resource-store, ./local-disk-blob-store, the @/lib/postgres-sync* leaves, @cinatra-ai/objects/claims and @/lib/derived-store-ownership) were already reachable via the existing context-mcp / artifact-service graph, which is why the NET is +1 and not larger. /sign-in is flat at 147 — it does not reach the MCP tool-module registry in mcp-server.ts. Absorbed raise 1487->1488.",
      "pr": 1537
    },
    "/api/a2a": {
      "from": 1454,
      "to": 1455,
      "reason": "cinatra#1430 (PR #1537) capture content snapshots on the MCP context_resolve path, MEASURED in the PR worktree merged on origin/main 532874da (base ceilings set by #1517, which raised every tracked route +2) with the companion extension repos cloned pinned (missingCount 0) — never arithmetic (measured NET reachable-module-set diff via ablation: restoring origin/main's src/lib/artifacts/context-mcp.ts, which drops ONLY the new capture-wiring import edge, returns all four grown routes to EXACTLY their base ceilings and holds /sign-in at 147, so the entire growth is attributable to this feature and nothing else). Exactly one first-party module becomes reachable on each grown route: src/lib/artifacts/object-content-snapshot.ts, pulled onto the resolution path by context-mcp.ts's new static import of captureSnapshotsForContextSlot (the cinatra#1430 \"capture at resolution time\" wiring in registerContextPrimitives). It is the single net-new node — object-content-snapshot's own transitive imports (./context-resolver, ./resource-store, ./local-disk-blob-store, the @/lib/postgres-sync* leaves, @cinatra-ai/objects/claims and @/lib/derived-store-ownership) were already reachable via the existing context-mcp / artifact-service graph, which is why the NET is +1 and not larger. /sign-in is flat at 147 — it does not reach the MCP tool-module registry in mcp-server.ts. Absorbed raise 1454->1455.",
      "pr": 1537
    },
    "/api/llm-bridge": {
      "from": 1460,
      "to": 1461,
      "reason": "cinatra#1430 (PR #1537) capture content snapshots on the MCP context_resolve path, MEASURED in the PR worktree merged on origin/main 532874da (base ceilings set by #1517, which raised every tracked route +2) with the companion extension repos cloned pinned (missingCount 0) — never arithmetic (measured NET reachable-module-set diff via ablation: restoring origin/main's src/lib/artifacts/context-mcp.ts, which drops ONLY the new capture-wiring import edge, returns all four grown routes to EXACTLY their base ceilings and holds /sign-in at 147, so the entire growth is attributable to this feature and nothing else). Exactly one first-party module becomes reachable on each grown route: src/lib/artifacts/object-content-snapshot.ts, pulled onto the resolution path by context-mcp.ts's new static import of captureSnapshotsForContextSlot (the cinatra#1430 \"capture at resolution time\" wiring in registerContextPrimitives). It is the single net-new node — object-content-snapshot's own transitive imports (./context-resolver, ./resource-store, ./local-disk-blob-store, the @/lib/postgres-sync* leaves, @cinatra-ai/objects/claims and @/lib/derived-store-ownership) were already reachable via the existing context-mcp / artifact-service graph, which is why the NET is +1 and not larger. /sign-in is flat at 147 — it does not reach the MCP tool-module registry in mcp-server.ts. Absorbed raise 1460->1461.",
      "pr": 1537
    }
  }
}
