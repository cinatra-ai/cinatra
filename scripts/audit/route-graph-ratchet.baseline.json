{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1578,
    "/api/mcp": 1574,
    "/chat": 1648,
    "/api/a2a": 1579,
    "/api/llm-bridge": 1589
  },
  "absorbs": {
    "/sign-in": {
      "from": 1574,
      "to": 1578,
      "reason": "#2066 C0/C1 (epic #2037 lifecycle-interceptions): the canonical run view under /agents wires the run-embedding read layer into the eagerly-registered agent screens graph (packages/agents/src/instance-screens.tsx, the SetupScreen the plugins-registry statically maps). The SetupScreen now statically imports the left step rail + the run-scoped gate reader, so FOUR first-party modules become reachable on the universally-registered screens path every tracked route transitively hits: packages/agents/src/run-step-rail.ts (the rail merge-contract projection), packages/agents/src/run-step-rail-panel.tsx (the rail UI), packages/agents/src/artifact-review-gate-store.ts (via listReviewGatesForRun, the run-scoped gate enumerator C0 adds), and its transitive src/lib/artifacts/artifact-review-target.ts (the pinned review-target normaliser the gate store reads). The CI-measured delta is a UNIFORM +4 first-party modules (extensions cloned pinned, 0 missing). The growth is intrinsic to rendering a run’s steps + gates in the run view and irreducible: these are distinct read/render seams the run-view SetupScreen must reach, and dynamic-importing them purely to dodge the ratchet would game the metric at net zero. Raise is on top of origin/main’s carried #2041 ceiling 1574 for this route; 1574 + 4 = 1578. Replaces the base branch’s carried #2041 record for this route via this new annotated raise. If CI measures a different count, update this ceiling + the absorb to.",
      "pr": 2068
    },
    "/api/mcp": {
      "from": 1570,
      "to": 1574,
      "reason": "#2066 C0/C1 (epic #2037 lifecycle-interceptions): the canonical run view under /agents wires the run-embedding read layer into the eagerly-registered agent screens graph (packages/agents/src/instance-screens.tsx, the SetupScreen the plugins-registry statically maps). The SetupScreen now statically imports the left step rail + the run-scoped gate reader, so FOUR first-party modules become reachable on the universally-registered screens path every tracked route transitively hits: packages/agents/src/run-step-rail.ts (the rail merge-contract projection), packages/agents/src/run-step-rail-panel.tsx (the rail UI), packages/agents/src/artifact-review-gate-store.ts (via listReviewGatesForRun, the run-scoped gate enumerator C0 adds), and its transitive src/lib/artifacts/artifact-review-target.ts (the pinned review-target normaliser the gate store reads). The CI-measured delta is a UNIFORM +4 first-party modules (extensions cloned pinned, 0 missing). The growth is intrinsic to rendering a run’s steps + gates in the run view and irreducible: these are distinct read/render seams the run-view SetupScreen must reach, and dynamic-importing them purely to dodge the ratchet would game the metric at net zero. Raise is on top of origin/main’s carried #2041 ceiling 1570 for this route; 1570 + 4 = 1574. Replaces the base branch’s carried #2041 record for this route via this new annotated raise. If CI measures a different count, update this ceiling + the absorb to.",
      "pr": 2068
    },
    "/chat": {
      "from": 1644,
      "to": 1648,
      "reason": "#2066 C0/C1 (epic #2037 lifecycle-interceptions): the canonical run view under /agents wires the run-embedding read layer into the eagerly-registered agent screens graph (packages/agents/src/instance-screens.tsx, the SetupScreen the plugins-registry statically maps). The SetupScreen now statically imports the left step rail + the run-scoped gate reader, so FOUR first-party modules become reachable on the universally-registered screens path every tracked route transitively hits: packages/agents/src/run-step-rail.ts (the rail merge-contract projection), packages/agents/src/run-step-rail-panel.tsx (the rail UI), packages/agents/src/artifact-review-gate-store.ts (via listReviewGatesForRun, the run-scoped gate enumerator C0 adds), and its transitive src/lib/artifacts/artifact-review-target.ts (the pinned review-target normaliser the gate store reads). The CI-measured delta is a UNIFORM +4 first-party modules (extensions cloned pinned, 0 missing). The growth is intrinsic to rendering a run’s steps + gates in the run view and irreducible: these are distinct read/render seams the run-view SetupScreen must reach, and dynamic-importing them purely to dodge the ratchet would game the metric at net zero. Raise is on top of origin/main’s carried #2041 ceiling 1644 for this route; 1644 + 4 = 1648. Replaces the base branch’s carried #2041 record for this route via this new annotated raise. If CI measures a different count, update this ceiling + the absorb to.",
      "pr": 2068
    },
    "/api/a2a": {
      "from": 1575,
      "to": 1579,
      "reason": "#2066 C0/C1 (epic #2037 lifecycle-interceptions): the canonical run view under /agents wires the run-embedding read layer into the eagerly-registered agent screens graph (packages/agents/src/instance-screens.tsx, the SetupScreen the plugins-registry statically maps). The SetupScreen now statically imports the left step rail + the run-scoped gate reader, so FOUR first-party modules become reachable on the universally-registered screens path every tracked route transitively hits: packages/agents/src/run-step-rail.ts (the rail merge-contract projection), packages/agents/src/run-step-rail-panel.tsx (the rail UI), packages/agents/src/artifact-review-gate-store.ts (via listReviewGatesForRun, the run-scoped gate enumerator C0 adds), and its transitive src/lib/artifacts/artifact-review-target.ts (the pinned review-target normaliser the gate store reads). The CI-measured delta is a UNIFORM +4 first-party modules (extensions cloned pinned, 0 missing). The growth is intrinsic to rendering a run’s steps + gates in the run view and irreducible: these are distinct read/render seams the run-view SetupScreen must reach, and dynamic-importing them purely to dodge the ratchet would game the metric at net zero. Raise is on top of origin/main’s carried #2041 ceiling 1575 for this route; 1575 + 4 = 1579. Replaces the base branch’s carried #2041 record for this route via this new annotated raise. If CI measures a different count, update this ceiling + the absorb to.",
      "pr": 2068
    },
    "/api/llm-bridge": {
      "from": 1585,
      "to": 1589,
      "reason": "#2066 C0/C1 (epic #2037 lifecycle-interceptions): the canonical run view under /agents wires the run-embedding read layer into the eagerly-registered agent screens graph (packages/agents/src/instance-screens.tsx, the SetupScreen the plugins-registry statically maps). The SetupScreen now statically imports the left step rail + the run-scoped gate reader, so FOUR first-party modules become reachable on the universally-registered screens path every tracked route transitively hits: packages/agents/src/run-step-rail.ts (the rail merge-contract projection), packages/agents/src/run-step-rail-panel.tsx (the rail UI), packages/agents/src/artifact-review-gate-store.ts (via listReviewGatesForRun, the run-scoped gate enumerator C0 adds), and its transitive src/lib/artifacts/artifact-review-target.ts (the pinned review-target normaliser the gate store reads). The CI-measured delta is a UNIFORM +4 first-party modules (extensions cloned pinned, 0 missing). The growth is intrinsic to rendering a run’s steps + gates in the run view and irreducible: these are distinct read/render seams the run-view SetupScreen must reach, and dynamic-importing them purely to dodge the ratchet would game the metric at net zero. Raise is on top of origin/main’s carried #2041 ceiling 1585 for this route; 1585 + 4 = 1589. Replaces the base branch’s carried #2041 record for this route via this new annotated raise. If CI measures a different count, update this ceiling + the absorb to.",
      "pr": 2068
    }
  }
}
