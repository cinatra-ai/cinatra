{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 163,
    "/api/mcp": 1787,
    "/chat": 1890,
    "/api/a2a": 1795,
    "/api/llm-bridge": 1805
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1764,
      "to": 1795,
      "reason": "cinatra#3110 (issue #3033) raises each of these four ceilings by thirty-one, and the raise decomposes into two MEASURED parts on the one shared dispatch spine all four of these routes already reach and /sign-in does not. THIRTY: the four blog displays land on that spine - two host leaves on the artifact creation road, twenty-four renderer entries as the four displays enter the generated display map, and four for the versioned content channel wired to its two consumers. ONE: the later fix adds packages/agents/src/run-failure-floor.ts, the sanitized floor a failed run's declared artifact output draws, wired into packages/agents/src/execution.ts (composes the message on the server failure path) and packages/agents/src/agentic-run-panel.tsx (reduces a row written before that change on the client). MEASURED with the companion extension fleet cloned pinned (115 of 115, missing count 0), first at head e84470ec07f832a19aa909dc3e63c993c6138a7c and RE-MEASURED at the merge of the base branch into this one (merge commit e1550a6f74209a8fc71e3011379b9b9e5dbf228a): both trees measure /api/mcp 1787, /chat 1888, /api/a2a 1795, /api/llm-bridge 1805 and /sign-in 163, so every tracked route reports headroom zero at the merge and the wave the base branch took since this record was first written adds no first-party module to any tracked route. The base branch's own committed ceilings are unchanged by that wave (/api/mcp 1756, /chat 1857, /api/a2a 1764, /api/llm-bridge 1774), so `from` is still the base branch's own committed ceiling and the whole plus-thirty-one remains this branch's own. A per-route reachability probe over the SAME analyzer finds run-failure-floor.ts in exactly those four route graphs and NOT in /sign-in. A control measurement at the same head with only the two import edges to that leaf removed returns every route to exactly the ceiling committed before this raise (/api/mcp 1786, /chat 1887, /api/a2a 1794, /api/llm-bridge 1804), so the whole plus-one is that single leaf and nothing else. NARROWING WAS ATTEMPTED FIRST AND IS UNAVAILABLE: the leaf has ZERO first-party imports of its own, so there is no barrel import or cross-package edge to narrow; both consumers import functions they call, so neither import can become an erased type-only import; and a deferred import() narrows nothing because the analyzer follows dynamic imports too. The only way to drop the plus-one would be to drop the floor itself, and an installed display is reachable by design through the single dispatch spine. `from` is the base branch's own committed ceiling for the route; this record supersedes the record the earlier fix committed for the same route (same `from`, to 1794 / 1804 / 1786 / 1887), whose ceiling no longer exists. RE-MEASURED AGAIN AT THE SECOND MERGE of the base branch into this one, with the companion extension fleet cloned PINNED from the resolved lock (116 of 116, missing count 0): the merged tree measures /api/mcp 1787, /chat 1890, /api/a2a 1795, /api/llm-bridge 1805 and /sign-in 163. This route's ceiling and this record's from/to are UNCHANGED by that merge - the base branch's wave since the previous forward reaches only /chat, whose own record carries it - and the route reports headroom zero here.",
      "pr": 3110
    },
    "/api/llm-bridge": {
      "from": 1774,
      "to": 1805,
      "reason": "cinatra#3110 (issue #3033) raises each of these four ceilings by thirty-one, and the raise decomposes into two MEASURED parts on the one shared dispatch spine all four of these routes already reach and /sign-in does not. THIRTY: the four blog displays land on that spine - two host leaves on the artifact creation road, twenty-four renderer entries as the four displays enter the generated display map, and four for the versioned content channel wired to its two consumers. ONE: the later fix adds packages/agents/src/run-failure-floor.ts, the sanitized floor a failed run's declared artifact output draws, wired into packages/agents/src/execution.ts (composes the message on the server failure path) and packages/agents/src/agentic-run-panel.tsx (reduces a row written before that change on the client). MEASURED with the companion extension fleet cloned pinned (115 of 115, missing count 0), first at head e84470ec07f832a19aa909dc3e63c993c6138a7c and RE-MEASURED at the merge of the base branch into this one (merge commit e1550a6f74209a8fc71e3011379b9b9e5dbf228a): both trees measure /api/mcp 1787, /chat 1888, /api/a2a 1795, /api/llm-bridge 1805 and /sign-in 163, so every tracked route reports headroom zero at the merge and the wave the base branch took since this record was first written adds no first-party module to any tracked route. The base branch's own committed ceilings are unchanged by that wave (/api/mcp 1756, /chat 1857, /api/a2a 1764, /api/llm-bridge 1774), so `from` is still the base branch's own committed ceiling and the whole plus-thirty-one remains this branch's own. A per-route reachability probe over the SAME analyzer finds run-failure-floor.ts in exactly those four route graphs and NOT in /sign-in. A control measurement at the same head with only the two import edges to that leaf removed returns every route to exactly the ceiling committed before this raise (/api/mcp 1786, /chat 1887, /api/a2a 1794, /api/llm-bridge 1804), so the whole plus-one is that single leaf and nothing else. NARROWING WAS ATTEMPTED FIRST AND IS UNAVAILABLE: the leaf has ZERO first-party imports of its own, so there is no barrel import or cross-package edge to narrow; both consumers import functions they call, so neither import can become an erased type-only import; and a deferred import() narrows nothing because the analyzer follows dynamic imports too. The only way to drop the plus-one would be to drop the floor itself, and an installed display is reachable by design through the single dispatch spine. `from` is the base branch's own committed ceiling for the route; this record supersedes the record the earlier fix committed for the same route (same `from`, to 1794 / 1804 / 1786 / 1887), whose ceiling no longer exists. RE-MEASURED AGAIN AT THE SECOND MERGE of the base branch into this one, with the companion extension fleet cloned PINNED from the resolved lock (116 of 116, missing count 0): the merged tree measures /api/mcp 1787, /chat 1890, /api/a2a 1795, /api/llm-bridge 1805 and /sign-in 163. This route's ceiling and this record's from/to are UNCHANGED by that merge - the base branch's wave since the previous forward reaches only /chat, whose own record carries it - and the route reports headroom zero here.",
      "pr": 3110
    },
    "/api/mcp": {
      "from": 1756,
      "to": 1787,
      "reason": "cinatra#3110 (issue #3033) raises each of these four ceilings by thirty-one, and the raise decomposes into two MEASURED parts on the one shared dispatch spine all four of these routes already reach and /sign-in does not. THIRTY: the four blog displays land on that spine - two host leaves on the artifact creation road, twenty-four renderer entries as the four displays enter the generated display map, and four for the versioned content channel wired to its two consumers. ONE: the later fix adds packages/agents/src/run-failure-floor.ts, the sanitized floor a failed run's declared artifact output draws, wired into packages/agents/src/execution.ts (composes the message on the server failure path) and packages/agents/src/agentic-run-panel.tsx (reduces a row written before that change on the client). MEASURED with the companion extension fleet cloned pinned (115 of 115, missing count 0), first at head e84470ec07f832a19aa909dc3e63c993c6138a7c and RE-MEASURED at the merge of the base branch into this one (merge commit e1550a6f74209a8fc71e3011379b9b9e5dbf228a): both trees measure /api/mcp 1787, /chat 1888, /api/a2a 1795, /api/llm-bridge 1805 and /sign-in 163, so every tracked route reports headroom zero at the merge and the wave the base branch took since this record was first written adds no first-party module to any tracked route. The base branch's own committed ceilings are unchanged by that wave (/api/mcp 1756, /chat 1857, /api/a2a 1764, /api/llm-bridge 1774), so `from` is still the base branch's own committed ceiling and the whole plus-thirty-one remains this branch's own. A per-route reachability probe over the SAME analyzer finds run-failure-floor.ts in exactly those four route graphs and NOT in /sign-in. A control measurement at the same head with only the two import edges to that leaf removed returns every route to exactly the ceiling committed before this raise (/api/mcp 1786, /chat 1887, /api/a2a 1794, /api/llm-bridge 1804), so the whole plus-one is that single leaf and nothing else. NARROWING WAS ATTEMPTED FIRST AND IS UNAVAILABLE: the leaf has ZERO first-party imports of its own, so there is no barrel import or cross-package edge to narrow; both consumers import functions they call, so neither import can become an erased type-only import; and a deferred import() narrows nothing because the analyzer follows dynamic imports too. The only way to drop the plus-one would be to drop the floor itself, and an installed display is reachable by design through the single dispatch spine. `from` is the base branch's own committed ceiling for the route; this record supersedes the record the earlier fix committed for the same route (same `from`, to 1794 / 1804 / 1786 / 1887), whose ceiling no longer exists. RE-MEASURED AGAIN AT THE SECOND MERGE of the base branch into this one, with the companion extension fleet cloned PINNED from the resolved lock (116 of 116, missing count 0): the merged tree measures /api/mcp 1787, /chat 1890, /api/a2a 1795, /api/llm-bridge 1805 and /sign-in 163. This route's ceiling and this record's from/to are UNCHANGED by that merge - the base branch's wave since the previous forward reaches only /chat, whose own record carries it - and the route reports headroom zero here.",
      "pr": 3110
    },
    "/chat": {
      "from": 1859,
      "to": 1890,
      "reason": "cinatra#3110 (issue #3033) raises this ceiling by thirty-one, and the raise is this branch's own on the one shared dispatch spine all four raised routes reach and /sign-in does not. THIRTY: the four blog displays land on that spine - two host leaves on the artifact creation road, twenty-four renderer entries as the four displays enter the generated display map, and four for the versioned content channel wired to its two consumers. ONE: the later fix adds packages/agents/src/run-failure-floor.ts, the sanitized floor a failed run's declared artifact output draws, wired into packages/agents/src/execution.ts (composes the message on the server failure path) and packages/agents/src/agentic-run-panel.tsx (reduces a row written before that change on the client). RE-MEASURED AT THIS SECOND MERGE of the base branch into this one, the way CI measures and not computed: with the companion extension fleet cloned PINNED from the resolved lock (116 of 116, missing count 0) and the merge's own unmodified scripts/route-graph.mjs, the merged tree measures /chat 1890, /api/mcp 1787, /api/a2a 1795, /api/llm-bridge 1805 and /sign-in 163. `from` MOVES from 1857 to 1859 because that is the base branch's OWN committed ceiling now: the base branch raised this route by two after the previous forward (its own record, PR 3053, absorbing packages/chat/src/run-seed-request.ts and packages/chat/src/composer-reserved-space.ts), and this merge brings that raise forward. Those two modules are the WHOLE of the base branch's contribution to this route at the merge: the measured 1890 is exactly 1859 + 31, and the same thirty-one reproduces unchanged on the other three raised routes against their own unchanged base ceilings (1787-1756, 1795-1764, 1805-1774), so nothing of the base branch's wave composed onto this branch's own delta and nothing of this branch's delta was absorbed by it. /sign-in measures 163 and is written at its measured value with no record, exactly as the previous two forwards of this branch committed it. NARROWING WAS ATTEMPTED FIRST AND IS UNAVAILABLE for this branch's own leaves: the floor leaf has ZERO first-party imports of its own, so there is no barrel import or cross-package edge to narrow; both of its consumers import functions they call, so neither import can become an erased type-only import; and a deferred import() narrows nothing because the analyzer follows dynamic imports too. This record supersedes the record the previous forward committed for this route (from 1857, to 1888), whose ceiling no longer exists.",
      "pr": 3110
    }
  }
}
