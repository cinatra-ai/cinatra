{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1488,
    "/api/mcp": 1484,
    "/chat": 1544,
    "/api/a2a": 1489,
    "/api/llm-bridge": 1495
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1488,
      "to": 1489,
      "reason": "store.ts file-size-ratchet repair (cinatra#1823, PR #1827): the built-in assistant-agent registration seam — the BUILT_IN_{CINATRA,WORDPRESS,DRUPAL}_ASSISTANT_{TEMPLATE_ID,PACKAGE_NAME} identity constants, upsertBuiltInAssistantAgentTemplate, and readAssistantConfigByPrincipalId — was extracted VERBATIM out of packages/agents/src/store.ts into the new leaf module packages/agents/src/builtin-assistant-template.ts so store.ts drops back under its 4323-line file-size ceiling (the P4.1 WordPress+Drupal registration added the two sibling built-in identities + optional templateId/packageName/sourceNl params inline, pushing store.ts to 4353; the file-size ratchet is shrink-only). ./store re-exports the whole surface, so the new module is first-party-reachable wherever ./store is; it pulls NO new subtree (its only first-party imports — db from ./db, agentTemplates from ./schema — are already reachable from ./store), so it adds exactly itself (+1 module) to this route's graph, exactly like the ./agent-run-hitl-prompts (PR #1813) and ./template-snapshot (PR #1754) precedents. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0): a clean +1 (1488->1489).",
      "pr": 1827
    },
    "/api/llm-bridge": {
      "from": 1494,
      "to": 1495,
      "reason": "store.ts file-size-ratchet repair (cinatra#1823, PR #1827): the built-in assistant-agent registration seam — the BUILT_IN_{CINATRA,WORDPRESS,DRUPAL}_ASSISTANT_{TEMPLATE_ID,PACKAGE_NAME} identity constants, upsertBuiltInAssistantAgentTemplate, and readAssistantConfigByPrincipalId — was extracted VERBATIM out of packages/agents/src/store.ts into the new leaf module packages/agents/src/builtin-assistant-template.ts so store.ts drops back under its 4323-line file-size ceiling (the P4.1 WordPress+Drupal registration added the two sibling built-in identities + optional templateId/packageName/sourceNl params inline, pushing store.ts to 4353; the file-size ratchet is shrink-only). ./store re-exports the whole surface, so the new module is first-party-reachable wherever ./store is; it pulls NO new subtree (its only first-party imports — db from ./db, agentTemplates from ./schema — are already reachable from ./store), so it adds exactly itself (+1 module) to this route's graph, exactly like the ./agent-run-hitl-prompts (PR #1813) and ./template-snapshot (PR #1754) precedents. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0): a clean +1 (1494->1495).",
      "pr": 1827
    },
    "/api/mcp": {
      "from": 1483,
      "to": 1484,
      "reason": "store.ts file-size-ratchet repair (cinatra#1823, PR #1827): the built-in assistant-agent registration seam — the BUILT_IN_{CINATRA,WORDPRESS,DRUPAL}_ASSISTANT_{TEMPLATE_ID,PACKAGE_NAME} identity constants, upsertBuiltInAssistantAgentTemplate, and readAssistantConfigByPrincipalId — was extracted VERBATIM out of packages/agents/src/store.ts into the new leaf module packages/agents/src/builtin-assistant-template.ts so store.ts drops back under its 4323-line file-size ceiling (the P4.1 WordPress+Drupal registration added the two sibling built-in identities + optional templateId/packageName/sourceNl params inline, pushing store.ts to 4353; the file-size ratchet is shrink-only). ./store re-exports the whole surface, so the new module is first-party-reachable wherever ./store is; it pulls NO new subtree (its only first-party imports — db from ./db, agentTemplates from ./schema — are already reachable from ./store), so it adds exactly itself (+1 module) to this route's graph, exactly like the ./agent-run-hitl-prompts (PR #1813) and ./template-snapshot (PR #1754) precedents. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0): a clean +1 (1483->1484).",
      "pr": 1827
    },
    "/chat": {
      "from": 1543,
      "to": 1544,
      "reason": "store.ts file-size-ratchet repair (cinatra#1823, PR #1827): the built-in assistant-agent registration seam — the BUILT_IN_{CINATRA,WORDPRESS,DRUPAL}_ASSISTANT_{TEMPLATE_ID,PACKAGE_NAME} identity constants, upsertBuiltInAssistantAgentTemplate, and readAssistantConfigByPrincipalId — was extracted VERBATIM out of packages/agents/src/store.ts into the new leaf module packages/agents/src/builtin-assistant-template.ts so store.ts drops back under its 4323-line file-size ceiling (the P4.1 WordPress+Drupal registration added the two sibling built-in identities + optional templateId/packageName/sourceNl params inline, pushing store.ts to 4353; the file-size ratchet is shrink-only). ./store re-exports the whole surface, so the new module is first-party-reachable wherever ./store is; it pulls NO new subtree (its only first-party imports — db from ./db, agentTemplates from ./schema — are already reachable from ./store), so it adds exactly itself (+1 module) to this route's graph, exactly like the ./agent-run-hitl-prompts (PR #1813) and ./template-snapshot (PR #1754) precedents. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0): a clean +1 (1543->1544).",
      "pr": 1827
    },
    "/sign-in": {
      "from": 1487,
      "to": 1488,
      "reason": "store.ts file-size-ratchet repair (cinatra#1823, PR #1827): the built-in assistant-agent registration seam — the BUILT_IN_{CINATRA,WORDPRESS,DRUPAL}_ASSISTANT_{TEMPLATE_ID,PACKAGE_NAME} identity constants, upsertBuiltInAssistantAgentTemplate, and readAssistantConfigByPrincipalId — was extracted VERBATIM out of packages/agents/src/store.ts into the new leaf module packages/agents/src/builtin-assistant-template.ts so store.ts drops back under its 4323-line file-size ceiling (the P4.1 WordPress+Drupal registration added the two sibling built-in identities + optional templateId/packageName/sourceNl params inline, pushing store.ts to 4353; the file-size ratchet is shrink-only). ./store re-exports the whole surface, so the new module is first-party-reachable wherever ./store is; it pulls NO new subtree (its only first-party imports — db from ./db, agentTemplates from ./schema — are already reachable from ./store), so it adds exactly itself (+1 module) to this route's graph, exactly like the ./agent-run-hitl-prompts (PR #1813) and ./template-snapshot (PR #1754) precedents. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0): a clean +1 (1487->1488).",
      "pr": 1827
    }
  }
}
