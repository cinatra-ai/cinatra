{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1484,
    "/api/mcp": 1480,
    "/chat": 1540,
    "/api/a2a": 1485,
    "/api/llm-bridge": 1491
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1484,
      "to": 1485,
      "reason": "cinatra#1450 publication-operation ledger (PR #1774): one net-new leaf module becomes reachable on this route. src/lib/artifacts/publication-operation-schema.ts — the PURE-STRINGS bootstrap DDL leaf (publicationOperationLedgerSchemaQueries for the artifact_publication_operations table); ZERO first-party imports, a true leaf pulling no further subtree — spread into buildCreateStoreSchemaQueries in the already-reachable src/lib/drizzle-store.ts (the DB store is reachable from every route) so fresh-bootstrap and the core__0055 operator-upgrade converge. The sibling publication-* modules (the pure state machine, the DB ledger authority, the status port, the shared types) are consumed via the injected PublicationStatusPort / not yet wired into a route, so they are NOT reachable in this PR; the route grows by exactly +1. NOT a narrowable barrel import: buildCreateStoreSchemaQueries genuinely spreads the DDL leaf at store-bootstrap time. Re-measured on the #1774 branch merged onto main fa1a94109, with the pinned companion-extension clones present at missingCount 0 (scripts/audit/route-graph-ratchet.mjs --report): /api/a2a 1484 -> 1485 (NET +1). Retires the PR #1754 record by re-raising the ceiling.",
      "pr": 1774
    },
    "/api/llm-bridge": {
      "from": 1490,
      "to": 1491,
      "reason": "cinatra#1450 publication-operation ledger (PR #1774): one net-new leaf module becomes reachable on this route. src/lib/artifacts/publication-operation-schema.ts — the PURE-STRINGS bootstrap DDL leaf (publicationOperationLedgerSchemaQueries for the artifact_publication_operations table); ZERO first-party imports, a true leaf pulling no further subtree — spread into buildCreateStoreSchemaQueries in the already-reachable src/lib/drizzle-store.ts (the DB store is reachable from every route) so fresh-bootstrap and the core__0055 operator-upgrade converge. The sibling publication-* modules (the pure state machine, the DB ledger authority, the status port, the shared types) are consumed via the injected PublicationStatusPort / not yet wired into a route, so they are NOT reachable in this PR; the route grows by exactly +1. NOT a narrowable barrel import: buildCreateStoreSchemaQueries genuinely spreads the DDL leaf at store-bootstrap time. Re-measured on the #1774 branch merged onto main fa1a94109, with the pinned companion-extension clones present at missingCount 0 (scripts/audit/route-graph-ratchet.mjs --report): /api/llm-bridge 1490 -> 1491 (NET +1). Retires the PR #1754 record by re-raising the ceiling.",
      "pr": 1774
    },
    "/api/mcp": {
      "from": 1479,
      "to": 1480,
      "reason": "cinatra#1450 publication-operation ledger (PR #1774): one net-new leaf module becomes reachable on this route. src/lib/artifacts/publication-operation-schema.ts — the PURE-STRINGS bootstrap DDL leaf (publicationOperationLedgerSchemaQueries for the artifact_publication_operations table); ZERO first-party imports, a true leaf pulling no further subtree — spread into buildCreateStoreSchemaQueries in the already-reachable src/lib/drizzle-store.ts (the DB store is reachable from every route) so fresh-bootstrap and the core__0055 operator-upgrade converge. The sibling publication-* modules (the pure state machine, the DB ledger authority, the status port, the shared types) are consumed via the injected PublicationStatusPort / not yet wired into a route, so they are NOT reachable in this PR; the route grows by exactly +1. NOT a narrowable barrel import: buildCreateStoreSchemaQueries genuinely spreads the DDL leaf at store-bootstrap time. Re-measured on the #1774 branch merged onto main fa1a94109, with the pinned companion-extension clones present at missingCount 0 (scripts/audit/route-graph-ratchet.mjs --report): /api/mcp 1479 -> 1480 (NET +1). Retires the PR #1754 record by re-raising the ceiling.",
      "pr": 1774
    },
    "/chat": {
      "from": 1539,
      "to": 1540,
      "reason": "cinatra#1450 publication-operation ledger (PR #1774): one net-new leaf module becomes reachable on this route. src/lib/artifacts/publication-operation-schema.ts — the PURE-STRINGS bootstrap DDL leaf (publicationOperationLedgerSchemaQueries for the artifact_publication_operations table); ZERO first-party imports, a true leaf pulling no further subtree — spread into buildCreateStoreSchemaQueries in the already-reachable src/lib/drizzle-store.ts (the DB store is reachable from every route) so fresh-bootstrap and the core__0055 operator-upgrade converge. The sibling publication-* modules (the pure state machine, the DB ledger authority, the status port, the shared types) are consumed via the injected PublicationStatusPort / not yet wired into a route, so they are NOT reachable in this PR; the route grows by exactly +1. NOT a narrowable barrel import: buildCreateStoreSchemaQueries genuinely spreads the DDL leaf at store-bootstrap time. Re-measured on the #1774 branch merged onto main fa1a94109, with the pinned companion-extension clones present at missingCount 0 (scripts/audit/route-graph-ratchet.mjs --report): /chat 1539 -> 1540 (NET +1). Retires the PR #1754 record by re-raising the ceiling.",
      "pr": 1774
    },
    "/sign-in": {
      "from": 1483,
      "to": 1484,
      "reason": "cinatra#1450 publication-operation ledger (PR #1774): one net-new leaf module becomes reachable on this route. src/lib/artifacts/publication-operation-schema.ts — the PURE-STRINGS bootstrap DDL leaf (publicationOperationLedgerSchemaQueries for the artifact_publication_operations table); ZERO first-party imports, a true leaf pulling no further subtree — spread into buildCreateStoreSchemaQueries in the already-reachable src/lib/drizzle-store.ts (the DB store is reachable from every route) so fresh-bootstrap and the core__0055 operator-upgrade converge. The sibling publication-* modules (the pure state machine, the DB ledger authority, the status port, the shared types) are consumed via the injected PublicationStatusPort / not yet wired into a route, so they are NOT reachable in this PR; the route grows by exactly +1. NOT a narrowable barrel import: buildCreateStoreSchemaQueries genuinely spreads the DDL leaf at store-bootstrap time. Re-measured on the #1774 branch merged onto main fa1a94109, with the pinned companion-extension clones present at missingCount 0 (scripts/audit/route-graph-ratchet.mjs --report): /sign-in 1483 -> 1484 (NET +1). Retires the PR #1754 record by re-raising the ceiling.",
      "pr": 1774
    }
  }
}
