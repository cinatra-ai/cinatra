{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions \u2014 a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 134,
    "/api/mcp": 1386,
    "/chat": 1424,
    "/api/a2a": 1391,
    "/api/llm-bridge": 1397
  },
  "absorbs": {
    "/sign-in": {
      "from": 133,
      "to": 134,
      "reason": "cinatra#534 (PR #1289): the capability-ownership grant primitive (S0) extracted the two grant tables' bootstrap DDL into a pure-strings leaf src/lib/extension-grant-schema.ts imported by drizzle-store.ts. /sign-in reaches drizzle-store (auth -> DB) but NOT the install pipeline, so it gains exactly this one leaf (the ownership-grant primitive stays off /sign-in's graph). Delta measured B-A on the identical rebased tree. Absorbed raise 133->134.",
      "pr": 1289
    },
    "/api/mcp": {
      "from": 1384,
      "to": 1386,
      "reason": "cinatra#534 (PR #1289): the capability-ownership grant primitive (S0). +2 reachable first-party leaves on every locked route that reaches BOTH the extension install pipeline AND drizzle-store: (1) src/lib/extension-capability-ownership-grants.ts (the grant primitive + its install-orchestration helpers) reached from extension-install-pipeline.ts via the static step-helper imports + the deps factory; (2) src/lib/extension-grant-schema.ts (the pure-strings bootstrap-DDL leaf the two grant tables were extracted into) reached from drizzle-store.ts. Neither new module has any first-party transitive import (server-only / node:crypto / pg only), so each contributes exactly one leaf. Delta measured B-A on the identical rebased tree with the companion extension repos linked (same unresolved-edge offset on both sides, so the +2 is exact). Absorbed raise 1384->1386.",
      "pr": 1289
    },
    "/chat": {
      "from": 1422,
      "to": 1424,
      "reason": "cinatra#534 (PR #1289): the capability-ownership grant primitive (S0). +2 reachable first-party leaves on every locked route that reaches BOTH the extension install pipeline AND drizzle-store: (1) src/lib/extension-capability-ownership-grants.ts (the grant primitive + its install-orchestration helpers) reached from extension-install-pipeline.ts via the static step-helper imports + the deps factory; (2) src/lib/extension-grant-schema.ts (the pure-strings bootstrap-DDL leaf the two grant tables were extracted into) reached from drizzle-store.ts. Neither new module has any first-party transitive import (server-only / node:crypto / pg only), so each contributes exactly one leaf. Delta measured B-A on the identical rebased tree with the companion extension repos linked (same unresolved-edge offset on both sides, so the +2 is exact). Absorbed raise 1422->1424.",
      "pr": 1289
    },
    "/api/a2a": {
      "from": 1389,
      "to": 1391,
      "reason": "cinatra#534 (PR #1289): the capability-ownership grant primitive (S0). +2 reachable first-party leaves on every locked route that reaches BOTH the extension install pipeline AND drizzle-store: (1) src/lib/extension-capability-ownership-grants.ts (the grant primitive + its install-orchestration helpers) reached from extension-install-pipeline.ts via the static step-helper imports + the deps factory; (2) src/lib/extension-grant-schema.ts (the pure-strings bootstrap-DDL leaf the two grant tables were extracted into) reached from drizzle-store.ts. Neither new module has any first-party transitive import (server-only / node:crypto / pg only), so each contributes exactly one leaf. Delta measured B-A on the identical rebased tree with the companion extension repos linked (same unresolved-edge offset on both sides, so the +2 is exact). Absorbed raise 1389->1391.",
      "pr": 1289
    },
    "/api/llm-bridge": {
      "from": 1395,
      "to": 1397,
      "reason": "cinatra#534 (PR #1289): the capability-ownership grant primitive (S0). +2 reachable first-party leaves on every locked route that reaches BOTH the extension install pipeline AND drizzle-store: (1) src/lib/extension-capability-ownership-grants.ts (the grant primitive + its install-orchestration helpers) reached from extension-install-pipeline.ts via the static step-helper imports + the deps factory; (2) src/lib/extension-grant-schema.ts (the pure-strings bootstrap-DDL leaf the two grant tables were extracted into) reached from drizzle-store.ts. Neither new module has any first-party transitive import (server-only / node:crypto / pg only), so each contributes exactly one leaf. Delta measured B-A on the identical rebased tree with the companion extension repos linked (same unresolved-edge offset on both sides, so the +2 is exact). Absorbed raise 1395->1397.",
      "pr": 1289
    }
  }
}
