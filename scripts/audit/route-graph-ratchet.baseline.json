{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1648,
    "/api/mcp": 1644,
    "/chat": 1722,
    "/api/a2a": 1649,
    "/api/llm-bridge": 1659
  },
  "absorbs": {
    "/sign-in": {
      "from": 1647,
      "to": 1648,
      "reason": "cinatra#1940 P4 (lease-expiry finalizer): systemLoopPhases() (src/lib/boot/phases/system-loops.ts — already reachable from all five locked routes via the boot registrar, the same chain the #2213 record above documents) gains one more boot phase that dynamically imports @cinatra-ai/agents/lease-expiry-finalizer to register the runner slot and seed the ~60s sweep loop. scripts/route-graph.mjs follows dynamic import() edges (established by the #2213 record's own note), so this adds exactly ONE new first-party leaf module — lease-expiry-finalizer.ts itself — to every locked route's graph. The delta is +1, not more, because everything that leaf module in turn imports (the org-write-kernel guardOrgLifecycleMutation fence, the A2A cancel client, the agents store) was already reachable from these routes via existing @cinatra-ai/agents / @cinatra-ai/org-write-kernel usage elsewhere in the app-shell graph; only the new file itself is net-new. The raise cannot be narrowed further: the sweep core is deliberately kept OUT of background-jobs-registry.ts's own (already-on-this-graph) import surface via a registerLeaseExpiryFinalizerRunner indirection specifically to avoid widening this edge any further (see that phase's own comment), and a background loop cannot be seeded at boot without some reference — static or dynamic — to the function it seeds. Re-measured on this PR head with the companion extensions cloned pinned, matching CI's own count.",
      "pr": 2224
    },
    "/api/mcp": {
      "from": 1643,
      "to": 1644,
      "reason": "cinatra#1940 P4 (lease-expiry finalizer): systemLoopPhases() (src/lib/boot/phases/system-loops.ts — already reachable from all five locked routes via the boot registrar, the same chain the #2213 record above documents) gains one more boot phase that dynamically imports @cinatra-ai/agents/lease-expiry-finalizer to register the runner slot and seed the ~60s sweep loop. scripts/route-graph.mjs follows dynamic import() edges (established by the #2213 record's own note), so this adds exactly ONE new first-party leaf module — lease-expiry-finalizer.ts itself — to every locked route's graph. The delta is +1, not more, because everything that leaf module in turn imports (the org-write-kernel guardOrgLifecycleMutation fence, the A2A cancel client, the agents store) was already reachable from these routes via existing @cinatra-ai/agents / @cinatra-ai/org-write-kernel usage elsewhere in the app-shell graph; only the new file itself is net-new. The raise cannot be narrowed further: the sweep core is deliberately kept OUT of background-jobs-registry.ts's own (already-on-this-graph) import surface via a registerLeaseExpiryFinalizerRunner indirection specifically to avoid widening this edge any further (see that phase's own comment), and a background loop cannot be seeded at boot without some reference — static or dynamic — to the function it seeds. Re-measured on this PR head with the companion extensions cloned pinned, matching CI's own count.",
      "pr": 2224
    },
    "/chat": {
      "from": 1721,
      "to": 1722,
      "reason": "cinatra#1940 P4 (lease-expiry finalizer): systemLoopPhases() (src/lib/boot/phases/system-loops.ts — already reachable from all five locked routes via the boot registrar, the same chain the #2213 record above documents) gains one more boot phase that dynamically imports @cinatra-ai/agents/lease-expiry-finalizer to register the runner slot and seed the ~60s sweep loop. scripts/route-graph.mjs follows dynamic import() edges (established by the #2213 record's own note), so this adds exactly ONE new first-party leaf module — lease-expiry-finalizer.ts itself — to every locked route's graph. The delta is +1, not more, because everything that leaf module in turn imports (the org-write-kernel guardOrgLifecycleMutation fence, the A2A cancel client, the agents store) was already reachable from these routes via existing @cinatra-ai/agents / @cinatra-ai/org-write-kernel usage elsewhere in the app-shell graph; only the new file itself is net-new. The raise cannot be narrowed further: the sweep core is deliberately kept OUT of background-jobs-registry.ts's own (already-on-this-graph) import surface via a registerLeaseExpiryFinalizerRunner indirection specifically to avoid widening this edge any further (see that phase's own comment), and a background loop cannot be seeded at boot without some reference — static or dynamic — to the function it seeds. Re-measured on this PR head with the companion extensions cloned pinned, matching CI's own count.",
      "pr": 2224
    },
    "/api/a2a": {
      "from": 1648,
      "to": 1649,
      "reason": "cinatra#1940 P4 (lease-expiry finalizer): systemLoopPhases() (src/lib/boot/phases/system-loops.ts — already reachable from all five locked routes via the boot registrar, the same chain the #2213 record above documents) gains one more boot phase that dynamically imports @cinatra-ai/agents/lease-expiry-finalizer to register the runner slot and seed the ~60s sweep loop. scripts/route-graph.mjs follows dynamic import() edges (established by the #2213 record's own note), so this adds exactly ONE new first-party leaf module — lease-expiry-finalizer.ts itself — to every locked route's graph. The delta is +1, not more, because everything that leaf module in turn imports (the org-write-kernel guardOrgLifecycleMutation fence, the A2A cancel client, the agents store) was already reachable from these routes via existing @cinatra-ai/agents / @cinatra-ai/org-write-kernel usage elsewhere in the app-shell graph; only the new file itself is net-new. The raise cannot be narrowed further: the sweep core is deliberately kept OUT of background-jobs-registry.ts's own (already-on-this-graph) import surface via a registerLeaseExpiryFinalizerRunner indirection specifically to avoid widening this edge any further (see that phase's own comment), and a background loop cannot be seeded at boot without some reference — static or dynamic — to the function it seeds. Re-measured on this PR head with the companion extensions cloned pinned, matching CI's own count.",
      "pr": 2224
    },
    "/api/llm-bridge": {
      "from": 1658,
      "to": 1659,
      "reason": "cinatra#1940 P4 (lease-expiry finalizer): systemLoopPhases() (src/lib/boot/phases/system-loops.ts — already reachable from all five locked routes via the boot registrar, the same chain the #2213 record above documents) gains one more boot phase that dynamically imports @cinatra-ai/agents/lease-expiry-finalizer to register the runner slot and seed the ~60s sweep loop. scripts/route-graph.mjs follows dynamic import() edges (established by the #2213 record's own note), so this adds exactly ONE new first-party leaf module — lease-expiry-finalizer.ts itself — to every locked route's graph. The delta is +1, not more, because everything that leaf module in turn imports (the org-write-kernel guardOrgLifecycleMutation fence, the A2A cancel client, the agents store) was already reachable from these routes via existing @cinatra-ai/agents / @cinatra-ai/org-write-kernel usage elsewhere in the app-shell graph; only the new file itself is net-new. The raise cannot be narrowed further: the sweep core is deliberately kept OUT of background-jobs-registry.ts's own (already-on-this-graph) import surface via a registerLeaseExpiryFinalizerRunner indirection specifically to avoid widening this edge any further (see that phase's own comment), and a background loop cannot be seeded at boot without some reference — static or dynamic — to the function it seeds. Re-measured on this PR head with the companion extensions cloned pinned, matching CI's own count.",
      "pr": 2224
    }
  }
}
