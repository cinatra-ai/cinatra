{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 223,
    "/api/mcp": 1755,
    "/chat": 1856,
    "/api/a2a": 1763,
    "/api/llm-bridge": 1773
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1761,
      "to": 1763,
      "reason": "Pull request #3074 carries this record. `from` (1761) is main's OWN committed ceiling and `to` (1763) is this branch's MEASURED count on this route: a raise of +2 modules since main, and both of them are named below.\n\nMEASURED, NOT PICKED — in ONE environment, with the companion extension fleet cloned PINNED at the merged lock's SHAs (115/115), an unmodified scripts/route-graph.mjs and missingCount 0 on every tracked route. A CONTROL checkout of this branch's PARENT — the forward merge of main, 94ea79ecb494503b7952ef1738aa8f380cc9e139 — measures 1754 / 1855 / 1762 / 1772 for /api/mcp, /chat, /api/a2a and /api/llm-bridge, reproducing the ceilings this baseline already carried EXACTLY (1762 on this route), and this branch's head measures 1755 / 1856 / 1763 / 1773 (1763 here). So the split is unambiguous: +1 was already absorbed for the forward merge, whose record this raise REPLACES rather than deletes — main's own move since the merge base is +8 on this route (the overlay-collision bound of cinatra#3105, the shared local-caller decision of cinatra#3123, and the episode display joining the generated artifact-renderer map, cinatra#3093), already inside `from`, and the branch's own leaf on that +1 is `packages/agents/src/end-node-output-pickup.ts` — and the remaining +1 is this leg's, measured as the set difference against that control and stated next.\n\nWHAT THIS LEG ADDS. The conformance fix makes the run's own record a STEP THAT OPENS, one card per step page, so the rail's shared non-step row `packages/agents/src/run-step-rail-extra-entry.tsx` — already on all four of these routes before this leg — now has to read the frame's selection.\n\nNARROWING WAS AVAILABLE AND WAS TAKEN, so the record is for +1 and not +2. Read from the frame module `run-surface-rail.tsx`, that single import dragged the whole two-column frame and its step module onto every one of these routes: +2 each, and this leg's first head measured 1764 here (1756 / 1857 / 1764 / 1774 across the four). The context and its hook were therefore lifted into their own module, `packages/agents/src/run-surface-rail-selection.tsx`, whose only value import is React — the same split the rail's labels (`run-surface-rail-labels.ts`) and its step predicates (`run-surface-rail-step.ts`) already took, for the same reason. The measured set difference against the control is then EXACTLY ONE path on each of the four routes, that module and nothing else.\n\nIT CANNOT BE FOLDED AWAY. The row and the frame are drawn by different modules on the two branches of this screen — one where the rail is the page's own column, one where it lives inside the stepper panel — so the row cannot be handed the selection as a prop from a parent that does not always have one; the selection has to be a context, and a context has to live in a module. Putting it back in the frame is the +2 this record refuses. `packages/agents/src/__tests__/run-surface-rail-selection-narrowness.test.ts` reds if the row reaches for the frame module again, or if any value import appears in the selection module and puts the cost back.\n\n/sign-in measures 161 in every tree and its ceiling is kept at main's 223 with main's own record carried forward unaltered.",
      "pr": 3074
    },
    "/api/llm-bridge": {
      "from": 1771,
      "to": 1773,
      "reason": "Pull request #3074 carries this record. `from` (1771) is main's OWN committed ceiling and `to` (1773) is this branch's MEASURED count on this route: a raise of +2 modules since main, and both of them are named below.\n\nMEASURED, NOT PICKED — in ONE environment, with the companion extension fleet cloned PINNED at the merged lock's SHAs (115/115), an unmodified scripts/route-graph.mjs and missingCount 0 on every tracked route. A CONTROL checkout of this branch's PARENT — the forward merge of main, 94ea79ecb494503b7952ef1738aa8f380cc9e139 — measures 1754 / 1855 / 1762 / 1772 for /api/mcp, /chat, /api/a2a and /api/llm-bridge, reproducing the ceilings this baseline already carried EXACTLY (1772 on this route), and this branch's head measures 1755 / 1856 / 1763 / 1773 (1773 here). So the split is unambiguous: +1 was already absorbed for the forward merge, whose record this raise REPLACES rather than deletes — main's own move since the merge base is +8 on this route (the overlay-collision bound of cinatra#3105, the shared local-caller decision of cinatra#3123, and the episode display joining the generated artifact-renderer map, cinatra#3093), already inside `from`, and the branch's own leaf on that +1 is `packages/agents/src/end-node-output-pickup.ts` — and the remaining +1 is this leg's, measured as the set difference against that control and stated next.\n\nWHAT THIS LEG ADDS. The conformance fix makes the run's own record a STEP THAT OPENS, one card per step page, so the rail's shared non-step row `packages/agents/src/run-step-rail-extra-entry.tsx` — already on all four of these routes before this leg — now has to read the frame's selection.\n\nNARROWING WAS AVAILABLE AND WAS TAKEN, so the record is for +1 and not +2. Read from the frame module `run-surface-rail.tsx`, that single import dragged the whole two-column frame and its step module onto every one of these routes: +2 each, and this leg's first head measured 1774 here (1756 / 1857 / 1764 / 1774 across the four). The context and its hook were therefore lifted into their own module, `packages/agents/src/run-surface-rail-selection.tsx`, whose only value import is React — the same split the rail's labels (`run-surface-rail-labels.ts`) and its step predicates (`run-surface-rail-step.ts`) already took, for the same reason. The measured set difference against the control is then EXACTLY ONE path on each of the four routes, that module and nothing else.\n\nIT CANNOT BE FOLDED AWAY. The row and the frame are drawn by different modules on the two branches of this screen — one where the rail is the page's own column, one where it lives inside the stepper panel — so the row cannot be handed the selection as a prop from a parent that does not always have one; the selection has to be a context, and a context has to live in a module. Putting it back in the frame is the +2 this record refuses. `packages/agents/src/__tests__/run-surface-rail-selection-narrowness.test.ts` reds if the row reaches for the frame module again, or if any value import appears in the selection module and puts the cost back.\n\n/sign-in measures 161 in every tree and its ceiling is kept at main's 223 with main's own record carried forward unaltered.",
      "pr": 3074
    },
    "/api/mcp": {
      "from": 1753,
      "to": 1755,
      "reason": "Pull request #3074 carries this record. `from` (1753) is main's OWN committed ceiling and `to` (1755) is this branch's MEASURED count on this route: a raise of +2 modules since main, and both of them are named below.\n\nMEASURED, NOT PICKED — in ONE environment, with the companion extension fleet cloned PINNED at the merged lock's SHAs (115/115), an unmodified scripts/route-graph.mjs and missingCount 0 on every tracked route. A CONTROL checkout of this branch's PARENT — the forward merge of main, 94ea79ecb494503b7952ef1738aa8f380cc9e139 — measures 1754 / 1855 / 1762 / 1772 for /api/mcp, /chat, /api/a2a and /api/llm-bridge, reproducing the ceilings this baseline already carried EXACTLY (1754 on this route), and this branch's head measures 1755 / 1856 / 1763 / 1773 (1755 here). So the split is unambiguous: +1 was already absorbed for the forward merge, whose record this raise REPLACES rather than deletes — main's own move since the merge base is +5 on this route (the overlay-collision bound leaving the design registry's `utils` item, cinatra#3105, and the episode display joining the generated artifact-renderer map, cinatra#3093), already inside `from`, and the branch's own leaf on that +1 is `packages/agents/src/end-node-output-pickup.ts` — and the remaining +1 is this leg's, measured as the set difference against that control and stated next.\n\nWHAT THIS LEG ADDS. The conformance fix makes the run's own record a STEP THAT OPENS, one card per step page, so the rail's shared non-step row `packages/agents/src/run-step-rail-extra-entry.tsx` — already on all four of these routes before this leg — now has to read the frame's selection.\n\nNARROWING WAS AVAILABLE AND WAS TAKEN, so the record is for +1 and not +2. Read from the frame module `run-surface-rail.tsx`, that single import dragged the whole two-column frame and its step module onto every one of these routes: +2 each, and this leg's first head measured 1756 here (1756 / 1857 / 1764 / 1774 across the four). The context and its hook were therefore lifted into their own module, `packages/agents/src/run-surface-rail-selection.tsx`, whose only value import is React — the same split the rail's labels (`run-surface-rail-labels.ts`) and its step predicates (`run-surface-rail-step.ts`) already took, for the same reason. The measured set difference against the control is then EXACTLY ONE path on each of the four routes, that module and nothing else.\n\nIT CANNOT BE FOLDED AWAY. The row and the frame are drawn by different modules on the two branches of this screen — one where the rail is the page's own column, one where it lives inside the stepper panel — so the row cannot be handed the selection as a prop from a parent that does not always have one; the selection has to be a context, and a context has to live in a module. Putting it back in the frame is the +2 this record refuses. `packages/agents/src/__tests__/run-surface-rail-selection-narrowness.test.ts` reds if the row reaches for the frame module again, or if any value import appears in the selection module and puts the cost back.\n\n/sign-in measures 161 in every tree and its ceiling is kept at main's 223 with main's own record carried forward unaltered.",
      "pr": 3074
    },
    "/chat": {
      "from": 1854,
      "to": 1856,
      "reason": "Pull request #3074 carries this record. `from` (1854) is main's OWN committed ceiling and `to` (1856) is this branch's MEASURED count on this route: a raise of +2 modules since main, and both of them are named below.\n\nMEASURED, NOT PICKED — in ONE environment, with the companion extension fleet cloned PINNED at the merged lock's SHAs (115/115), an unmodified scripts/route-graph.mjs and missingCount 0 on every tracked route. A CONTROL checkout of this branch's PARENT — the forward merge of main, 94ea79ecb494503b7952ef1738aa8f380cc9e139 — measures 1754 / 1855 / 1762 / 1772 for /api/mcp, /chat, /api/a2a and /api/llm-bridge, reproducing the ceilings this baseline already carried EXACTLY (1855 on this route), and this branch's head measures 1755 / 1856 / 1763 / 1773 (1856 here). So the split is unambiguous: +1 was already absorbed for the forward merge, whose record this raise REPLACES rather than deletes — main's own move since the merge base is +5 on this route (the overlay-collision bound leaving the design registry's `utils` item, cinatra#3105, and the episode display joining the generated artifact-renderer map, cinatra#3093), already inside `from`, and the branch's own leaf on that +1 is `packages/agents/src/end-node-output-pickup.ts` — and the remaining +1 is this leg's, measured as the set difference against that control and stated next.\n\nWHAT THIS LEG ADDS. The conformance fix makes the run's own record a STEP THAT OPENS, one card per step page, so the rail's shared non-step row `packages/agents/src/run-step-rail-extra-entry.tsx` — already on all four of these routes before this leg — now has to read the frame's selection.\n\nNARROWING WAS AVAILABLE AND WAS TAKEN, so the record is for +1 and not +2. Read from the frame module `run-surface-rail.tsx`, that single import dragged the whole two-column frame and its step module onto every one of these routes: +2 each, and this leg's first head measured 1857 here (1756 / 1857 / 1764 / 1774 across the four). The context and its hook were therefore lifted into their own module, `packages/agents/src/run-surface-rail-selection.tsx`, whose only value import is React — the same split the rail's labels (`run-surface-rail-labels.ts`) and its step predicates (`run-surface-rail-step.ts`) already took, for the same reason. The measured set difference against the control is then EXACTLY ONE path on each of the four routes, that module and nothing else.\n\nIT CANNOT BE FOLDED AWAY. The row and the frame are drawn by different modules on the two branches of this screen — one where the rail is the page's own column, one where it lives inside the stepper panel — so the row cannot be handed the selection as a prop from a parent that does not always have one; the selection has to be a context, and a context has to live in a module. Putting it back in the frame is the +2 this record refuses. `packages/agents/src/__tests__/run-surface-rail-selection-narrowness.test.ts` reds if the row reaches for the frame module again, or if any value import appears in the selection module and puts the cost back.\n\n/sign-in measures 161 in every tree and its ceiling is kept at main's 223 with main's own record carried forward unaltered.",
      "pr": 3074
    },
    "/sign-in": {
      "from": 222,
      "to": 223,
      "reason": "cinatra#2988 (lifecycle-b W5a, cinatra#2932) carries this record — this merge brings origin/main forward into the W5a branch, so `from` (222) is main's OWN committed ceiling at 1fb86826b078b7031c422cfab7c60c0182d9b8f3. Main adds NOTHING to this route: its trigger-claim leaf (PR #2987, cinatra#2981) and its two recommendation-hold cores (PR #2890, cinatra#2790 S9f host parity, which also carries the cinatra#2997 placeholder review slot) are not reachable from the sign-in entry, and main measures 222 here unchanged. The whole +1 is this branch's own: ONE leaf of the W5a slice reachable from the auth plugins — the six leaves the slice adds (the bound-reference resolver, the bound-turn actor, the bound-card binding, the grant codec, its single-use store and the store's DDL leaf) are reached from `runAssistantTurn`, which mints the grant at send time, and only one of them lands on this route's graph. MEASURED THE WAY CI MEASURES, not computed: with the companion extension fleet cloned PINNED (112/112 at their lock SHAs, missingCount 0) and an identical scripts/route-graph.mjs, THREE trees were analysed in ONE environment — origin/main at 1fb86826b078b7031c422cfab7c60c0182d9b8f3 measures 222, this branch's pre-merge head at f84dc8a9758e9d2647e9d47acdc3e463346a23a1 reproduces this branch's own committed ceiling EXACTLY (223), and the merge measures 223: the branch's ceiling unchanged, so nothing of main's composed onto it. The carried-forward PR #2928 record for this route is RETIRED BY THIS RAISE, which is the one lifecycle the gate allows for an annotation whose ceiling moves.",
      "pr": 2988
    }
  }
}
