{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 163,
    "/api/mcp": 1803,
    "/chat": 1904,
    "/api/a2a": 1811,
    "/api/llm-bridge": 1821
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1764,
      "to": 1811,
      "reason": "cinatra#3091 (lifecycle-d W3) forwards main into the branch and completes the packaging road for the wave-5 display packs. Two MEASURED steps sit between main's ceiling and this one, both taken with the companion extension fleet cloned PINNED to this branch's lock (115/115, missing 0) and an identical scripts/route-graph.mjs, exactly as CI measures. First, the merge of main into the branch measures /api/mcp 1778, /chat 1879, /api/a2a 1786, /api/llm-bridge 1796 - the branch's own wave-5 re-pin of the display extensions composed with main's per-scope assignment-store spine, with the generated display map still stale because the extension-manifest generator could not run on that tree. Second, the generator's packaging rule is satisfied: the display packs whose re-pinned heads publish their own renderer exports (blog-idea-artifact detail+preview, blog-image-artifact detail+list-row+preview, blog-post-artifact detail+preview, linkedin-artifacts detail+preview) get the guarded-optional resolution road they were missing, and email-artifacts' already-aliased list-row enters the regenerated map. That adds the same +25 first-party modules to each of these four routes: /api/mcp 1803, /chat 1904, /api/a2a 1811, /api/llm-bridge 1821. /sign-in reaches none of it and measures 163, below main's 223, so its ceiling is LOWERED and its carried-forward record is retired by that lowering. Narrowing is unavailable: the generated display map is a literal-import dispatch map - every entry must name its own module for the build to contain it - and a pack that publishes a renderer with no resolution road fails the generator closed, which is the state this leg repairs.",
      "pr": 3143
    },
    "/api/llm-bridge": {
      "from": 1774,
      "to": 1821,
      "reason": "cinatra#3091 (lifecycle-d W3) forwards main into the branch and completes the packaging road for the wave-5 display packs. Two MEASURED steps sit between main's ceiling and this one, both taken with the companion extension fleet cloned PINNED to this branch's lock (115/115, missing 0) and an identical scripts/route-graph.mjs, exactly as CI measures. First, the merge of main into the branch measures /api/mcp 1778, /chat 1879, /api/a2a 1786, /api/llm-bridge 1796 - the branch's own wave-5 re-pin of the display extensions composed with main's per-scope assignment-store spine, with the generated display map still stale because the extension-manifest generator could not run on that tree. Second, the generator's packaging rule is satisfied: the display packs whose re-pinned heads publish their own renderer exports (blog-idea-artifact detail+preview, blog-image-artifact detail+list-row+preview, blog-post-artifact detail+preview, linkedin-artifacts detail+preview) get the guarded-optional resolution road they were missing, and email-artifacts' already-aliased list-row enters the regenerated map. That adds the same +25 first-party modules to each of these four routes: /api/mcp 1803, /chat 1904, /api/a2a 1811, /api/llm-bridge 1821. /sign-in reaches none of it and measures 163, below main's 223, so its ceiling is LOWERED and its carried-forward record is retired by that lowering. Narrowing is unavailable: the generated display map is a literal-import dispatch map - every entry must name its own module for the build to contain it - and a pack that publishes a renderer with no resolution road fails the generator closed, which is the state this leg repairs.",
      "pr": 3143
    },
    "/api/mcp": {
      "from": 1756,
      "to": 1803,
      "reason": "cinatra#3091 (lifecycle-d W3) forwards main into the branch and completes the packaging road for the wave-5 display packs. Two MEASURED steps sit between main's ceiling and this one, both taken with the companion extension fleet cloned PINNED to this branch's lock (115/115, missing 0) and an identical scripts/route-graph.mjs, exactly as CI measures. First, the merge of main into the branch measures /api/mcp 1778, /chat 1879, /api/a2a 1786, /api/llm-bridge 1796 - the branch's own wave-5 re-pin of the display extensions composed with main's per-scope assignment-store spine, with the generated display map still stale because the extension-manifest generator could not run on that tree. Second, the generator's packaging rule is satisfied: the display packs whose re-pinned heads publish their own renderer exports (blog-idea-artifact detail+preview, blog-image-artifact detail+list-row+preview, blog-post-artifact detail+preview, linkedin-artifacts detail+preview) get the guarded-optional resolution road they were missing, and email-artifacts' already-aliased list-row enters the regenerated map. That adds the same +25 first-party modules to each of these four routes: /api/mcp 1803, /chat 1904, /api/a2a 1811, /api/llm-bridge 1821. /sign-in reaches none of it and measures 163, below main's 223, so its ceiling is LOWERED and its carried-forward record is retired by that lowering. Narrowing is unavailable: the generated display map is a literal-import dispatch map - every entry must name its own module for the build to contain it - and a pack that publishes a renderer with no resolution road fails the generator closed, which is the state this leg repairs.",
      "pr": 3143
    },
    "/chat": {
      "from": 1857,
      "to": 1904,
      "reason": "cinatra#3091 (lifecycle-d W3) forwards main into the branch and completes the packaging road for the wave-5 display packs. Two MEASURED steps sit between main's ceiling and this one, both taken with the companion extension fleet cloned PINNED to this branch's lock (115/115, missing 0) and an identical scripts/route-graph.mjs, exactly as CI measures. First, the merge of main into the branch measures /api/mcp 1778, /chat 1879, /api/a2a 1786, /api/llm-bridge 1796 - the branch's own wave-5 re-pin of the display extensions composed with main's per-scope assignment-store spine, with the generated display map still stale because the extension-manifest generator could not run on that tree. Second, the generator's packaging rule is satisfied: the display packs whose re-pinned heads publish their own renderer exports (blog-idea-artifact detail+preview, blog-image-artifact detail+list-row+preview, blog-post-artifact detail+preview, linkedin-artifacts detail+preview) get the guarded-optional resolution road they were missing, and email-artifacts' already-aliased list-row enters the regenerated map. That adds the same +25 first-party modules to each of these four routes: /api/mcp 1803, /chat 1904, /api/a2a 1811, /api/llm-bridge 1821. /sign-in reaches none of it and measures 163, below main's 223, so its ceiling is LOWERED and its carried-forward record is retired by that lowering. Narrowing is unavailable: the generated display map is a literal-import dispatch map - every entry must name its own module for the build to contain it - and a pack that publishes a renderer with no resolution road fails the generator closed, which is the state this leg repairs.",
      "pr": 3143
    }
  }
}
