{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1590,
    "/api/mcp": 1586,
    "/chat": 1660,
    "/api/a2a": 1591,
    "/api/llm-bridge": 1601
  },
  "absorbs": {
    "/sign-in": {
      "from": 1584,
      "to": 1590,
      "reason": "#2044 L-A (epic #2037 S6): dev-enrolling the CMS-snapshot renderer @cinatra-ai/cms-snapshot-artifact makes its renderer graph statically reachable from every locked route via the generated artifact-renderer map (+6 per route). The +6 is exactly FIVE modules of the pack itself (its detail/preview slot renderers and the local view/model/props modules they import) PLUS ONE shared host module, src/lib/extension-load-guard, which src/lib/generated/artifact-renderers.ts now imports because these are the FIRST resolution:\"guardedOptional\" entries in GENERATED_ARTIFACT_RENDERERS (every prior entry was \"required\" and used a bare () => import()). Neither half can be narrowed: the pack is already minimal — its slot renderers import only their own local view/model/props/text-hook modules, never the package barrel — and the guardedExtensionImport edge is generator-owned and is precisely what makes the entry provably degradable (the required-extensions cover gate classifies it acquirable-on-demand instead of demanding a cinatra.extensions + required-lock entry). The renderer itself stays lazily loaded at runtime; this is static reachability, not boot cost. Retires the #2084 records for these routes (their raise is baked into the base ceiling).",
      "pr": 2101
    },
    "/api/mcp": {
      "from": 1580,
      "to": 1586,
      "reason": "#2044 L-A (epic #2037 S6): dev-enrolling the CMS-snapshot renderer @cinatra-ai/cms-snapshot-artifact makes its renderer graph statically reachable from every locked route via the generated artifact-renderer map (+6 per route). The +6 is exactly FIVE modules of the pack itself (its detail/preview slot renderers and the local view/model/props modules they import) PLUS ONE shared host module, src/lib/extension-load-guard, which src/lib/generated/artifact-renderers.ts now imports because these are the FIRST resolution:\"guardedOptional\" entries in GENERATED_ARTIFACT_RENDERERS (every prior entry was \"required\" and used a bare () => import()). Neither half can be narrowed: the pack is already minimal — its slot renderers import only their own local view/model/props/text-hook modules, never the package barrel — and the guardedExtensionImport edge is generator-owned and is precisely what makes the entry provably degradable (the required-extensions cover gate classifies it acquirable-on-demand instead of demanding a cinatra.extensions + required-lock entry). The renderer itself stays lazily loaded at runtime; this is static reachability, not boot cost. Retires the #2084 records for these routes (their raise is baked into the base ceiling).",
      "pr": 2101
    },
    "/chat": {
      "from": 1654,
      "to": 1660,
      "reason": "#2044 L-A (epic #2037 S6): dev-enrolling the CMS-snapshot renderer @cinatra-ai/cms-snapshot-artifact makes its renderer graph statically reachable from every locked route via the generated artifact-renderer map (+6 per route). The +6 is exactly FIVE modules of the pack itself (its detail/preview slot renderers and the local view/model/props modules they import) PLUS ONE shared host module, src/lib/extension-load-guard, which src/lib/generated/artifact-renderers.ts now imports because these are the FIRST resolution:\"guardedOptional\" entries in GENERATED_ARTIFACT_RENDERERS (every prior entry was \"required\" and used a bare () => import()). Neither half can be narrowed: the pack is already minimal — its slot renderers import only their own local view/model/props/text-hook modules, never the package barrel — and the guardedExtensionImport edge is generator-owned and is precisely what makes the entry provably degradable (the required-extensions cover gate classifies it acquirable-on-demand instead of demanding a cinatra.extensions + required-lock entry). The renderer itself stays lazily loaded at runtime; this is static reachability, not boot cost. Retires the #2084 records for these routes (their raise is baked into the base ceiling).",
      "pr": 2101
    },
    "/api/a2a": {
      "from": 1585,
      "to": 1591,
      "reason": "#2044 L-A (epic #2037 S6): dev-enrolling the CMS-snapshot renderer @cinatra-ai/cms-snapshot-artifact makes its renderer graph statically reachable from every locked route via the generated artifact-renderer map (+6 per route). The +6 is exactly FIVE modules of the pack itself (its detail/preview slot renderers and the local view/model/props modules they import) PLUS ONE shared host module, src/lib/extension-load-guard, which src/lib/generated/artifact-renderers.ts now imports because these are the FIRST resolution:\"guardedOptional\" entries in GENERATED_ARTIFACT_RENDERERS (every prior entry was \"required\" and used a bare () => import()). Neither half can be narrowed: the pack is already minimal — its slot renderers import only their own local view/model/props/text-hook modules, never the package barrel — and the guardedExtensionImport edge is generator-owned and is precisely what makes the entry provably degradable (the required-extensions cover gate classifies it acquirable-on-demand instead of demanding a cinatra.extensions + required-lock entry). The renderer itself stays lazily loaded at runtime; this is static reachability, not boot cost. Retires the #2084 records for these routes (their raise is baked into the base ceiling).",
      "pr": 2101
    },
    "/api/llm-bridge": {
      "from": 1595,
      "to": 1601,
      "reason": "#2044 L-A (epic #2037 S6): dev-enrolling the CMS-snapshot renderer @cinatra-ai/cms-snapshot-artifact makes its renderer graph statically reachable from every locked route via the generated artifact-renderer map (+6 per route). The +6 is exactly FIVE modules of the pack itself (its detail/preview slot renderers and the local view/model/props modules they import) PLUS ONE shared host module, src/lib/extension-load-guard, which src/lib/generated/artifact-renderers.ts now imports because these are the FIRST resolution:\"guardedOptional\" entries in GENERATED_ARTIFACT_RENDERERS (every prior entry was \"required\" and used a bare () => import()). Neither half can be narrowed: the pack is already minimal — its slot renderers import only their own local view/model/props/text-hook modules, never the package barrel — and the guardedExtensionImport edge is generator-owned and is precisely what makes the entry provably degradable (the required-extensions cover gate classifies it acquirable-on-demand instead of demanding a cinatra.extensions + required-lock entry). The renderer itself stays lazily loaded at runtime; this is static reachability, not boot cost. Retires the #2084 records for these routes (their raise is baked into the base ceiling).",
      "pr": 2101
    }
  }
}
