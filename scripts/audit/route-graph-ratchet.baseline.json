{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 160,
    "/api/mcp": 1728,
    "/chat": 1824,
    "/api/a2a": 1733,
    "/api/llm-bridge": 1743
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1728,
      "to": 1733,
      "reason": "cinatra#2933 (lifecycle-b W5b) carries this record. `from` (1728) is main's OWN committed ceiling at 4e23061a1f113414116b6e1e25c8182ec77f55eb, so main's own raises are NOT re-absorbed here: the five delegated-chat admission modules main absorbed under PR #2914 (cinatra#2817) are already INSIDE it, as are the W5a slice from PR #2988 and the run-outbox slice from PR #2991 that preceded it. WHAT THIS BRANCH ADDS ON TOP OF MAIN IS FOUR FIRST-PARTY MODULES AND NOTHING ELSE, and they are the slice rather than an accident of it: the plan requires the prompt window's exchange to be STORED WITH THE RUN and answered by the run's own assistant turn, which is one store, one server bridge, one client controller and one turn road — `run-window-conversation-store.ts`, `run-window-actions.ts` and `use-run-window-conversation.ts` in @cinatra-ai/agents (+3) and `src/lib/lifecycle/run-window-turn.ts` in the app (+1). That set is not asserted, it is the complete diff: `git diff --diff-filter=A origin/main...HEAD` over first-party .ts/.tsx outside __tests__ lists exactly those four files and no others, and this branch deletes no first-party module vs main. No barrel import is widened and no new cross-package edge is introduced — all four sit inside packages these routes already reach. MEASURED THE WAY CI MEASURES, not computed: with the companion extension fleet cloned PINNED (112/112 at their lock SHAs) and an identical scripts/route-graph.mjs, the merged tree measures 1732 on this route, which is main's committed ceiling +4, and the same +4 and nothing else on each of the four MCP-reachable routes. The two slices' module sets are disjoint — main's are its own leaves, named above; this branch's are the four window modules — so the raises add rather than overlap. /sign-in takes no raise: it reaches none of the four, it measures 160 on this tree, and its ceiling is LOWERED to that measured value rather than carried at 223, which retires main's own record for it in the direction the ratchet exists to move. main's records for these four routes are retired by this re-raise, which is the ordinary lifecycle for an annotation whose ceiling moves. RE-MEASURED AT THE FIFTH MODULE (cinatra#3016): the window's per-run FRAME — `src/lib/lifecycle/run-window-frame.ts` — joins the four above, so what this branch adds on top of main is FIVE first-party modules and nothing else, and `git diff --diff-filter=A origin/main...HEAD` over first-party .ts/.tsx outside __tests__ lists exactly those five. The frame reads the run's paused gate and the run's trigger through modules these four routes ALREADY reach — the measurement says so: the ceiling moves by exactly +1 on each of them, which is the frame module itself and no reader behind it. The `./hitl-context` subpath this branch adds to @cinatra-ai/agents opens no new edge either; it names a module already inside each of these graphs, reached through the package barrel today. Measured the same way as the figures above, with the companion extension fleet cloned PINNED (112/112) and ROUTE_GRAPH_RATCHET_BASE=origin/main.",
      "pr": 2995
    },
    "/api/llm-bridge": {
      "from": 1738,
      "to": 1743,
      "reason": "cinatra#2933 (lifecycle-b W5b) carries this record. `from` (1738) is main's OWN committed ceiling at 4e23061a1f113414116b6e1e25c8182ec77f55eb, so main's own raises are NOT re-absorbed here: the five delegated-chat admission modules main absorbed under PR #2914 (cinatra#2817) are already INSIDE it, as are the W5a slice from PR #2988 and the run-outbox slice from PR #2991 that preceded it. WHAT THIS BRANCH ADDS ON TOP OF MAIN IS FOUR FIRST-PARTY MODULES AND NOTHING ELSE, and they are the slice rather than an accident of it: the plan requires the prompt window's exchange to be STORED WITH THE RUN and answered by the run's own assistant turn, which is one store, one server bridge, one client controller and one turn road — `run-window-conversation-store.ts`, `run-window-actions.ts` and `use-run-window-conversation.ts` in @cinatra-ai/agents (+3) and `src/lib/lifecycle/run-window-turn.ts` in the app (+1). That set is not asserted, it is the complete diff: `git diff --diff-filter=A origin/main...HEAD` over first-party .ts/.tsx outside __tests__ lists exactly those four files and no others, and this branch deletes no first-party module vs main. No barrel import is widened and no new cross-package edge is introduced — all four sit inside packages these routes already reach. MEASURED THE WAY CI MEASURES, not computed: with the companion extension fleet cloned PINNED (112/112 at their lock SHAs) and an identical scripts/route-graph.mjs, the merged tree measures 1742 on this route, which is main's committed ceiling +4, and the same +4 and nothing else on each of the four MCP-reachable routes. The two slices' module sets are disjoint — main's are its own leaves, named above; this branch's are the four window modules — so the raises add rather than overlap. /sign-in takes no raise: it reaches none of the four, it measures 160 on this tree, and its ceiling is LOWERED to that measured value rather than carried at 223, which retires main's own record for it in the direction the ratchet exists to move. main's records for these four routes are retired by this re-raise, which is the ordinary lifecycle for an annotation whose ceiling moves. RE-MEASURED AT THE FIFTH MODULE (cinatra#3016): the window's per-run FRAME — `src/lib/lifecycle/run-window-frame.ts` — joins the four above, so what this branch adds on top of main is FIVE first-party modules and nothing else, and `git diff --diff-filter=A origin/main...HEAD` over first-party .ts/.tsx outside __tests__ lists exactly those five. The frame reads the run's paused gate and the run's trigger through modules these four routes ALREADY reach — the measurement says so: the ceiling moves by exactly +1 on each of them, which is the frame module itself and no reader behind it. The `./hitl-context` subpath this branch adds to @cinatra-ai/agents opens no new edge either; it names a module already inside each of these graphs, reached through the package barrel today. Measured the same way as the figures above, with the companion extension fleet cloned PINNED (112/112) and ROUTE_GRAPH_RATCHET_BASE=origin/main.",
      "pr": 2995
    },
    "/api/mcp": {
      "from": 1723,
      "to": 1728,
      "reason": "cinatra#2933 (lifecycle-b W5b) carries this record. `from` (1723) is main's OWN committed ceiling at 4e23061a1f113414116b6e1e25c8182ec77f55eb, so main's own raises are NOT re-absorbed here: the five delegated-chat admission modules main absorbed under PR #2914 (cinatra#2817) are already INSIDE it, as are the W5a slice from PR #2988 and the run-outbox slice from PR #2991 that preceded it. WHAT THIS BRANCH ADDS ON TOP OF MAIN IS FOUR FIRST-PARTY MODULES AND NOTHING ELSE, and they are the slice rather than an accident of it: the plan requires the prompt window's exchange to be STORED WITH THE RUN and answered by the run's own assistant turn, which is one store, one server bridge, one client controller and one turn road — `run-window-conversation-store.ts`, `run-window-actions.ts` and `use-run-window-conversation.ts` in @cinatra-ai/agents (+3) and `src/lib/lifecycle/run-window-turn.ts` in the app (+1). That set is not asserted, it is the complete diff: `git diff --diff-filter=A origin/main...HEAD` over first-party .ts/.tsx outside __tests__ lists exactly those four files and no others, and this branch deletes no first-party module vs main. No barrel import is widened and no new cross-package edge is introduced — all four sit inside packages these routes already reach. MEASURED THE WAY CI MEASURES, not computed: with the companion extension fleet cloned PINNED (112/112 at their lock SHAs) and an identical scripts/route-graph.mjs, the merged tree measures 1727 on this route, which is main's committed ceiling +4, and the same +4 and nothing else on each of the four MCP-reachable routes. The two slices' module sets are disjoint — main's are its own leaves, named above; this branch's are the four window modules — so the raises add rather than overlap. /sign-in takes no raise: it reaches none of the four, it measures 160 on this tree, and its ceiling is LOWERED to that measured value rather than carried at 223, which retires main's own record for it in the direction the ratchet exists to move. main's records for these four routes are retired by this re-raise, which is the ordinary lifecycle for an annotation whose ceiling moves. RE-MEASURED AT THE FIFTH MODULE (cinatra#3016): the window's per-run FRAME — `src/lib/lifecycle/run-window-frame.ts` — joins the four above, so what this branch adds on top of main is FIVE first-party modules and nothing else, and `git diff --diff-filter=A origin/main...HEAD` over first-party .ts/.tsx outside __tests__ lists exactly those five. The frame reads the run's paused gate and the run's trigger through modules these four routes ALREADY reach — the measurement says so: the ceiling moves by exactly +1 on each of them, which is the frame module itself and no reader behind it. The `./hitl-context` subpath this branch adds to @cinatra-ai/agents opens no new edge either; it names a module already inside each of these graphs, reached through the package barrel today. Measured the same way as the figures above, with the companion extension fleet cloned PINNED (112/112) and ROUTE_GRAPH_RATCHET_BASE=origin/main.",
      "pr": 2995
    },
    "/chat": {
      "from": 1819,
      "to": 1824,
      "reason": "cinatra#2933 (lifecycle-b W5b) carries this record. `from` (1819) is main's OWN committed ceiling at 4e23061a1f113414116b6e1e25c8182ec77f55eb, so main's own raises are NOT re-absorbed here: the five delegated-chat admission modules main absorbed under PR #2914 (cinatra#2817) are already INSIDE it, as are the W5a slice from PR #2988 and the run-outbox slice from PR #2991 that preceded it. WHAT THIS BRANCH ADDS ON TOP OF MAIN IS FOUR FIRST-PARTY MODULES AND NOTHING ELSE, and they are the slice rather than an accident of it: the plan requires the prompt window's exchange to be STORED WITH THE RUN and answered by the run's own assistant turn, which is one store, one server bridge, one client controller and one turn road — `run-window-conversation-store.ts`, `run-window-actions.ts` and `use-run-window-conversation.ts` in @cinatra-ai/agents (+3) and `src/lib/lifecycle/run-window-turn.ts` in the app (+1). That set is not asserted, it is the complete diff: `git diff --diff-filter=A origin/main...HEAD` over first-party .ts/.tsx outside __tests__ lists exactly those four files and no others, and this branch deletes no first-party module vs main. No barrel import is widened and no new cross-package edge is introduced — all four sit inside packages these routes already reach. MEASURED THE WAY CI MEASURES, not computed: with the companion extension fleet cloned PINNED (112/112 at their lock SHAs) and an identical scripts/route-graph.mjs, the merged tree measures 1823 on this route, which is main's committed ceiling +4, and the same +4 and nothing else on each of the four MCP-reachable routes. The two slices' module sets are disjoint — main's are its own leaves, named above; this branch's are the four window modules — so the raises add rather than overlap. /sign-in takes no raise: it reaches none of the four, it measures 160 on this tree, and its ceiling is LOWERED to that measured value rather than carried at 223, which retires main's own record for it in the direction the ratchet exists to move. main's records for these four routes are retired by this re-raise, which is the ordinary lifecycle for an annotation whose ceiling moves. RE-MEASURED AT THE FIFTH MODULE (cinatra#3016): the window's per-run FRAME — `src/lib/lifecycle/run-window-frame.ts` — joins the four above, so what this branch adds on top of main is FIVE first-party modules and nothing else, and `git diff --diff-filter=A origin/main...HEAD` over first-party .ts/.tsx outside __tests__ lists exactly those five. The frame reads the run's paused gate and the run's trigger through modules these four routes ALREADY reach — the measurement says so: the ceiling moves by exactly +1 on each of them, which is the frame module itself and no reader behind it. The `./hitl-context` subpath this branch adds to @cinatra-ai/agents opens no new edge either; it names a module already inside each of these graphs, reached through the package barrel today. Measured the same way as the figures above, with the companion extension fleet cloned PINNED (112/112) and ROUTE_GRAPH_RATCHET_BASE=origin/main.",
      "pr": 2995
    }
  }
}
