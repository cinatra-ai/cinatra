{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 150,
    "/api/mcp": 1472,
    "/chat": 1532,
    "/api/a2a": 1477,
    "/api/llm-bridge": 1483
  },
  "absorbs": {
    "/chat": {
      "from": 1528,
      "to": 1532,
      "reason": "cinatra#1437 (PR #1673) artifact row-scope promotion REQUEST surface: src/lib/artifacts/mcp.ts — already first-party-reachable on this route (it registers the artifacts MCP primitives) — gains the artifact_promote_request tool, statically importing the net-new promotion subgraph: src/lib/artifacts/artifact-promotion-request.ts (the shared actor-gated request service), src/lib/objects/artifact-row-promotion.ts (the decide/create data layer) and src/lib/objects/artifact-promotion-request-store.ts (the raw-SQL request store) plus exactly one transitively-new leaf of that subgraph on this route. STATIC imports are REQUIRED here: the entry-66 ruling (PR #1748, absorbed into this branch) A/B-proved that a server-side dynamic import over this exact subgraph detonates Turbopack's linux-x64 native compile (unbounded >33G runaway), so the lazy-import narrowing the ratchet would otherwise suggest is the one shape this subgraph must not take. MEASURED as a single net-new +4 promotion subgraph on this route (CI merge-ref run 29595216607, missingCount 0, on the pre-reconcile branch); reconciled onto origin/main so the delta re-stacks on main's exec-plane S2 base (PR #1755): /chat 1528 -> 1532 (NET +4), retiring the PR #1755 record for this route. CI's route-graph-ratchet re-measures authoritatively on the merged ref (three-tree differential: base fbfe6bcb + PR #1673's +4 promotion subgraph + PR #1755's +1 skill-read leaf).",
      "pr": 1673
    },
    "/api/a2a": {
      "from": 1473,
      "to": 1477,
      "reason": "cinatra#1437 (PR #1673) artifact row-scope promotion REQUEST surface: src/lib/artifacts/mcp.ts — already first-party-reachable on this route (it registers the artifacts MCP primitives) — gains the artifact_promote_request tool, statically importing the net-new promotion subgraph: src/lib/artifacts/artifact-promotion-request.ts (the shared actor-gated request service), src/lib/objects/artifact-row-promotion.ts (the decide/create data layer) and src/lib/objects/artifact-promotion-request-store.ts (the raw-SQL request store) plus exactly one transitively-new leaf of that subgraph on this route. STATIC imports are REQUIRED here: the entry-66 ruling (PR #1748, absorbed into this branch) A/B-proved that a server-side dynamic import over this exact subgraph detonates Turbopack's linux-x64 native compile (unbounded >33G runaway), so the lazy-import narrowing the ratchet would otherwise suggest is the one shape this subgraph must not take. MEASURED as a single net-new +4 promotion subgraph on this route (CI merge-ref run 29595216607, missingCount 0, on the pre-reconcile branch); reconciled onto origin/main so the delta re-stacks on main's exec-plane S2 base (PR #1755): /api/a2a 1473 -> 1477 (NET +4), retiring the PR #1755 record for this route. CI's route-graph-ratchet re-measures authoritatively on the merged ref (three-tree differential: base fbfe6bcb + PR #1673's +4 promotion subgraph + PR #1755's +1 skill-read leaf).",
      "pr": 1673
    },
    "/api/llm-bridge": {
      "from": 1479,
      "to": 1483,
      "reason": "cinatra#1437 (PR #1673) artifact row-scope promotion REQUEST surface: src/lib/artifacts/mcp.ts — already first-party-reachable on this route (it registers the artifacts MCP primitives) — gains the artifact_promote_request tool, statically importing the net-new promotion subgraph: src/lib/artifacts/artifact-promotion-request.ts (the shared actor-gated request service), src/lib/objects/artifact-row-promotion.ts (the decide/create data layer) and src/lib/objects/artifact-promotion-request-store.ts (the raw-SQL request store) plus exactly one transitively-new leaf of that subgraph on this route. STATIC imports are REQUIRED here: the entry-66 ruling (PR #1748, absorbed into this branch) A/B-proved that a server-side dynamic import over this exact subgraph detonates Turbopack's linux-x64 native compile (unbounded >33G runaway), so the lazy-import narrowing the ratchet would otherwise suggest is the one shape this subgraph must not take. MEASURED as a single net-new +4 promotion subgraph on this route (CI merge-ref run 29595216607, missingCount 0, on the pre-reconcile branch); reconciled onto origin/main so the delta re-stacks on main's exec-plane S2 base (PR #1755): /api/llm-bridge 1479 -> 1483 (NET +4), retiring the PR #1755 record for this route. CI's route-graph-ratchet re-measures authoritatively on the merged ref (three-tree differential: base fbfe6bcb + PR #1673's +4 promotion subgraph + PR #1755's +1 skill-read leaf).",
      "pr": 1673
    },
    "/api/mcp": {
      "from": 1468,
      "to": 1472,
      "reason": "cinatra#1437 (PR #1673) artifact row-scope promotion REQUEST surface: src/lib/artifacts/mcp.ts — already first-party-reachable on this route (it registers the artifacts MCP primitives) — gains the artifact_promote_request tool, statically importing the net-new promotion subgraph: src/lib/artifacts/artifact-promotion-request.ts (the shared actor-gated request service), src/lib/objects/artifact-row-promotion.ts (the decide/create data layer) and src/lib/objects/artifact-promotion-request-store.ts (the raw-SQL request store) plus exactly one transitively-new leaf of that subgraph on this route. STATIC imports are REQUIRED here: the entry-66 ruling (PR #1748, absorbed into this branch) A/B-proved that a server-side dynamic import over this exact subgraph detonates Turbopack's linux-x64 native compile (unbounded >33G runaway), so the lazy-import narrowing the ratchet would otherwise suggest is the one shape this subgraph must not take. MEASURED as a single net-new +4 promotion subgraph on this route (CI merge-ref run 29595216607, missingCount 0, on the pre-reconcile branch); reconciled onto origin/main so the delta re-stacks on main's exec-plane S2 base (PR #1755): /api/mcp 1468 -> 1472 (NET +4), retiring the PR #1755 record for this route. CI's route-graph-ratchet re-measures authoritatively on the merged ref (three-tree differential: base fbfe6bcb + PR #1673's +4 promotion subgraph + PR #1755's +1 skill-read leaf).",
      "pr": 1673
    },
    "/sign-in": {
      "from": 149,
      "to": 150,
      "reason": "cinatra#1565 (PR #1579) org member-invitation email: src/lib/auth.ts (reachable from every tracked route) gains the new leaf module src/lib/org-invitation-email.ts — the pure accept-link/subject/body builder wired into the organization.sendInvitationEmail callback (Better Auth ships NO default invitation sender, so inviteMember created an invitation row but sent nothing). MEASURED via an A/B differential using scripts/route-graph.mjs analyzeRoute: with the module present vs moved aside, EVERY tracked route drops by exactly 1, so the raise is a single net-new fully-resolvable leaf, invariant to the missing companion-extension modules. /sign-in measured 150 locally at missingCount 0; the four companion routes measured the identical -1 delta (their absolute count is CI-measured with the extensions cloned pinned, this base+1). Absorbed raise 149->150 (NET +1). RE-BASED 2026-07-15 after main-side baseline rewrite (objects/memory merges); from/to re-derived on the new base, same measured NET +1 — CI re-verifies on the merged tree.",
      "pr": 1579
    }
  }
}
