{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions \u2014 a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1678,
    "/api/mcp": 1674,
    "/chat": 1754,
    "/api/a2a": 1679,
    "/api/llm-bridge": 1689
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1678,
      "to": 1679,
      "reason": "cinatra#2568 (epic #2564 S4) the recommendation hold on the run wire: ONE new first-party module, packages/agent-ui-protocol/src/renderable-views/lifecycle-cards.ts, becomes reachable from these four routes. It is the wire CONTRACT the S1 card registry declared (the interaction kinds and their carriage, the typed-interrupt discriminator, its schema version and ref bound); the run-start recommendation hold's producer lives in packages/agents/src/recommendation-hold.ts, which sits on the pre-dispatch path every locked route already reaches through the @cinatra-ai/agents barrel \u2014 so the module the producer needs comes with it. No new cross-package EDGE is added: the import rides the agent-ui-protocol path these routes already carry, and it is deliberately the DEEP specifier (renderable-views/lifecycle-cards) rather than the renderable-views barrel, which would have pulled the whole zod view-schema chain and cost +9 on each of them. The +1 cannot be narrowed away without duplicating the wire contract inside the producer, which is precisely the drift the registry exists to prevent; the one consumer that could take a weaker check (the AG-UI subscriber's 'is this an ordinary review-task gate' guard) does exactly that and adds nothing. /chat does not grow \u2014 it already carries the module, so its #2611 record is carried forward unchanged. Counts measured with the companion extension repos cloned pinned, exactly as CI measures them.",
      "pr": 2618
    },
    "/api/llm-bridge": {
      "from": 1688,
      "to": 1689,
      "reason": "cinatra#2568 (epic #2564 S4) the recommendation hold on the run wire: ONE new first-party module, packages/agent-ui-protocol/src/renderable-views/lifecycle-cards.ts, becomes reachable from these four routes. It is the wire CONTRACT the S1 card registry declared (the interaction kinds and their carriage, the typed-interrupt discriminator, its schema version and ref bound); the run-start recommendation hold's producer lives in packages/agents/src/recommendation-hold.ts, which sits on the pre-dispatch path every locked route already reaches through the @cinatra-ai/agents barrel \u2014 so the module the producer needs comes with it. No new cross-package EDGE is added: the import rides the agent-ui-protocol path these routes already carry, and it is deliberately the DEEP specifier (renderable-views/lifecycle-cards) rather than the renderable-views barrel, which would have pulled the whole zod view-schema chain and cost +9 on each of them. The +1 cannot be narrowed away without duplicating the wire contract inside the producer, which is precisely the drift the registry exists to prevent; the one consumer that could take a weaker check (the AG-UI subscriber's 'is this an ordinary review-task gate' guard) does exactly that and adds nothing. /chat does not grow \u2014 it already carries the module, so its #2611 record is carried forward unchanged. Counts measured with the companion extension repos cloned pinned, exactly as CI measures them.",
      "pr": 2618
    },
    "/api/mcp": {
      "from": 1673,
      "to": 1674,
      "reason": "cinatra#2568 (epic #2564 S4) the recommendation hold on the run wire: ONE new first-party module, packages/agent-ui-protocol/src/renderable-views/lifecycle-cards.ts, becomes reachable from these four routes. It is the wire CONTRACT the S1 card registry declared (the interaction kinds and their carriage, the typed-interrupt discriminator, its schema version and ref bound); the run-start recommendation hold's producer lives in packages/agents/src/recommendation-hold.ts, which sits on the pre-dispatch path every locked route already reaches through the @cinatra-ai/agents barrel \u2014 so the module the producer needs comes with it. No new cross-package EDGE is added: the import rides the agent-ui-protocol path these routes already carry, and it is deliberately the DEEP specifier (renderable-views/lifecycle-cards) rather than the renderable-views barrel, which would have pulled the whole zod view-schema chain and cost +9 on each of them. The +1 cannot be narrowed away without duplicating the wire contract inside the producer, which is precisely the drift the registry exists to prevent; the one consumer that could take a weaker check (the AG-UI subscriber's 'is this an ordinary review-task gate' guard) does exactly that and adds nothing. /chat does not grow \u2014 it already carries the module, so its #2611 record is carried forward unchanged. Counts measured with the companion extension repos cloned pinned, exactly as CI measures them.",
      "pr": 2618
    },
    "/chat": {
      "from": 1750,
      "to": 1754,
      "reason": "cinatra#2567 (epic #2564 S3) conversational pull: FOUR new first-party modules become reachable from every locked route, because the app's auth plugins mount the MCP server \u2014 so an MCP capability module lands on all five graphs, not just /api/mcp. They are (1) src/lib/lifecycle/lifecycle-pull-mcp.ts, the three read-only pull primitives themselves; (2) src/lib/lifecycle/lifecycle-card-refetch.ts, S1's authorization ladder (run READ -> gate state -> decision axis) \u2014 reached rather than re-implemented, which is the whole point: a second copy of that ordering is exactly how a gate-existence leak gets introduced; (3) src/lib/assistant-runtime/lifecycle-view-envelope.ts, S1's producer, an import-free leaf and the only way to mint a card ref; (4) packages/agents/src/lifecycle-verification-read-store.ts, a NEW leaf this slice carves out. That fourth module is a NARROWING, not growth: reaching readVerificationRecordForGate through the verification WRITE store pulled its whole lane (the core-analysis lane, the advisory store and their app-side cores \u2014 EIGHT modules) onto all five routes just to answer 'is there a row?'. Splitting the read into its own leaf, with the write store re-exporting it so no caller changes, turns a measured +11 into this +4. No further narrowing is available without duplicating S1's ladder or its producer. Uniform +4 on every locked route, measured as a DELTA against this branch's merge base with the companion extension repos cloned, exactly as CI measures.",
      "pr": 2611
    },
    "/sign-in": {
      "from": 1677,
      "to": 1678,
      "reason": "cinatra#2568 (epic #2564 S4) the recommendation hold on the run wire: ONE new first-party module, packages/agent-ui-protocol/src/renderable-views/lifecycle-cards.ts, becomes reachable from these four routes. It is the wire CONTRACT the S1 card registry declared (the interaction kinds and their carriage, the typed-interrupt discriminator, its schema version and ref bound); the run-start recommendation hold's producer lives in packages/agents/src/recommendation-hold.ts, which sits on the pre-dispatch path every locked route already reaches through the @cinatra-ai/agents barrel \u2014 so the module the producer needs comes with it. No new cross-package EDGE is added: the import rides the agent-ui-protocol path these routes already carry, and it is deliberately the DEEP specifier (renderable-views/lifecycle-cards) rather than the renderable-views barrel, which would have pulled the whole zod view-schema chain and cost +9 on each of them. The +1 cannot be narrowed away without duplicating the wire contract inside the producer, which is precisely the drift the registry exists to prevent; the one consumer that could take a weaker check (the AG-UI subscriber's 'is this an ordinary review-task gate' guard) does exactly that and adds nothing. /chat does not grow \u2014 it already carries the module, so its #2611 record is carried forward unchanged. Counts measured with the companion extension repos cloned pinned, exactly as CI measures them.",
      "pr": 2618
    }
  }
}
