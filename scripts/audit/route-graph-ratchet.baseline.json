{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions \u2014 a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 220,
    "/api/mcp": 1671,
    "/chat": 1771,
    "/api/a2a": 1676,
    "/api/llm-bridge": 1686
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1675,
      "to": 1676,
      "reason": "cinatra#2823 S9j (PR #2871 carries this record): src/lib/assistant-turn-supersede.ts \u2014 the assistant_turns SUPERSEDE (tombstone) statement builder. It is its OWN module because the org-axis write ledger requires one: src/lib/project-inheritance.ts is a BROAD module the raw org-axis table sweep does not prefix-cover (it carries a committed per-file DML-site count instead), so a new org-axis statement there would hide behind that count rather than register. Its own module is what gives it a row in src/lib/org-write/write-registry.ts, a prefix in scripts/audit/org-write-table-sweep.mjs and a writer-set lockstep pin over its export surface. It is reached from src/lib/project-inheritance.ts, which sits under the objects/thread store graph, so it is +1 on every locked route that reaches that store. It imports nothing but server-only: the edge is one pure statement builder, not a barrel. Measured with the companion extension repos cloned pinned (112/112), on the merge of this branch with origin/main, which is the tree CI checks out for a pull_request.",
      "pr": 2871
    },
    "/api/llm-bridge": {
      "from": 1685,
      "to": 1686,
      "reason": "cinatra#2823 S9j (PR #2871 carries this record): src/lib/assistant-turn-supersede.ts \u2014 the assistant_turns SUPERSEDE (tombstone) statement builder. It is its OWN module because the org-axis write ledger requires one: src/lib/project-inheritance.ts is a BROAD module the raw org-axis table sweep does not prefix-cover (it carries a committed per-file DML-site count instead), so a new org-axis statement there would hide behind that count rather than register. Its own module is what gives it a row in src/lib/org-write/write-registry.ts, a prefix in scripts/audit/org-write-table-sweep.mjs and a writer-set lockstep pin over its export surface. It is reached from src/lib/project-inheritance.ts, which sits under the objects/thread store graph, so it is +1 on every locked route that reaches that store. It imports nothing but server-only: the edge is one pure statement builder, not a barrel. Measured with the companion extension repos cloned pinned (112/112), on the merge of this branch with origin/main, which is the tree CI checks out for a pull_request.",
      "pr": 2871
    },
    "/api/mcp": {
      "from": 1670,
      "to": 1671,
      "reason": "cinatra#2823 S9j (PR #2871 carries this record): src/lib/assistant-turn-supersede.ts \u2014 the assistant_turns SUPERSEDE (tombstone) statement builder. It is its OWN module because the org-axis write ledger requires one: src/lib/project-inheritance.ts is a BROAD module the raw org-axis table sweep does not prefix-cover (it carries a committed per-file DML-site count instead), so a new org-axis statement there would hide behind that count rather than register. Its own module is what gives it a row in src/lib/org-write/write-registry.ts, a prefix in scripts/audit/org-write-table-sweep.mjs and a writer-set lockstep pin over its export surface. It is reached from src/lib/project-inheritance.ts, which sits under the objects/thread store graph, so it is +1 on every locked route that reaches that store. It imports nothing but server-only: the edge is one pure statement builder, not a barrel. Measured with the companion extension repos cloned pinned (112/112), on the merge of this branch with origin/main, which is the tree CI checks out for a pull_request.",
      "pr": 2871
    },
    "/sign-in": {
      "from": 219,
      "to": 220,
      "reason": "cinatra#2823 S9j (PR #2871 carries this record): src/lib/assistant-turn-supersede.ts \u2014 the assistant_turns SUPERSEDE (tombstone) statement builder. It is its OWN module because the org-axis write ledger requires one: src/lib/project-inheritance.ts is a BROAD module the raw org-axis table sweep does not prefix-cover (it carries a committed per-file DML-site count instead), so a new org-axis statement there would hide behind that count rather than register. Its own module is what gives it a row in src/lib/org-write/write-registry.ts, a prefix in scripts/audit/org-write-table-sweep.mjs and a writer-set lockstep pin over its export surface. It is reached from src/lib/project-inheritance.ts, which sits under the objects/thread store graph, so it is +1 on every locked route that reaches that store. It imports nothing but server-only: the edge is one pure statement builder, not a barrel. Measured with the companion extension repos cloned pinned (112/112), on the merge of this branch with origin/main, which is the tree CI checks out for a pull_request.",
      "pr": 2871
    },
    "/chat": {
      "from": 1766,
      "to": 1771,
      "reason": "An EXPIRED schedule proposal must stay VISIBLE with an Adjust that re-proposes, instead of resolving to the undrawn state reserved for a reader who may not see the subject. Drawing a reading that did not exist before costs this route FIVE modules, and these are the five, enumerated by name because the count and the list have to agree: (1) packages/chat/src/renderable-views/trigger-schedule-proposal-expired.tsx, the expired reading's own component; and the four the Adjust edge pulls in behind it, (2) packages/agents/src/trigger-schedule-proposal-actions.ts, the server-action module the press handler reaches, (3) packages/agents/src/trigger-schedule-proposal-service.ts and (4) packages/agents/src/trigger-schedule-proposal-store.ts, which that action module imports in turn, and (5) packages/agents/src/trigger-recurrence.ts, which that service imports for VALUES. Four for the action edge because this route already carries the session graph the action sits on, and the action is reached through a DYNAMIC import inside the press handler so the confirm and propose half stays off the initial graph. The kind's view-schema leaf, packages/agent-ui-protocol/src/renderable-views/trigger-schedule-proposal-view.ts, costs this route NOTHING and is deliberately not in the list: it was already reachable from /chat before this change, and after rebasing onto S9c's resolve envelope (PR #2795) the shell no longer imports it at RUNTIME at all \u2014 the envelope is parsed once at the protocol's own seam and the shell reads an already-typed body, so its only remaining protocol import is type-only and erased. An earlier revision of this record named that leaf as one of them, which made the count and the enumeration disagree by one; the enumeration above is measured as the reachable-module set difference between this head and origin/main, with the companion extension tree cloned pinned on BOTH sides (1762 -> 1767), and it is exactly these five and nothing else. THE FIFTH MODULE IS NEW TO THIS RECORD AND NOT TO THIS CHANGE. packages/agents/src/trigger-recurrence.ts was already reachable from /chat while packages/agents/src/trigger-screen-client.tsx carried it, which is why an earlier revision of this record measured the raise at FOUR against the then-current base and committed 1797. PR #2861 narrowed the renderable-view registry and took trigger-screen-client.tsx off /chat; trigger-recurrence.ts left the graph with it, and the proposal service's import of buildCron and describeRecurrence puts it back. That import is a VALUE import the service needs at runtime to build the cron and to describe the recurrence it re-proposes, so it is not type-only and it cannot be erased. The 1762 base is PR #2861's LOWERED ceiling; that narrowing retired every /chat absorb record this route carried, so this record supersedes none. No other locked route moves: the other four ceilings are re-measured at this head and equal origin/main's exactly. The alternatives were rejected: a dedicated API route for one button widens the surface more than the modules it would save; folding Adjust into an actions module already on this graph would undo the split that keeps the confirm transaction and the install outbox off the five locked MCP route graphs; and leaving the card undrawn is the defect itself. The reading is drawn once, in the shell every declared host already renders, so it costs no new host mount. Re-based after merging origin/main (which carries the #2871 absorb that moved /chat 1762 -> 1766): from is main's committed ceiling 1766, to is 1771, measured on THIS merge with the companion extension repos cloned pinned (112/112) via scripts/audit/route-graph-ratchet.mjs --write-baseline; the +5 delta and its five named modules are unchanged.",
      "pr": 2837
    }
  }
}
