{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1649,
    "/api/mcp": 1645,
    "/chat": 1723,
    "/api/a2a": 1650,
    "/api/llm-bridge": 1660
  },
  "absorbs": {
    "/sign-in": {
      "from": 1647,
      "to": 1649,
      "reason": "cinatra#1942 archive V2 (PR 2214): src/lib/auth.ts gains the archived-organization Better Auth hooks, importing two new first-party modules — organization-dispatch-policy.ts (the /organization/list after-filter + the per-endpoint archived-org mutation policy) and organization-archive-guard.ts (the shared app-native archived-org write guard the policy module reuses for its archivedAt reads). auth.ts is statically reachable from all five locked routes, so each route's reachable first-party count grows by exactly +2 with no further fan-out (both modules' remaining imports — better-auth-db, drizzle-orm, database — are already reachable from every locked route). Re-measured after merging main's #2213/#2217 growth (base ceiling moved 1642 -> 1647 for unrelated reasons, already reflected in the base-branch baseline this record diffs against): the local scripts/route-graph.mjs delta between the merged tree and bare main tip is still a uniform +2 per route, confirming V2's two modules do not overlap with anything main independently added. The prior #2213 record for this route retires per the gate's retire-on-re-raise rule now that the ceiling has moved again.",
      "pr": 2214
    },
    "/api/mcp": {
      "from": 1643,
      "to": 1645,
      "reason": "cinatra#1942 archive V2 (PR 2214): src/lib/auth.ts gains the archived-organization Better Auth hooks, importing two new first-party modules — organization-dispatch-policy.ts (the /organization/list after-filter + the per-endpoint archived-org mutation policy) and organization-archive-guard.ts (the shared app-native archived-org write guard the policy module reuses for its archivedAt reads). auth.ts is statically reachable from all five locked routes, so each route's reachable first-party count grows by exactly +2 with no further fan-out (both modules' remaining imports — better-auth-db, drizzle-orm, database — are already reachable from every locked route). Re-measured after merging main's #2213/#2217 growth (base ceiling moved 1638 -> 1643 for unrelated reasons, already reflected in the base-branch baseline this record diffs against): the local scripts/route-graph.mjs delta between the merged tree and bare main tip is still a uniform +2 per route, confirming V2's two modules do not overlap with anything main independently added. The prior #2213 record for this route retires per the gate's retire-on-re-raise rule now that the ceiling has moved again.",
      "pr": 2214
    },
    "/chat": {
      "from": 1721,
      "to": 1723,
      "reason": "cinatra#1942 archive V2 (PR 2214): src/lib/auth.ts gains the archived-organization Better Auth hooks, importing two new first-party modules — organization-dispatch-policy.ts (the /organization/list after-filter + the per-endpoint archived-org mutation policy) and organization-archive-guard.ts (the shared app-native archived-org write guard the policy module reuses for its archivedAt reads). auth.ts is statically reachable from all five locked routes, so each route's reachable first-party count grows by exactly +2 with no further fan-out (both modules' remaining imports — better-auth-db, drizzle-orm, database — are already reachable from every locked route). Re-measured after merging main's #2213/#2217 growth (base ceiling moved 1716 -> 1721 for unrelated reasons, already reflected in the base-branch baseline this record diffs against): the local scripts/route-graph.mjs delta between the merged tree and bare main tip is still a uniform +2 per route, confirming V2's two modules do not overlap with anything main independently added. The prior #2213 record for this route retires per the gate's retire-on-re-raise rule now that the ceiling has moved again.",
      "pr": 2214
    },
    "/api/a2a": {
      "from": 1648,
      "to": 1650,
      "reason": "cinatra#1942 archive V2 (PR 2214): src/lib/auth.ts gains the archived-organization Better Auth hooks, importing two new first-party modules — organization-dispatch-policy.ts (the /organization/list after-filter + the per-endpoint archived-org mutation policy) and organization-archive-guard.ts (the shared app-native archived-org write guard the policy module reuses for its archivedAt reads). auth.ts is statically reachable from all five locked routes, so each route's reachable first-party count grows by exactly +2 with no further fan-out (both modules' remaining imports — better-auth-db, drizzle-orm, database — are already reachable from every locked route). Re-measured after merging main's #2213/#2217 growth (base ceiling moved 1643 -> 1648 for unrelated reasons, already reflected in the base-branch baseline this record diffs against): the local scripts/route-graph.mjs delta between the merged tree and bare main tip is still a uniform +2 per route, confirming V2's two modules do not overlap with anything main independently added. The prior #2213 record for this route retires per the gate's retire-on-re-raise rule now that the ceiling has moved again.",
      "pr": 2214
    },
    "/api/llm-bridge": {
      "from": 1658,
      "to": 1660,
      "reason": "cinatra#1942 archive V2 (PR 2214): src/lib/auth.ts gains the archived-organization Better Auth hooks, importing two new first-party modules — organization-dispatch-policy.ts (the /organization/list after-filter + the per-endpoint archived-org mutation policy) and organization-archive-guard.ts (the shared app-native archived-org write guard the policy module reuses for its archivedAt reads). auth.ts is statically reachable from all five locked routes, so each route's reachable first-party count grows by exactly +2 with no further fan-out (both modules' remaining imports — better-auth-db, drizzle-orm, database — are already reachable from every locked route). Re-measured after merging main's #2213/#2217 growth (base ceiling moved 1653 -> 1658 for unrelated reasons, already reflected in the base-branch baseline this record diffs against): the local scripts/route-graph.mjs delta between the merged tree and bare main tip is still a uniform +2 per route, confirming V2's two modules do not overlap with anything main independently added. The prior #2213 record for this route retires per the gate's retire-on-re-raise rule now that the ceiling has moved again.",
      "pr": 2214
    }
  }
}
