{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 220,
    "/api/mcp": 1673,
    "/chat": 1768,
    "/api/a2a": 1678,
    "/api/llm-bridge": 1688
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1676,
      "to": 1678,
      "pr": 2890,
      "reason": "cinatra#2790 S9f (PR #2890 carries this record): the recommendation-hold slice adds TWO first-party modules to the agents workspace package — packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision). Both are shared between the run page's server-action path (run-recommendation-actions.ts, run-actions.ts, server-actions.ts) and the widget's API branch (src/app/api/lifecycle-views/recommendation-hold/*, src/lib/lifecycle/recommendation-hold-widget-branch.ts), which is why they are standalone modules rather than bodies inside the server-action files the API branch cannot import. They sit on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on the four assistant-runtime routes, /sign-in unchanged. Measured on THIS merge of the branch with origin/main (the tree CI checks out for a pull_request) with the companion extension repos cloned pinned (112/112), against an origin/main control measured in the same environment — the control reproduced main's committed baseline byte for byte, so the growth is the branch's."
    },
    "/api/llm-bridge": {
      "from": 1686,
      "to": 1688,
      "pr": 2890,
      "reason": "cinatra#2790 S9f (PR #2890 carries this record): the recommendation-hold slice adds TWO first-party modules to the agents workspace package — packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision). Both are shared between the run page's server-action path (run-recommendation-actions.ts, run-actions.ts, server-actions.ts) and the widget's API branch (src/app/api/lifecycle-views/recommendation-hold/*, src/lib/lifecycle/recommendation-hold-widget-branch.ts), which is why they are standalone modules rather than bodies inside the server-action files the API branch cannot import. They sit on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on the four assistant-runtime routes, /sign-in unchanged. Measured on THIS merge of the branch with origin/main (the tree CI checks out for a pull_request) with the companion extension repos cloned pinned (112/112), against an origin/main control measured in the same environment — the control reproduced main's committed baseline byte for byte, so the growth is the branch's."
    },
    "/api/mcp": {
      "from": 1671,
      "to": 1673,
      "pr": 2890,
      "reason": "cinatra#2790 S9f (PR #2890 carries this record): the recommendation-hold slice adds TWO first-party modules to the agents workspace package — packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision). Both are shared between the run page's server-action path (run-recommendation-actions.ts, run-actions.ts, server-actions.ts) and the widget's API branch (src/app/api/lifecycle-views/recommendation-hold/*, src/lib/lifecycle/recommendation-hold-widget-branch.ts), which is why they are standalone modules rather than bodies inside the server-action files the API branch cannot import. They sit on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on the four assistant-runtime routes, /sign-in unchanged. Measured on THIS merge of the branch with origin/main (the tree CI checks out for a pull_request) with the companion extension repos cloned pinned (112/112), against an origin/main control measured in the same environment — the control reproduced main's committed baseline byte for byte, so the growth is the branch's."
    },
    "/chat": {
      "from": 1766,
      "to": 1768,
      "pr": 2890,
      "reason": "cinatra#2790 S9f (PR #2890 carries this record): the recommendation-hold slice adds TWO first-party modules to the agents workspace package — packages/agents/src/run-recommendation-core.ts (the per-chip decision core) and packages/agents/src/run-dispatch-core.ts (the run-dispatch core that advances the run after a decision). Both are shared between the run page's server-action path (run-recommendation-actions.ts, run-actions.ts, server-actions.ts) and the widget's API branch (src/app/api/lifecycle-views/recommendation-hold/*, src/lib/lifecycle/recommendation-hold-widget-branch.ts), which is why they are standalone modules rather than bodies inside the server-action files the API branch cannot import. They sit on the assistant-runtime graph, so they are reachable from every locked route that mounts it: +2 on the four assistant-runtime routes, /sign-in unchanged. Measured on THIS merge of the branch with origin/main (the tree CI checks out for a pull_request) with the companion extension repos cloned pinned (112/112), against an origin/main control measured in the same environment — the control reproduced main's committed baseline byte for byte, so the growth is the branch's."
    },
    "/sign-in": {
      "from": 219,
      "to": 220,
      "reason": "cinatra#2823 S9j (PR #2871 carries this record): src/lib/assistant-turn-supersede.ts — the assistant_turns SUPERSEDE (tombstone) statement builder. It is its OWN module because the org-axis write ledger requires one: src/lib/project-inheritance.ts is a BROAD module the raw org-axis table sweep does not prefix-cover (it carries a committed per-file DML-site count instead), so a new org-axis statement there would hide behind that count rather than register. Its own module is what gives it a row in src/lib/org-write/write-registry.ts, a prefix in scripts/audit/org-write-table-sweep.mjs and a writer-set lockstep pin over its export surface. It is reached from src/lib/project-inheritance.ts, which sits under the objects/thread store graph, so it is +1 on every locked route that reaches that store. It imports nothing but server-only: the edge is one pure statement builder, not a barrel. Measured with the companion extension repos cloned pinned (112/112), on the merge of this branch with origin/main, which is the tree CI checks out for a pull_request.",
      "pr": 2871
    }
  }
}
