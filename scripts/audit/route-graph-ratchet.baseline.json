{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1492,
    "/api/mcp": 1488,
    "/chat": 1548,
    "/api/a2a": 1493,
    "/api/llm-bridge": 1499
  },
  "absorbs": {
    "/sign-in": {
      "from": 1490,
      "to": 1492,
      "reason": "P4.1 WordPress+Drupal assistant registration + store.ts file-size-ratchet repair (cinatra#1823/#1037, PR #1827) adds exactly TWO first-party modules reachable from every route via the auth.ts bootstrap graph, a uniform +2: (1) packages/agents/src/builtin-assistant-template.ts — the built-in assistant-agent registration seam (BUILT_IN_{CINATRA,WORDPRESS,DRUPAL}_ASSISTANT_* identity constants, upsertBuiltInAssistantAgentTemplate, readAssistantConfigByPrincipalId) extracted VERBATIM out of packages/agents/src/store.ts so store.ts drops back under its 4323-line file-size ceiling; ./store re-exports the whole surface so it is first-party-reachable wherever ./store is and pulls NO new subtree (its only first-party imports, ./db and ./schema, are already reachable from ./store). (2) src/lib/assistant-runtime/cms-assistant-config.ts — the vendor-neutral WordPress/Drupal AssistantConfig definitions, imported by src/lib/assistant-agent-registration.ts (which the auth.ts bootstrap makes reachable from every route); its only first-party import (@/lib/assistant-config) was already reachable from that module pre-PR, so it adds exactly itself. resolve-runtime-config.ts is imported ONLY by the untracked /api/assistants/chat route and does not touch a tracked route. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0): a clean +2 (1490->1492). (Supersedes the branch's earlier stale +1 absorb, which predated the cms-assistant-config.ts vendor-neutralization refactor.)",
      "pr": 1827
    },
    "/api/mcp": {
      "from": 1486,
      "to": 1488,
      "reason": "P4.1 WordPress+Drupal assistant registration + store.ts file-size-ratchet repair (cinatra#1823/#1037, PR #1827) adds exactly TWO first-party modules reachable from every route via the auth.ts bootstrap graph, a uniform +2: (1) packages/agents/src/builtin-assistant-template.ts — the built-in assistant-agent registration seam (BUILT_IN_{CINATRA,WORDPRESS,DRUPAL}_ASSISTANT_* identity constants, upsertBuiltInAssistantAgentTemplate, readAssistantConfigByPrincipalId) extracted VERBATIM out of packages/agents/src/store.ts so store.ts drops back under its 4323-line file-size ceiling; ./store re-exports the whole surface so it is first-party-reachable wherever ./store is and pulls NO new subtree (its only first-party imports, ./db and ./schema, are already reachable from ./store). (2) src/lib/assistant-runtime/cms-assistant-config.ts — the vendor-neutral WordPress/Drupal AssistantConfig definitions, imported by src/lib/assistant-agent-registration.ts (which the auth.ts bootstrap makes reachable from every route); its only first-party import (@/lib/assistant-config) was already reachable from that module pre-PR, so it adds exactly itself. resolve-runtime-config.ts is imported ONLY by the untracked /api/assistants/chat route and does not touch a tracked route. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0): a clean +2 (1486->1488). (Supersedes the branch's earlier stale +1 absorb, which predated the cms-assistant-config.ts vendor-neutralization refactor.)",
      "pr": 1827
    },
    "/chat": {
      "from": 1546,
      "to": 1548,
      "reason": "P4.1 WordPress+Drupal assistant registration + store.ts file-size-ratchet repair (cinatra#1823/#1037, PR #1827) adds exactly TWO first-party modules reachable from every route via the auth.ts bootstrap graph, a uniform +2: (1) packages/agents/src/builtin-assistant-template.ts — the built-in assistant-agent registration seam (BUILT_IN_{CINATRA,WORDPRESS,DRUPAL}_ASSISTANT_* identity constants, upsertBuiltInAssistantAgentTemplate, readAssistantConfigByPrincipalId) extracted VERBATIM out of packages/agents/src/store.ts so store.ts drops back under its 4323-line file-size ceiling; ./store re-exports the whole surface so it is first-party-reachable wherever ./store is and pulls NO new subtree (its only first-party imports, ./db and ./schema, are already reachable from ./store). (2) src/lib/assistant-runtime/cms-assistant-config.ts — the vendor-neutral WordPress/Drupal AssistantConfig definitions, imported by src/lib/assistant-agent-registration.ts (which the auth.ts bootstrap makes reachable from every route); its only first-party import (@/lib/assistant-config) was already reachable from that module pre-PR, so it adds exactly itself. resolve-runtime-config.ts is imported ONLY by the untracked /api/assistants/chat route and does not touch a tracked route. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0): a clean +2 (1546->1548). (Supersedes the branch's earlier stale +1 absorb, which predated the cms-assistant-config.ts vendor-neutralization refactor.)",
      "pr": 1827
    },
    "/api/a2a": {
      "from": 1491,
      "to": 1493,
      "reason": "P4.1 WordPress+Drupal assistant registration + store.ts file-size-ratchet repair (cinatra#1823/#1037, PR #1827) adds exactly TWO first-party modules reachable from every route via the auth.ts bootstrap graph, a uniform +2: (1) packages/agents/src/builtin-assistant-template.ts — the built-in assistant-agent registration seam (BUILT_IN_{CINATRA,WORDPRESS,DRUPAL}_ASSISTANT_* identity constants, upsertBuiltInAssistantAgentTemplate, readAssistantConfigByPrincipalId) extracted VERBATIM out of packages/agents/src/store.ts so store.ts drops back under its 4323-line file-size ceiling; ./store re-exports the whole surface so it is first-party-reachable wherever ./store is and pulls NO new subtree (its only first-party imports, ./db and ./schema, are already reachable from ./store). (2) src/lib/assistant-runtime/cms-assistant-config.ts — the vendor-neutral WordPress/Drupal AssistantConfig definitions, imported by src/lib/assistant-agent-registration.ts (which the auth.ts bootstrap makes reachable from every route); its only first-party import (@/lib/assistant-config) was already reachable from that module pre-PR, so it adds exactly itself. resolve-runtime-config.ts is imported ONLY by the untracked /api/assistants/chat route and does not touch a tracked route. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0): a clean +2 (1491->1493). (Supersedes the branch's earlier stale +1 absorb, which predated the cms-assistant-config.ts vendor-neutralization refactor.)",
      "pr": 1827
    },
    "/api/llm-bridge": {
      "from": 1497,
      "to": 1499,
      "reason": "P4.1 WordPress+Drupal assistant registration + store.ts file-size-ratchet repair (cinatra#1823/#1037, PR #1827) adds exactly TWO first-party modules reachable from every route via the auth.ts bootstrap graph, a uniform +2: (1) packages/agents/src/builtin-assistant-template.ts — the built-in assistant-agent registration seam (BUILT_IN_{CINATRA,WORDPRESS,DRUPAL}_ASSISTANT_* identity constants, upsertBuiltInAssistantAgentTemplate, readAssistantConfigByPrincipalId) extracted VERBATIM out of packages/agents/src/store.ts so store.ts drops back under its 4323-line file-size ceiling; ./store re-exports the whole surface so it is first-party-reachable wherever ./store is and pulls NO new subtree (its only first-party imports, ./db and ./schema, are already reachable from ./store). (2) src/lib/assistant-runtime/cms-assistant-config.ts — the vendor-neutral WordPress/Drupal AssistantConfig definitions, imported by src/lib/assistant-agent-registration.ts (which the auth.ts bootstrap makes reachable from every route); its only first-party import (@/lib/assistant-config) was already reachable from that module pre-PR, so it adds exactly itself. resolve-runtime-config.ts is imported ONLY by the untracked /api/assistants/chat route and does not touch a tracked route. MEASURED via the route-graph ratchet with the companion extension repos cloned pinned (missingCount 0): a clean +2 (1497->1499). (Supersedes the branch's earlier stale +1 absorb, which predated the cms-assistant-config.ts vendor-neutralization refactor.)",
      "pr": 1827
    }
  }
}
