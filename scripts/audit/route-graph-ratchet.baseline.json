{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1480,
    "/api/mcp": 1476,
    "/chat": 1536,
    "/api/a2a": 1481,
    "/api/llm-bridge": 1487
  },
  "absorbs": {
    "/sign-in": {
      "from": 1479,
      "to": 1480,
      "reason": "cinatra#1713 AC2 (PR #1750) ratified MCP approval vocabulary — post-#1674 /sign-in reach. The net-new leaf module src/lib/mcp-approval-vocabulary.ts (checkMcpApprovalVocabulary; PURE module, ZERO imports) is imported by the two extension-toolbox boundaries src/lib/external-mcp-toolbox-loader.server.ts and src/lib/llm-toolbox-providers.ts. Before #1674 /sign-in was a thin pre-auth page that did NOT reach those boundaries (this PR's original baseline correctly left /sign-in unchanged). #1674 made src/lib/auth.ts statically reach src/lib/assistant-agent-registration.ts -> the @cinatra-ai/agents store -> the host connector/agent registry (the 150->1479 /sign-in raise #1674 itself absorbed), and auth.ts is reachable from EVERY tracked route via @cinatra-ai/permissions/pages; that same graph now reaches the two toolbox boundaries, so /sign-in picks up the single net-new leaf exactly like the four companion routes. NOT a narrowable barrel import: the leaf is the single shared enforcement boundary (AC2). RE-MEASURED on the reconcile merge tree (origin/main f703e57b [#1674] merged into the lane) with the companion extension repos cloned pinned (missingCount 0, exactly as CI does via clone-extensions): /sign-in 1479 -> 1480 (NET +1). Re-raises the PR #1674 /sign-in record (150->1479) by +1 (a re-raise replaces the record it layers on).",
      "pr": 1750
    },
    "/api/mcp": {
      "from": 1475,
      "to": 1476,
      "reason": "cinatra#1713 AC2 (PR #1750) ratified MCP approval vocabulary: the net-new leaf module src/lib/mcp-approval-vocabulary.ts (checkMcpApprovalVocabulary — the ONE shared fail-closed approval-vocabulary boundary check; PURE module, ZERO imports) is imported by the two extension-toolbox boundaries src/lib/external-mcp-toolbox-loader.server.ts and src/lib/llm-toolbox-providers.ts, both already first-party-reachable on this route, so the route grows by exactly +1 with no further subtree pulled. NOT a narrowable barrel import: AC2 deliberately extracts the check into ONE shared module so the two boundaries cannot drift — inlining it per-boundary to avoid the edge would defeat the single-enforcement design. RE-MEASURED on the reconcile merge tree (origin/main f703e57b [#1674] merged into the lane) with the companion extension repos cloned pinned (missingCount 0, exactly as CI does via clone-extensions): /api/mcp 1475 -> 1476 (NET +1). Re-raises the PR #1674 base ceiling for this route by +1 (a re-raise replaces the record it layers on). Post-#1674 /sign-in now reaches these same boundaries through the assistant-agent-registration graph #1674 wired into auth.ts — see the /sign-in record.",
      "pr": 1750
    },
    "/chat": {
      "from": 1535,
      "to": 1536,
      "reason": "cinatra#1713 AC2 (PR #1750) ratified MCP approval vocabulary: the net-new leaf module src/lib/mcp-approval-vocabulary.ts (checkMcpApprovalVocabulary — the ONE shared fail-closed approval-vocabulary boundary check; PURE module, ZERO imports) is imported by the two extension-toolbox boundaries src/lib/external-mcp-toolbox-loader.server.ts and src/lib/llm-toolbox-providers.ts, both already first-party-reachable on this route, so the route grows by exactly +1 with no further subtree pulled. NOT a narrowable barrel import: AC2 deliberately extracts the check into ONE shared module so the two boundaries cannot drift — inlining it per-boundary to avoid the edge would defeat the single-enforcement design. RE-MEASURED on the reconcile merge tree (origin/main f703e57b [#1674] merged into the lane) with the companion extension repos cloned pinned (missingCount 0, exactly as CI does via clone-extensions): /chat 1535 -> 1536 (NET +1). Re-raises the PR #1674 base ceiling for this route by +1 (a re-raise replaces the record it layers on). Post-#1674 /sign-in now reaches these same boundaries through the assistant-agent-registration graph #1674 wired into auth.ts — see the /sign-in record.",
      "pr": 1750
    },
    "/api/a2a": {
      "from": 1480,
      "to": 1481,
      "reason": "cinatra#1713 AC2 (PR #1750) ratified MCP approval vocabulary: the net-new leaf module src/lib/mcp-approval-vocabulary.ts (checkMcpApprovalVocabulary — the ONE shared fail-closed approval-vocabulary boundary check; PURE module, ZERO imports) is imported by the two extension-toolbox boundaries src/lib/external-mcp-toolbox-loader.server.ts and src/lib/llm-toolbox-providers.ts, both already first-party-reachable on this route, so the route grows by exactly +1 with no further subtree pulled. NOT a narrowable barrel import: AC2 deliberately extracts the check into ONE shared module so the two boundaries cannot drift — inlining it per-boundary to avoid the edge would defeat the single-enforcement design. RE-MEASURED on the reconcile merge tree (origin/main f703e57b [#1674] merged into the lane) with the companion extension repos cloned pinned (missingCount 0, exactly as CI does via clone-extensions): /api/a2a 1480 -> 1481 (NET +1). Re-raises the PR #1674 base ceiling for this route by +1 (a re-raise replaces the record it layers on). Post-#1674 /sign-in now reaches these same boundaries through the assistant-agent-registration graph #1674 wired into auth.ts — see the /sign-in record.",
      "pr": 1750
    },
    "/api/llm-bridge": {
      "from": 1486,
      "to": 1487,
      "reason": "cinatra#1713 AC2 (PR #1750) ratified MCP approval vocabulary: the net-new leaf module src/lib/mcp-approval-vocabulary.ts (checkMcpApprovalVocabulary — the ONE shared fail-closed approval-vocabulary boundary check; PURE module, ZERO imports) is imported by the two extension-toolbox boundaries src/lib/external-mcp-toolbox-loader.server.ts and src/lib/llm-toolbox-providers.ts, both already first-party-reachable on this route, so the route grows by exactly +1 with no further subtree pulled. NOT a narrowable barrel import: AC2 deliberately extracts the check into ONE shared module so the two boundaries cannot drift — inlining it per-boundary to avoid the edge would defeat the single-enforcement design. RE-MEASURED on the reconcile merge tree (origin/main f703e57b [#1674] merged into the lane) with the companion extension repos cloned pinned (missingCount 0, exactly as CI does via clone-extensions): /api/llm-bridge 1486 -> 1487 (NET +1). Re-raises the PR #1674 base ceiling for this route by +1 (a re-raise replaces the record it layers on). Post-#1674 /sign-in now reaches these same boundaries through the assistant-agent-registration graph #1674 wired into auth.ts — see the /sign-in record.",
      "pr": 1750
    }
  }
}
