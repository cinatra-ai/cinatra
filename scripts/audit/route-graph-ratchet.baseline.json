{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 133,
    "/api/mcp": 1373,
    "/chat": 1411,
    "/api/a2a": 1378,
    "/api/llm-bridge": 1384
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1376,
      "to": 1378,
      "reason": "cinatra#1193 (PR #1232): the run-token spine (W1) adds TWO net-new reachable first-party modules on every route that reaches the agent-execution graph — src/lib/agent-run-token.ts (the dispatch-minted per-run credential: mint + sha256 hash + the single fail-closed verifier) and packages/agents/src/wayflow-dispatch-payload.ts (the spread-then-overwrite WayFlow initial-message builder that carries the token) — both imported by packages/agents/src/execution.ts, which every locked agent-execution route reaches. setAgentRunTokenHash stays in packages/agents/src/store.ts (already in the graph), so it adds no module; the migration/schema changes add none either. The +2 delta is disjoint from the #1233 (+17) and #1269 (+2) module sets already in main. Re-reconciled onto main's CURRENT post-#1233/#1269 ceiling during the merge-queue rebase: +2. Absorbed raise 1376->1378.",
      "pr": 1232
    },
    "/api/llm-bridge": {
      "from": 1382,
      "to": 1384,
      "reason": "cinatra#1193 (PR #1232): the run-token spine (W1) adds TWO net-new reachable first-party modules on every route that reaches the agent-execution graph — src/lib/agent-run-token.ts (the dispatch-minted per-run credential: mint + sha256 hash + the single fail-closed verifier) and packages/agents/src/wayflow-dispatch-payload.ts (the spread-then-overwrite WayFlow initial-message builder that carries the token) — both imported by packages/agents/src/execution.ts, which every locked agent-execution route reaches. setAgentRunTokenHash stays in packages/agents/src/store.ts (already in the graph), so it adds no module; the migration/schema changes add none either. The +2 delta is disjoint from the #1233 (+17) and #1269 (+2) module sets already in main. Re-reconciled onto main's CURRENT post-#1233/#1269 ceiling during the merge-queue rebase: +2. Absorbed raise 1382->1384.",
      "pr": 1232
    },
    "/api/mcp": {
      "from": 1371,
      "to": 1373,
      "reason": "cinatra#1193 (PR #1232): the run-token spine (W1) adds TWO net-new reachable first-party modules on every route that reaches the agent-execution graph — src/lib/agent-run-token.ts (the dispatch-minted per-run credential: mint + sha256 hash + the single fail-closed verifier) and packages/agents/src/wayflow-dispatch-payload.ts (the spread-then-overwrite WayFlow initial-message builder that carries the token) — both imported by packages/agents/src/execution.ts, which every locked agent-execution route reaches. setAgentRunTokenHash stays in packages/agents/src/store.ts (already in the graph), so it adds no module; the migration/schema changes add none either. The +2 delta is disjoint from the #1233 (+17) and #1269 (+2) module sets already in main. Re-reconciled onto main's CURRENT post-#1233/#1269 ceiling during the merge-queue rebase: +2. Absorbed raise 1371->1373.",
      "pr": 1232
    },
    "/chat": {
      "from": 1409,
      "to": 1411,
      "reason": "cinatra#1193 (PR #1232): the run-token spine (W1) adds TWO net-new reachable first-party modules on every route that reaches the agent-execution graph — src/lib/agent-run-token.ts (the dispatch-minted per-run credential: mint + sha256 hash + the single fail-closed verifier) and packages/agents/src/wayflow-dispatch-payload.ts (the spread-then-overwrite WayFlow initial-message builder that carries the token) — both imported by packages/agents/src/execution.ts, which every locked agent-execution route reaches. setAgentRunTokenHash stays in packages/agents/src/store.ts (already in the graph), so it adds no module; the migration/schema changes add none either. The +2 delta is disjoint from the #1233 (+17) and #1269 (+2) module sets already in main. Re-reconciled onto main's CURRENT post-#1233/#1269 ceiling during the merge-queue rebase: +2. Absorbed raise 1409->1411.",
      "pr": 1232
    },
    "/sign-in": {
      "from": 132,
      "to": 133,
      "reason": "cinatra#1135 (PR #1137): the one-shot initial-admin bootstrap decision was extracted into a new dependency-free policy module src/lib/initial-admin-bootstrap-policy.ts (pure decision core, unit-testable without the server-only auth graph; same pattern as closed-registration-gate.ts), imported by src/lib/auth.ts — one new reachable first-party leaf on every route that reaches the auth core. Delta verified locally vs origin/main with the companion extension repos cloned pinned (missingCount 0, so the +1 is exact). Absorbed raise 132->133.",
      "pr": 1137
    }
  }
}
