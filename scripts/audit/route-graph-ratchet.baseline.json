{
    "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
    "routes": {
        "/sign-in": 163,
        "/api/mcp": 1759,
        "/chat": 1862,
        "/api/a2a": 1767,
        "/api/llm-bridge": 1777
    },
    "absorbs": {
        "/api/a2a": {
            "from": 1764,
            "to": 1767,
            "reason": "cinatra#2368 acceptance item 3 adds three first-party leaves reachable through the two host surfaces every one of these four routes already reaches: src/lib/appointment-schedule-add-mcp.ts (the MCP registration module for the appointment_schedule_add bridge, imported by src/lib/mcp-server.ts), src/lib/appointment-schedule-add.server.ts (its SHARED implementation, imported by src/lib/primitive-handlers.ts for the deterministic passthrough registry), and src/lib/extension-egress-fetch.ts (the host's ONE bounded extension-egress scope, which that shared implementation now enters). None of the three is reachable from /sign-in, which is why that route's count and its own record are unchanged. Narrowing is unavailable: the bridge is registered at module scope beside every sibling primitive-handler factory on a synchronous registry, and ONE NAME ONE OWNER requires both host surfaces to resolve to ONE implementation, so the leaves can neither collapse into one nor be lazy-loaded without splitting the shared road appointment-schedule-chat-registration.test.ts pins; and the egress scope IS the fix — the tool road was measured unanswered past a minute where the same add answered in 1.95 s through the ui-action road, precisely because only that road entered this module, so making it unreachable again would restore the fault. MEASURED, NOT COMPUTED, on d571a94bff6deaff5dacf123f74d4ec89bd3ab06 plus this leg: the ONLY import edge this leg adds is src/lib/appointment-schedule-add.server.ts -> src/lib/extension-egress-fetch.ts, and the analyzer reports exactly +1 on each of the four routes that reach that shared implementation — /api/mcp 1759 (ceiling 1758), /chat 1860 (1859), /api/a2a 1767 (1766), /api/llm-bridge 1777 (1776), matching the failing run exactly — while /sign-in, which reaches none of the three, does not move. This record REPLACES the earlier PR 3173 record for /api/a2a, the lifecycle the gate allows for an annotation whose ceiling moves.",
            "pr": 3173
        },
        "/api/llm-bridge": {
            "from": 1774,
            "to": 1777,
            "reason": "cinatra#2368 acceptance item 3 adds three first-party leaves reachable through the two host surfaces every one of these four routes already reaches: src/lib/appointment-schedule-add-mcp.ts (the MCP registration module for the appointment_schedule_add bridge, imported by src/lib/mcp-server.ts), src/lib/appointment-schedule-add.server.ts (its SHARED implementation, imported by src/lib/primitive-handlers.ts for the deterministic passthrough registry), and src/lib/extension-egress-fetch.ts (the host's ONE bounded extension-egress scope, which that shared implementation now enters). None of the three is reachable from /sign-in, which is why that route's count and its own record are unchanged. Narrowing is unavailable: the bridge is registered at module scope beside every sibling primitive-handler factory on a synchronous registry, and ONE NAME ONE OWNER requires both host surfaces to resolve to ONE implementation, so the leaves can neither collapse into one nor be lazy-loaded without splitting the shared road appointment-schedule-chat-registration.test.ts pins; and the egress scope IS the fix — the tool road was measured unanswered past a minute where the same add answered in 1.95 s through the ui-action road, precisely because only that road entered this module, so making it unreachable again would restore the fault. MEASURED, NOT COMPUTED, on d571a94bff6deaff5dacf123f74d4ec89bd3ab06 plus this leg: the ONLY import edge this leg adds is src/lib/appointment-schedule-add.server.ts -> src/lib/extension-egress-fetch.ts, and the analyzer reports exactly +1 on each of the four routes that reach that shared implementation — /api/mcp 1759 (ceiling 1758), /chat 1860 (1859), /api/a2a 1767 (1766), /api/llm-bridge 1777 (1776), matching the failing run exactly — while /sign-in, which reaches none of the three, does not move. This record REPLACES the earlier PR 3173 record for /api/llm-bridge, the lifecycle the gate allows for an annotation whose ceiling moves.",
            "pr": 3173
        },
        "/api/mcp": {
            "from": 1756,
            "to": 1759,
            "reason": "cinatra#2368 acceptance item 3 adds three first-party leaves reachable through the two host surfaces every one of these four routes already reaches: src/lib/appointment-schedule-add-mcp.ts (the MCP registration module for the appointment_schedule_add bridge, imported by src/lib/mcp-server.ts), src/lib/appointment-schedule-add.server.ts (its SHARED implementation, imported by src/lib/primitive-handlers.ts for the deterministic passthrough registry), and src/lib/extension-egress-fetch.ts (the host's ONE bounded extension-egress scope, which that shared implementation now enters). None of the three is reachable from /sign-in, which is why that route's count and its own record are unchanged. Narrowing is unavailable: the bridge is registered at module scope beside every sibling primitive-handler factory on a synchronous registry, and ONE NAME ONE OWNER requires both host surfaces to resolve to ONE implementation, so the leaves can neither collapse into one nor be lazy-loaded without splitting the shared road appointment-schedule-chat-registration.test.ts pins; and the egress scope IS the fix — the tool road was measured unanswered past a minute where the same add answered in 1.95 s through the ui-action road, precisely because only that road entered this module, so making it unreachable again would restore the fault. MEASURED, NOT COMPUTED, on d571a94bff6deaff5dacf123f74d4ec89bd3ab06 plus this leg: the ONLY import edge this leg adds is src/lib/appointment-schedule-add.server.ts -> src/lib/extension-egress-fetch.ts, and the analyzer reports exactly +1 on each of the four routes that reach that shared implementation — /api/mcp 1759 (ceiling 1758), /chat 1860 (1859), /api/a2a 1767 (1766), /api/llm-bridge 1777 (1776), matching the failing run exactly — while /sign-in, which reaches none of the three, does not move. This record REPLACES the earlier PR 3173 record for /api/mcp, the lifecycle the gate allows for an annotation whose ceiling moves.",
            "pr": 3173
        },
        "/chat": {
            "from": 1859,
            "to": 1862,
            "reason": "Forward merge of origin/main into fix/2368-second-live-round (PR 3173). `from` (1859) is main's own committed ceiling for /chat at the merged origin/main tip (main's PR 3053 schedule-card/placeholder record already raised this route 1857 to 1859, and that record is unaffected by this merge). This branch's own PR 3173 acceptance-item-3 leg independently raises /chat by the SAME three leaves documented in this baseline's /api/mcp, /api/a2a and /api/llm-bridge absorb records for the identical reason: src/lib/appointment-schedule-add-mcp.ts, src/lib/appointment-schedule-add.server.ts and src/lib/extension-egress-fetch.ts, all reachable from /chat through the same two host surfaces those routes already reach. MEASURED, NOT COMPUTED, on the merged tree with the companion extension fleet cloned pinned (115/115, missingCount 0): /chat measures 1862, exactly main's committed 1859 plus this branch's own +3 (the identical +3 the sibling routes recorded pre-merge), while /sign-in, which reaches none of the three leaves, is unaffected by this branch and independently measures BELOW its prior ceiling on the merged tree, so its own record is retired by --write-baseline rather than raised. Narrowing is unavailable for the same reason recorded on the sibling routes: the bridge is registered at module scope on a synchronous registry and ONE NAME ONE OWNER requires both host surfaces to resolve to ONE shared implementation.",
            "pr": 3173
        }
    }
}
