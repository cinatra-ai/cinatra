{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 1486,
    "/api/mcp": 1482,
    "/chat": 1542,
    "/api/a2a": 1487,
    "/api/llm-bridge": 1493
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1484,
      "to": 1487,
      "reason": "cinatra#1625 (PR #1784) S8/M3 field-renderer relocation — blog-wordpress-publish-agent claimant, per the entry-73 owner ruling (remove from core, move to the owning extension, do NOT add to prod; the extension stays dev-only). The draft-confirm HITL renderer moved OUT of packages/agents INTO @cinatra-ai/blog-wordpress-publish-agent, and the generated field-renderer component map (src/lib/generated/field-renderer-components.ts) gained a literal lazy import(\"@cinatra-ai/blog-wordpress-publish-agent/src/renderers/draft-confirm\"). That map is statically reachable from the client field-renderer registry (packages/agents/src/register-default-renderers.ts) on this route, so the extension-shipped renderer subtree — the renderer plus its VENDORED own-your-code design primitives (button/card + lib/utils; a pure confirm/reject card, no editable textarea/label) — becomes first-party-reachable; the deleted in-core copy shared the host primitives. MEASURED via an A/B differential with the companion extension repos cloned pinned (missingCount 0): the local baseline B reproduced the committed CI ceilings EXACTLY on every tracked route (post-#1754 macOS->CI reachability skew is 0), so the structural delta is a clean +3 (1484->1487). Layers on the PR #1754 exec-plane raise, whose record this retires by re-raising the ceiling.",
      "pr": 1784
    },
    "/api/llm-bridge": {
      "from": 1490,
      "to": 1493,
      "reason": "cinatra#1625 (PR #1784) S8/M3 field-renderer relocation — blog-wordpress-publish-agent claimant, per the entry-73 owner ruling (remove from core, move to the owning extension, do NOT add to prod; the extension stays dev-only). The draft-confirm HITL renderer moved OUT of packages/agents INTO @cinatra-ai/blog-wordpress-publish-agent, and the generated field-renderer component map (src/lib/generated/field-renderer-components.ts) gained a literal lazy import(\"@cinatra-ai/blog-wordpress-publish-agent/src/renderers/draft-confirm\"). That map is statically reachable from the client field-renderer registry (packages/agents/src/register-default-renderers.ts) on this route, so the extension-shipped renderer subtree — the renderer plus its VENDORED own-your-code design primitives (button/card + lib/utils; a pure confirm/reject card, no editable textarea/label) — becomes first-party-reachable; the deleted in-core copy shared the host primitives. MEASURED via an A/B differential with the companion extension repos cloned pinned (missingCount 0): the local baseline B reproduced the committed CI ceilings EXACTLY on every tracked route (post-#1754 macOS->CI reachability skew is 0), so the structural delta is a clean +3 (1490->1493). Layers on the PR #1754 exec-plane raise, whose record this retires by re-raising the ceiling.",
      "pr": 1784
    },
    "/api/mcp": {
      "from": 1479,
      "to": 1482,
      "reason": "cinatra#1625 (PR #1784) S8/M3 field-renderer relocation — blog-wordpress-publish-agent claimant, per the entry-73 owner ruling (remove from core, move to the owning extension, do NOT add to prod; the extension stays dev-only). The draft-confirm HITL renderer moved OUT of packages/agents INTO @cinatra-ai/blog-wordpress-publish-agent, and the generated field-renderer component map (src/lib/generated/field-renderer-components.ts) gained a literal lazy import(\"@cinatra-ai/blog-wordpress-publish-agent/src/renderers/draft-confirm\"). That map is statically reachable from the client field-renderer registry (packages/agents/src/register-default-renderers.ts) on this route, so the extension-shipped renderer subtree — the renderer plus its VENDORED own-your-code design primitives (button/card + lib/utils; a pure confirm/reject card, no editable textarea/label) — becomes first-party-reachable; the deleted in-core copy shared the host primitives. MEASURED via an A/B differential with the companion extension repos cloned pinned (missingCount 0): the local baseline B reproduced the committed CI ceilings EXACTLY on every tracked route (post-#1754 macOS->CI reachability skew is 0), so the structural delta is a clean +3 (1479->1482). Layers on the PR #1754 exec-plane raise, whose record this retires by re-raising the ceiling.",
      "pr": 1784
    },
    "/chat": {
      "from": 1539,
      "to": 1542,
      "reason": "cinatra#1625 (PR #1784) S8/M3 field-renderer relocation — blog-wordpress-publish-agent claimant, per the entry-73 owner ruling (remove from core, move to the owning extension, do NOT add to prod; the extension stays dev-only). The draft-confirm HITL renderer moved OUT of packages/agents INTO @cinatra-ai/blog-wordpress-publish-agent, and the generated field-renderer component map (src/lib/generated/field-renderer-components.ts) gained a literal lazy import(\"@cinatra-ai/blog-wordpress-publish-agent/src/renderers/draft-confirm\"). That map is statically reachable from the client field-renderer registry (packages/agents/src/register-default-renderers.ts), so the extension-shipped renderer subtree — the renderer plus its VENDORED own-your-code design primitives (button/card + lib/utils; a pure confirm/reject card, no editable textarea/label) — becomes first-party-reachable; the deleted in-core copy shared the host primitives. /chat reaches the full field-renderer registry graph. MEASURED via an A/B differential with the companion extension repos cloned pinned (missingCount 0): the local baseline B reproduced the committed CI ceilings EXACTLY on every tracked route (post-#1754 macOS->CI reachability skew is 0), so the structural delta is a clean +3 (1539->1542). Layers on the PR #1754 exec-plane raise, whose record this retires by re-raising the ceiling.",
      "pr": 1784
    },
    "/sign-in": {
      "from": 1483,
      "to": 1486,
      "reason": "cinatra#1625 (PR #1784) S8/M3 field-renderer relocation — blog-wordpress-publish-agent claimant, per the entry-73 owner ruling (remove from core, move to the owning extension, do NOT add to prod; the extension stays dev-only). The draft-confirm HITL renderer moved OUT of packages/agents INTO @cinatra-ai/blog-wordpress-publish-agent, and the generated field-renderer component map (src/lib/generated/field-renderer-components.ts) gained a literal lazy import(\"@cinatra-ai/blog-wordpress-publish-agent/src/renderers/draft-confirm\"). That map is statically reachable from the client field-renderer registry (packages/agents/src/register-default-renderers.ts) on this route, so the extension-shipped renderer subtree — the renderer plus its VENDORED own-your-code design primitives (button/card + lib/utils; a pure confirm/reject card, no editable textarea/label) — becomes first-party-reachable; the deleted in-core copy shared the host primitives. MEASURED via an A/B differential with the companion extension repos cloned pinned (missingCount 0): the local baseline B reproduced the committed CI ceilings EXACTLY on every tracked route (post-#1754 macOS->CI reachability skew is 0), so the structural delta is a clean +3 (1483->1486). Layers on the PR #1754 exec-plane raise, whose record this retires by re-raising the ceiling.",
      "pr": 1784
    }
  }
}
