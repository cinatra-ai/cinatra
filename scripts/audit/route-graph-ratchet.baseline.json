{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 222,
    "/api/mcp": 1675,
    "/chat": 1771,
    "/api/a2a": 1680,
    "/api/llm-bridge": 1690
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1679,
      "to": 1680,
      "reason": "cinatra#2788 (PR #2939 carries this record), REBASED ON THE MERGE-FORWARD onto origin/main: the S9d schedule card's save path arms a recurring trigger through `packages/agents/src/trigger-recurrence.ts` — the ONE module that says what a recurrence selection means — instead of restating the cron vocabulary a second time. `packages/agents/src/trigger-service.ts` (already inside the graph every locked route reaches) imports `buildCron` and `RecurringConfig` from it, so that leaf becomes reachable and the count is +1. MEASURED AS A DELTA on four trees analysed identically by scripts/route-graph.mjs — the merge-base, origin/main at 95f3dd65163577becfa3d591b91c1fb2c637c3d4, this branch's head at abdc5b9995a3ff9c175f831a4c2be70b56d7436c and the merge commit itself — with the companion extension fleet absent on ALL FOUR, so the same first-party imports are unresolved in each and the DIFFERENCE between two counts is exact even though the absolute counts are deflated. `from` is therefore not re-measured here: it is main's own ceiling, measured in CI with the fleet cloned pinned. TWO CONTROLS hold the method: merge-base -> origin/main reproduces main's committed W2a delta on this route exactly, and merge-base -> this branch's head reproduces this branch's own committed delta exactly. The two raises COMPOSE because their modules are disjoint — neither of W2a's two modules (the lifecycle coordinator and the moment-triple DDL leaf) is one of this branch's, and this branch's route through the coordinator adds no module of its own: `packages/agents/src/trigger-service.ts` already imported `packages/agents/src/lifecycle-coordinator.ts` on main.",
      "pr": 2939
    },
    "/api/llm-bridge": {
      "from": 1689,
      "to": 1690,
      "reason": "cinatra#2788 (PR #2939 carries this record), REBASED ON THE MERGE-FORWARD onto origin/main: the S9d schedule card's save path arms a recurring trigger through `packages/agents/src/trigger-recurrence.ts` — the ONE module that says what a recurrence selection means — instead of restating the cron vocabulary a second time. `packages/agents/src/trigger-service.ts` (already inside the graph every locked route reaches) imports `buildCron` and `RecurringConfig` from it, so that leaf becomes reachable and the count is +1. MEASURED AS A DELTA on four trees analysed identically by scripts/route-graph.mjs — the merge-base, origin/main at 95f3dd65163577becfa3d591b91c1fb2c637c3d4, this branch's head at abdc5b9995a3ff9c175f831a4c2be70b56d7436c and the merge commit itself — with the companion extension fleet absent on ALL FOUR, so the same first-party imports are unresolved in each and the DIFFERENCE between two counts is exact even though the absolute counts are deflated. `from` is therefore not re-measured here: it is main's own ceiling, measured in CI with the fleet cloned pinned. TWO CONTROLS hold the method: merge-base -> origin/main reproduces main's committed W2a delta on this route exactly, and merge-base -> this branch's head reproduces this branch's own committed delta exactly. The two raises COMPOSE because their modules are disjoint — neither of W2a's two modules (the lifecycle coordinator and the moment-triple DDL leaf) is one of this branch's, and this branch's route through the coordinator adds no module of its own: `packages/agents/src/trigger-service.ts` already imported `packages/agents/src/lifecycle-coordinator.ts` on main.",
      "pr": 2939
    },
    "/api/mcp": {
      "from": 1674,
      "to": 1675,
      "reason": "cinatra#2788 (PR #2939 carries this record), REBASED ON THE MERGE-FORWARD onto origin/main: the S9d schedule card's save path arms a recurring trigger through `packages/agents/src/trigger-recurrence.ts` — the ONE module that says what a recurrence selection means — instead of restating the cron vocabulary a second time. `packages/agents/src/trigger-service.ts` (already inside the graph every locked route reaches) imports `buildCron` and `RecurringConfig` from it, so that leaf becomes reachable and the count is +1. MEASURED AS A DELTA on four trees analysed identically by scripts/route-graph.mjs — the merge-base, origin/main at 95f3dd65163577becfa3d591b91c1fb2c637c3d4, this branch's head at abdc5b9995a3ff9c175f831a4c2be70b56d7436c and the merge commit itself — with the companion extension fleet absent on ALL FOUR, so the same first-party imports are unresolved in each and the DIFFERENCE between two counts is exact even though the absolute counts are deflated. `from` is therefore not re-measured here: it is main's own ceiling, measured in CI with the fleet cloned pinned. TWO CONTROLS hold the method: merge-base -> origin/main reproduces main's committed W2a delta on this route exactly, and merge-base -> this branch's head reproduces this branch's own committed delta exactly. The two raises COMPOSE because their modules are disjoint — neither of W2a's two modules (the lifecycle coordinator and the moment-triple DDL leaf) is one of this branch's, and this branch's route through the coordinator adds no module of its own: `packages/agents/src/trigger-service.ts` already imported `packages/agents/src/lifecycle-coordinator.ts` on main.",
      "pr": 2939
    },
    "/chat": {
      "from": 1769,
      "to": 1771,
      "reason": "cinatra#2788 (PR #2939 carries this record), REBASED ON THE MERGE-FORWARD onto origin/main: +2 on this route. (1) `packages/agents/src/trigger-recurrence.ts` — `packages/agents/src/trigger-service.ts` now imports `buildCron` and `RecurringConfig` from it so the S9d save path arms a recurring trigger through the ONE module that says what a recurrence selection means, and that leaf becomes reachable on every locked route that reaches the trigger surface. (2) `packages/agents/src/schedule-proposal-card.tsx` — the chat renderable-view registry now dispatches `trigger_schedule_proposal` to the DRAWN card and the S1 placeholder shell is retired for this kind, so the card is reachable from /chat alone. No other module joins this route's graph. MEASURED AS A DELTA on four trees analysed identically by scripts/route-graph.mjs — the merge-base, origin/main at 95f3dd65163577becfa3d591b91c1fb2c637c3d4, this branch's head at abdc5b9995a3ff9c175f831a4c2be70b56d7436c and the merge commit itself — with the companion extension fleet absent on ALL FOUR, so the same first-party imports are unresolved in each and the DIFFERENCE between two counts is exact even though the absolute counts are deflated. `from` is therefore not re-measured here: it is main's own ceiling, measured in CI with the fleet cloned pinned. TWO CONTROLS hold the method: merge-base -> origin/main reproduces main's committed W2a delta on this route exactly, and merge-base -> this branch's head reproduces this branch's own committed delta exactly. The two raises COMPOSE because their modules are disjoint — neither of W2a's two modules (the lifecycle coordinator and the moment-triple DDL leaf) is one of this branch's, and this branch's route through the coordinator adds no module of its own: `packages/agents/src/trigger-service.ts` already imported `packages/agents/src/lifecycle-coordinator.ts` on main.",
      "pr": 2939
    },
    "/sign-in": {
      "from": 221,
      "to": 222,
      "reason": "cinatra#2928 (epic #2926 W2a): the LIFECYCLE COORDINATOR and the agent_runs moment-triple DDL leaf. TWO new modules, and they are the two the slice is: (1) packages/agents/src/lifecycle-coordinator.ts — the one runner every producer creates through, which is what the creation fence (scripts/audit/run-creation-fence.mjs) enforces; it replaces three copies of the create-parked -> evaluate -> release-or-park sequence rather than adding a fourth, so the graph gains one module while the code it reaches loses two duplicates. (2) src/lib/agent-run-lifecycle-moment-schema.ts — a zero-import string builder holding the three additive nullable agent_runs columns and their partial index, in its OWN leaf for the same reason the created-at leaf of #2911 is one: src/lib/drizzle-store.ts sits AT its file-size ceiling, which may only shrink, so the DDL cannot be inlined. Neither module imports a barrel; each edge is one module. MEASURED with the companion extension repos cloned PINNED (112/112 at their lock SHAs) on this branch and, as the control, on origin/main at 7432e9562f0e0ef179bb79dfdfb0e8d3c7cb7b8e — the control reproduced the committed main ceilings EXACTLY (the ratchet reported no route over baseline), and the branch measured +1 (the DDL leaf ONLY — this route reaches the store graph but not the agents graph, which is the asymmetry that makes the two edges legible).",
      "pr": 2928
    }
  }
}
