{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 150,
    "/api/mcp": 1467,
    "/chat": 1527,
    "/api/a2a": 1472,
    "/api/llm-bridge": 1478
  },
  "absorbs": {
    "/chat": {
      "from": 1525,
      "to": 1527,
      "reason": "cinatra#1737 (PR #1741) breadcrumb-contribution channel: packages/agents/src/agent-page-layout.tsx — already first-party-reachable on this route — now publishes the run/template identity it owns into the app-wide crumb channel (ONE agent-name channel, replacing the divergent name-changed event + /api/agents/instance-name fetch), importing the two net-new leaf modules src/lib/breadcrumb-contributions.ts (framework-free contribution bus; zero imports) and src/components/crumb-epoch-context.tsx (session/org epoch context fencing stale snapshots; imports react only). Both are true leaves, so every route already reaching agent-page-layout grows by exactly +2 with no further subtree pulled. NOT a narrowable barrel import: the agent-page-layout edge is pre-existing; narrowing it is an agents-architecture change out of this PR's scope. MEASURED by CI's authoritative merge-ref run (Actions run 29579073295, missingCount 0): /chat 1525 -> 1527 (NET +2). Layers on the PR #1740 chart-host raise (1508->1525), whose record this retires by re-raising the ceiling.",
      "pr": 1741
    },
    "/api/a2a": {
      "from": 1470,
      "to": 1472,
      "reason": "cinatra#1737 (PR #1741) breadcrumb-contribution channel: packages/agents/src/agent-page-layout.tsx — already first-party-reachable on this route via the agents client surface — now publishes the run/template identity it owns into the app-wide crumb channel (ONE agent-name channel, replacing the divergent name-changed event + /api/agents/instance-name fetch), importing the two net-new leaf modules src/lib/breadcrumb-contributions.ts (framework-free contribution bus; zero imports) and src/components/crumb-epoch-context.tsx (session/org epoch context fencing stale snapshots; imports react only). Both are true leaves, so every route already reaching agent-page-layout grows by exactly +2 with no further subtree pulled. NOT a narrowable barrel import: the agent-page-layout edge is pre-existing; narrowing it is an agents-architecture change out of this PR's scope. MEASURED by CI's authoritative merge-ref run (Actions run 29579073295, missingCount 0): /api/a2a 1470 -> 1472 (NET +2).",
      "pr": 1741
    },
    "/api/llm-bridge": {
      "from": 1476,
      "to": 1478,
      "reason": "cinatra#1737 (PR #1741) breadcrumb-contribution channel: packages/agents/src/agent-page-layout.tsx — already first-party-reachable on this route via the agents client surface — now publishes the run/template identity it owns into the app-wide crumb channel (ONE agent-name channel, replacing the divergent name-changed event + /api/agents/instance-name fetch), importing the two net-new leaf modules src/lib/breadcrumb-contributions.ts (framework-free contribution bus; zero imports) and src/components/crumb-epoch-context.tsx (session/org epoch context fencing stale snapshots; imports react only). Both are true leaves, so every route already reaching agent-page-layout grows by exactly +2 with no further subtree pulled. NOT a narrowable barrel import: the agent-page-layout edge is pre-existing; narrowing it is an agents-architecture change out of this PR's scope. MEASURED by CI's authoritative merge-ref run (Actions run 29579073295, missingCount 0): /api/llm-bridge 1476 -> 1478 (NET +2).",
      "pr": 1741
    },
    "/api/mcp": {
      "from": 1465,
      "to": 1467,
      "reason": "cinatra#1737 (PR #1741) breadcrumb-contribution channel: packages/agents/src/agent-page-layout.tsx — already first-party-reachable on this route via the agents client surface — now publishes the run/template identity it owns into the app-wide crumb channel (ONE agent-name channel, replacing the divergent name-changed event + /api/agents/instance-name fetch), importing the two net-new leaf modules src/lib/breadcrumb-contributions.ts (framework-free contribution bus; zero imports) and src/components/crumb-epoch-context.tsx (session/org epoch context fencing stale snapshots; imports react only). Both are true leaves, so every route already reaching agent-page-layout grows by exactly +2 with no further subtree pulled. NOT a narrowable barrel import: the agent-page-layout edge is pre-existing; narrowing it is an agents-architecture change out of this PR's scope. MEASURED by CI's authoritative merge-ref run (Actions run 29579073295, missingCount 0): /api/mcp 1465 -> 1467 (NET +2).",
      "pr": 1741
    },
    "/sign-in": {
      "from": 149,
      "to": 150,
      "reason": "cinatra#1565 (PR #1579) org member-invitation email: src/lib/auth.ts (reachable from every tracked route) gains the new leaf module src/lib/org-invitation-email.ts — the pure accept-link/subject/body builder wired into the organization.sendInvitationEmail callback (Better Auth ships NO default invitation sender, so inviteMember created an invitation row but sent nothing). MEASURED via an A/B differential using scripts/route-graph.mjs analyzeRoute: with the module present vs moved aside, EVERY tracked route drops by exactly 1, so the raise is a single net-new fully-resolvable leaf, invariant to the missing companion-extension modules. /sign-in measured 150 locally at missingCount 0; the four companion routes measured the identical -1 delta (their absolute count is CI-measured with the extensions cloned pinned, this base+1). Absorbed raise 149->150 (NET +1). RE-BASED 2026-07-15 after main-side baseline rewrite (objects/memory merges); from/to re-derived on the new base, same measured NET +1 — CI re-verifies on the merged tree.",
      "pr": 1579
    }
  }
}
