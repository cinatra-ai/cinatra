{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 136,
    "/api/mcp": 1398,
    "/chat": 1436,
    "/api/a2a": 1403,
    "/api/llm-bridge": 1409
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1402,
      "to": 1403,
      "reason": "cinatra#1195 (PR #1354): the durable run-context binding adds ONE net-new reachable first-party module — src/lib/agent-run-context-durable.ts (the run-token-keyed redis binding: writer/reader/classification + the served-by cutover counters). Its only imports are server-only, node:crypto, and ioredis (all non-first-party), so it contributes exactly one leaf. Reachable via src/lib/mcp-server.ts (the MCP mount wiring: resolveDurableRunContext + the onRunContextServedBy metric hook) on /api/mcp, /chat, and /api/a2a, and via the direct writer import in src/app/api/llm-bridge/route.ts on /api/llm-bridge. The run-context PRECEDENCE function was deliberately placed on the existing packages/mcp-server/src/request-context.ts module (already reachable) to avoid a second leaf. Re-anchored onto the post-#1304 ceilings after rebase: the +1 delta was re-proven by MEASUREMENT on the rebased tree vs origin/main under an identical extension-clone state (delta exact on all four routes, /sign-in unchanged), and the CI gate measurement of the pre-rebase PR tree (companion extension repos cloned pinned, missingCount 0) reproduced the same exact +1 per route. Absorbed raise per route: base ceiling +1.",
      "pr": 1354
    },
    "/api/llm-bridge": {
      "from": 1408,
      "to": 1409,
      "reason": "cinatra#1195 (PR #1354): the durable run-context binding adds ONE net-new reachable first-party module — src/lib/agent-run-context-durable.ts (the run-token-keyed redis binding: writer/reader/classification + the served-by cutover counters). Its only imports are server-only, node:crypto, and ioredis (all non-first-party), so it contributes exactly one leaf. Reachable via src/lib/mcp-server.ts (the MCP mount wiring: resolveDurableRunContext + the onRunContextServedBy metric hook) on /api/mcp, /chat, and /api/a2a, and via the direct writer import in src/app/api/llm-bridge/route.ts on /api/llm-bridge. The run-context PRECEDENCE function was deliberately placed on the existing packages/mcp-server/src/request-context.ts module (already reachable) to avoid a second leaf. Re-anchored onto the post-#1304 ceilings after rebase: the +1 delta was re-proven by MEASUREMENT on the rebased tree vs origin/main under an identical extension-clone state (delta exact on all four routes, /sign-in unchanged), and the CI gate measurement of the pre-rebase PR tree (companion extension repos cloned pinned, missingCount 0) reproduced the same exact +1 per route. Absorbed raise per route: base ceiling +1.",
      "pr": 1354
    },
    "/api/mcp": {
      "from": 1397,
      "to": 1398,
      "reason": "cinatra#1195 (PR #1354): the durable run-context binding adds ONE net-new reachable first-party module — src/lib/agent-run-context-durable.ts (the run-token-keyed redis binding: writer/reader/classification + the served-by cutover counters). Its only imports are server-only, node:crypto, and ioredis (all non-first-party), so it contributes exactly one leaf. Reachable via src/lib/mcp-server.ts (the MCP mount wiring: resolveDurableRunContext + the onRunContextServedBy metric hook) on /api/mcp, /chat, and /api/a2a, and via the direct writer import in src/app/api/llm-bridge/route.ts on /api/llm-bridge. The run-context PRECEDENCE function was deliberately placed on the existing packages/mcp-server/src/request-context.ts module (already reachable) to avoid a second leaf. Re-anchored onto the post-#1304 ceilings after rebase: the +1 delta was re-proven by MEASUREMENT on the rebased tree vs origin/main under an identical extension-clone state (delta exact on all four routes, /sign-in unchanged), and the CI gate measurement of the pre-rebase PR tree (companion extension repos cloned pinned, missingCount 0) reproduced the same exact +1 per route. Absorbed raise per route: base ceiling +1.",
      "pr": 1354
    },
    "/chat": {
      "from": 1435,
      "to": 1436,
      "reason": "cinatra#1195 (PR #1354): the durable run-context binding adds ONE net-new reachable first-party module — src/lib/agent-run-context-durable.ts (the run-token-keyed redis binding: writer/reader/classification + the served-by cutover counters). Its only imports are server-only, node:crypto, and ioredis (all non-first-party), so it contributes exactly one leaf. Reachable via src/lib/mcp-server.ts (the MCP mount wiring: resolveDurableRunContext + the onRunContextServedBy metric hook) on /api/mcp, /chat, and /api/a2a, and via the direct writer import in src/app/api/llm-bridge/route.ts on /api/llm-bridge. The run-context PRECEDENCE function was deliberately placed on the existing packages/mcp-server/src/request-context.ts module (already reachable) to avoid a second leaf. Re-anchored onto the post-#1304 ceilings after rebase: the +1 delta was re-proven by MEASUREMENT on the rebased tree vs origin/main under an identical extension-clone state (delta exact on all four routes, /sign-in unchanged), and the CI gate measurement of the pre-rebase PR tree (companion extension repos cloned pinned, missingCount 0) reproduced the same exact +1 per route. Absorbed raise per route: base ceiling +1.",
      "pr": 1354
    },
    "/sign-in": {
      "from": 135,
      "to": 136,
      "reason": "cinatra#1037 P2a (PR #1304): /sign-in reaches drizzle-store (auth -> DB) but NOT the chat runner, so it gains EXACTLY 1 first-party leaf: src/lib/assistant-thread-schema.ts (the new pure-strings bootstrap-DDL leaf for the assistant_threads/assistant_turns tables, imported by drizzle-store.ts). The four assistant-runtime leaves stay off /sign-in's graph. Delta measured B-A on the identical tree. Re-anchored onto main's current post-#1234 ceilings by REMEASUREMENT (never arithmetic): after merging current main (whose ceilings now include cinatra#1057 PR #1234's +2 dispatch-surface modules and cinatra#1317's +1 co-owner-constraint leaf) into the PR branch, scripts/audit/route-graph-ratchet.mjs was run on the merged tree with the companion extension repos cloned pinned (missingCount 0), measuring 136 against main's committed ceiling 135 — the exact +1 reproduced. Absorbed raise 135->136.",
      "pr": 1304
    }
  }
}
