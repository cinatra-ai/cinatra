{
  "note": "Route-graph ratchet baseline (no-new-rot ratchet). Each entry is the CURRENT reachable-first-party-module-count ceiling for a LOCKED FIXED_ROUTES route (the primary dev-perf 'first-party graph pressure' metric from scripts/route-graph.mjs). The gate fails when a tracked route's count grows BEYOND its ceiling and when the committed baseline raises any ceiling vs the base branch WITHOUT a matching absorb record. Counts are captured WITH the companion extension repos cloned pinned (exactly as CI does via clone-extensions) so they reproduce in CI. Regenerate with `node scripts/audit/route-graph-ratchet.mjs --write-baseline` after cloning the extensions — a ceiling should only ever be LOWERED as barrel imports are narrowed, and is never raised SILENTLY. A sanctioned raise must be ANNOTATED: a sibling `absorbs` record `route -> { from, to, reason, pr }` whose from/to exactly match the raise vs the base branch (from = the base ceiling, to = the new committed ceiling, reason = why the growth is accepted, pr = the PR carrying the absorbed change). The gate validates records strictly (malformed/stale/orphan records fail closed; a carried-forward record may not be deleted while its raised ceiling is kept) and prints a LOUD NOTICE line for every absorbed raise. The tracked route set is route-graph.mjs FIXED_ROUTES; change it there, then regenerate.",
  "routes": {
    "/sign-in": 218,
    "/api/mcp": 1705,
    "/chat": 1777,
    "/api/a2a": 1710,
    "/api/llm-bridge": 1720
  },
  "absorbs": {
    "/api/a2a": {
      "from": 1708,
      "pr": 2668,
      "reason": "cinatra#2577 (epic #2564 S8d), on top of the post-#2689 base ceiling: the widget lifecycle READ branch. The three read-only lifecycle pull primitives are now reachable on the `delegated-widget` policy, and the producer (src/lib/lifecycle/lifecycle-pull-mcp.ts) therefore needs a way to build the widget reader real standing — which is two NEW leaves on the locked routes: the live-standing actor (src/lib/lifecycle/widget-lifecycle-frame-actor.ts) and the widget auth-audit sink (src/lib/widget-auth-audit.ts). Uniform +2 on exactly the four routes that mount the MCP server (/sign-in untouched at 218). This record REPLACES the carried #2689 record on the same route: #2689 (the widget OBO token seals, +2) is already IN the base ceiling this raise starts from, so `from` is the post-#2689 value and the delta annotated here is only this PR own +2 — the two branches' new leaves are disjoint, so nothing is double-counted. NARROWING ALREADY APPLIED, and it is the reason this is +2 and not +15: the obvious edge is `widget-lifecycle-actor.ts`, the S8a door, which pulls @/lib/widget-user-auth and its synchronous postgres store leaf for a `cwu_` consume a widget MCP frame never performs. That module was SPLIT so the producer imports only the live-standing half; both entries still call one resolver, pinned by widget-lifecycle-actor-parity.test.ts. Further narrowing considered and rejected: dropping the audit sink would remove the authz audit trail from a security-relevant read path (and the analyzer follows dynamic import(), so deferring buys nothing). Measured as a uniform +2 delta against a clean origin/main worktree in the same environment, re-based onto the post-#2689 main.",
      "to": 1710
    },
    "/api/llm-bridge": {
      "from": 1718,
      "pr": 2668,
      "reason": "cinatra#2577 (epic #2564 S8d), on top of the post-#2689 base ceiling: the widget lifecycle READ branch. The three read-only lifecycle pull primitives are now reachable on the `delegated-widget` policy, and the producer (src/lib/lifecycle/lifecycle-pull-mcp.ts) therefore needs a way to build the widget reader real standing — which is two NEW leaves on the locked routes: the live-standing actor (src/lib/lifecycle/widget-lifecycle-frame-actor.ts) and the widget auth-audit sink (src/lib/widget-auth-audit.ts). Uniform +2 on exactly the four routes that mount the MCP server (/sign-in untouched at 218). This record REPLACES the carried #2689 record on the same route: #2689 (the widget OBO token seals, +2) is already IN the base ceiling this raise starts from, so `from` is the post-#2689 value and the delta annotated here is only this PR own +2 — the two branches' new leaves are disjoint, so nothing is double-counted. NARROWING ALREADY APPLIED, and it is the reason this is +2 and not +15: the obvious edge is `widget-lifecycle-actor.ts`, the S8a door, which pulls @/lib/widget-user-auth and its synchronous postgres store leaf for a `cwu_` consume a widget MCP frame never performs. That module was SPLIT so the producer imports only the live-standing half; both entries still call one resolver, pinned by widget-lifecycle-actor-parity.test.ts. Further narrowing considered and rejected: dropping the audit sink would remove the authz audit trail from a security-relevant read path (and the analyzer follows dynamic import(), so deferring buys nothing). Measured as a uniform +2 delta against a clean origin/main worktree in the same environment, re-based onto the post-#2689 main.",
      "to": 1720
    },
    "/api/mcp": {
      "from": 1703,
      "pr": 2668,
      "reason": "cinatra#2577 (epic #2564 S8d), on top of the post-#2689 base ceiling: the widget lifecycle READ branch. The three read-only lifecycle pull primitives are now reachable on the `delegated-widget` policy, and the producer (src/lib/lifecycle/lifecycle-pull-mcp.ts) therefore needs a way to build the widget reader real standing — which is two NEW leaves on the locked routes: the live-standing actor (src/lib/lifecycle/widget-lifecycle-frame-actor.ts) and the widget auth-audit sink (src/lib/widget-auth-audit.ts). Uniform +2 on exactly the four routes that mount the MCP server (/sign-in untouched at 218). This record REPLACES the carried #2689 record on the same route: #2689 (the widget OBO token seals, +2) is already IN the base ceiling this raise starts from, so `from` is the post-#2689 value and the delta annotated here is only this PR own +2 — the two branches' new leaves are disjoint, so nothing is double-counted. NARROWING ALREADY APPLIED, and it is the reason this is +2 and not +15: the obvious edge is `widget-lifecycle-actor.ts`, the S8a door, which pulls @/lib/widget-user-auth and its synchronous postgres store leaf for a `cwu_` consume a widget MCP frame never performs. That module was SPLIT so the producer imports only the live-standing half; both entries still call one resolver, pinned by widget-lifecycle-actor-parity.test.ts. Further narrowing considered and rejected: dropping the audit sink would remove the authz audit trail from a security-relevant read path (and the analyzer follows dynamic import(), so deferring buys nothing). Measured as a uniform +2 delta against a clean origin/main worktree in the same environment, re-based onto the post-#2689 main.",
      "to": 1705
    },
    "/chat": {
      "from": 1775,
      "pr": 2668,
      "reason": "cinatra#2577 (epic #2564 S8d), on top of the post-#2689 base ceiling: the widget lifecycle READ branch. The three read-only lifecycle pull primitives are now reachable on the `delegated-widget` policy, and the producer (src/lib/lifecycle/lifecycle-pull-mcp.ts) therefore needs a way to build the widget reader real standing — which is two NEW leaves on the locked routes: the live-standing actor (src/lib/lifecycle/widget-lifecycle-frame-actor.ts) and the widget auth-audit sink (src/lib/widget-auth-audit.ts). Uniform +2 on exactly the four routes that mount the MCP server (/sign-in untouched at 218). This record REPLACES the carried #2689 record on the same route: #2689 (the widget OBO token seals, +2) is already IN the base ceiling this raise starts from, so `from` is the post-#2689 value and the delta annotated here is only this PR own +2 — the two branches' new leaves are disjoint, so nothing is double-counted. NARROWING ALREADY APPLIED, and it is the reason this is +2 and not +15: the obvious edge is `widget-lifecycle-actor.ts`, the S8a door, which pulls @/lib/widget-user-auth and its synchronous postgres store leaf for a `cwu_` consume a widget MCP frame never performs. That module was SPLIT so the producer imports only the live-standing half; both entries still call one resolver, pinned by widget-lifecycle-actor-parity.test.ts. Further narrowing considered and rejected: dropping the audit sink would remove the authz audit trail from a security-relevant read path (and the analyzer follows dynamic import(), so deferring buys nothing). Measured as a uniform +2 delta against a clean origin/main worktree in the same environment, re-based onto the post-#2689 main.",
      "to": 1777
    }
  }
}
