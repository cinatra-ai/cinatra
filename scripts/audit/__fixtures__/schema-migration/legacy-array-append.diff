diff --git a/migrations/core/core__0015_narrow-usage-events-cost-usd.mjs b/migrations/core/core__0015_narrow-usage-events-cost-usd.mjs
new file mode 100644
--- /dev/null
+++ b/migrations/core/core__0015_narrow-usage-events-cost-usd.mjs
@@ -0,0 +1,11 @@
+// core__0015 — narrow usage_events.cost_usd from numeric(12,8) to numeric(12,4).
+
+/** @param {import("node-pg-migrate").MigrationBuilder} pgm */
+export function up(pgm) {
+  pgm.sql(`ALTER TABLE usage_events ALTER COLUMN cost_usd TYPE numeric(12,4);`);
+}
+
+/** @param {import("node-pg-migrate").MigrationBuilder} pgm */
+export function down(pgm) {
+  pgm.sql(`ALTER TABLE usage_events ALTER COLUMN cost_usd TYPE numeric(12,8);`);
+}
diff --git a/migrations/manifest.json b/migrations/manifest.json
--- a/migrations/manifest.json
+++ b/migrations/manifest.json
@@ -105,6 +105,13 @@
       "summary": "Pinned fixture snapshot entry for seq 0012 — real ledger summary elided; the schema-migration gate does not read this field.",
       "destructive": true,
       "tables": ["metadata", "startups", "startup_overrides", "skill_packages", "skills", "notifications", "campaigns", "chat_threads"]
+    },
+    {
+      "seq": "0015",
+      "file": "core/core__0015_narrow-usage-events-cost-usd.mjs",
+      "summary": "Narrow usage_events.cost_usd numeric(12,8) -> numeric(12,4); scale beyond 4 was never populated.",
+      "destructive": true,
+      "tables": ["usage_events"]
     }
   ]
 }
diff --git a/src/lib/drizzle-store.ts b/src/lib/drizzle-store.ts
--- a/src/lib/drizzle-store.ts
+++ b/src/lib/drizzle-store.ts
@@ -884,7 +884,7 @@ END $$` },
       cached_input_tokens integer NOT NULL DEFAULT 0,
       reasoning_output_tokens integer NOT NULL DEFAULT 0,
       credits_consumed integer NOT NULL DEFAULT 0,
-      cost_usd numeric(12,8),
+      cost_usd numeric(12,4),
       idempotency_key text NOT NULL
     )` },
     { text: `CREATE UNIQUE INDEX IF NOT EXISTS usage_events_idempotency_key_idx ON "${schemaName.replaceAll('"', '""')}"."usage_events" (idempotency_key)` },
