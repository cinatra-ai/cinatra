diff --git a/src/lib/better-auth-schema.ts b/src/lib/better-auth-schema.ts
index 2590f88d..d53e8ac5 100644
--- a/src/lib/better-auth-schema.ts
+++ b/src/lib/better-auth-schema.ts
@@ -30,12 +30,6 @@ export const cinatraAuthAdditionalUserFields = {
     defaultValue: "human",
     input: false,
   },
-  clientId: {
-    type: "string",
-    required: false,
-    defaultValue: null,
-    input: false,
-  },
   // Per-user Avatar accent (nullable; NULL = "derive"). This is the ONLY
   // provisioning path for `public."user".accent_color` on a fresh install:
   // core migrations are ledger-faked on a fresh schema (setup's
