## KaTeX math

Inline math $E = mc^2$ appears in a sentence, as does $\alpha + \beta = \gamma$.

Display math on its own line:

$$\int_0^1 x^2 \, dx = \tfrac{1}{3}$$

A more involved display expression:

$$\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$$
