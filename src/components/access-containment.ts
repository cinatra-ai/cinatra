// ---------------------------------------------------------------------------
// The pure scope-containment helpers, moved into the package that owns the
// access picker (cinatra#3385). This path stays the stable in-app import:
// every existing caller is unchanged.
// ---------------------------------------------------------------------------
export * from "@cinatra-ai/sdk-ui/access/containment";
