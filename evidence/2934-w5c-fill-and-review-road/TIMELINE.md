# TIMELINE — cinatra#2934 W5c picture leg, re-taken at the repaired head (all times UTC, 2026-08-28)

The driver's own clock, line by line, is in [`timeline.jsonl`](timeline.jsonl). This is the shape of it.

| when | what |
|---|---|
| 02:12 | The lane database is created empty on the verify Postgres and the app's own `setup:dev` builds its schema (exit 0). The agent runtime comes up from this checkout; the dev server boots and registers the in-tree extension fleet. |
| 02:40 | The instance administrator is created through the app's own sign-up — the endpoint the setup form posts to — and the run owner the same way. Neither is given a platform role beyond the organization the administrator owns. |
| 02:45 | **The instance namespace is typed into `/setup/name`**, the screen that owns it, and the form's own gate is waited for rather than a stopwatch: `Continue enabled=true`, the namespace field holding `northwind-media`, and the step settles off `/setup/name`. |
| 02:49 | The provider commit is attempted from the operator's own machine over the instance's public origin and the app REFUSES it — `Invalid origin` — because the origin is not yet trusted. Recorded rather than worked around: the instance is told about its own public origin through the documented environment escape hatch (a full origin, which is what better-auth compares against), the app is restarted, and the attempt is repeated. |
| 02:57 | **The provider is committed through the app's own `/setup/model` form**, driven from the operator's machine against the instance's public origin. The key is read from the environment the vault wrapper provides and never printed, logged or written to disk; the form's own field reports 167 characters and the instance ends the step holding a sealed connection row and `setup_provider_commit` `state=committed`, with a readiness receipt at `02:57:21.887Z`. |
| 02:59 | **The public origin is stored through the app's own development configuration screen** and read back off the re-rendered field: `""` → the instance's own public origin, `matches: true`. |
| 03:01 | Run `ac70cd70` is started by the person from the agent's own start page. Nothing is written to the database: the run id is taken off the app's own wire. |
| 03:04–03:10 | The run page's three readings on `ac70cd70`. The fill lands on the drawn `Idea` control; the question touches nothing; the ask fills and presses and the run moves `pending_approval` → `pending_trigger`. Every turn served on attempt 1 with its toolbox present. |
| 03:19 | **`run-page__submit-on-ask`** is photographed, light and dark, each theme in its own context — the card settled into the run's next reading, `Submitted.` the newest turn, the account footer drawing the person in both frames. |
| 03:21–03:23 | **The schedule reading** on that run's own unarmed scheduler form: `scheduledAt` `""` → `2026-08-29T09:00`, `Schedule for later` selected, `Europe/Berlin`, nothing submitted, no trigger row. **Both siblings** are photographed showing the fill's own ask. |
| 03:23–03:27 | Run `512836b7` is started and driven to the question reading; **`run-page__question-no-press`** is photographed with the question as the newest turn. The assistant's line is DRAWN — `strong=1`, zero raw `**`. |
| 03:27–03:30 | Run `3fa04248` is started and driven to the fill reading; **`run-page__fill-no-submit`** is photographed with the fill as the newest turn and the repaired platform sentence above the box. |
| 03:30–03:36 | Run `3c694cde` (Email Outreach Agent) is driven through the step-by-step readings. Its attachment turn is answered honestly that the screen cannot read a file's contents — a true answer about a capability this road does not claim — so the cell is re-driven on a fresh run with the message the clause is about. Both are recorded. |
| 03:37–03:42 | **The step-by-step readings** on run `89f947a2`: the half-typed message surviving a real reload (photographed in place, because a draft lives in the browser rather than in the run), the described change landing in the step's own fields with nothing submitted, and the attachment reaching the waiting run when the answer is finally sent — a second gate `{"stepNumber": 2}` materializing at `03:41:25.695801Z`. |
| 03:44–03:46 | The review run `baa08154` is carried to a real gate with the person's own presses. The run row ends `failed` (`WayFlow task failed`) and its artifact is still produced; `lifecycle-review-orchestration` mints the gate at `03:46:43.747774Z`. |
| 03:49–03:54 | **The review page's question reading.** `what changed in this draft?` — answered, gate still `pending`, zero dispositions, zero repairs, the decision bar unchanged. The answer is DRAWN: `strong=6`, `tables=1`, zero raw `**`, zero raw `\|`. The panel/decision-bar geometry is measured in the same pass. |
| 03:56–03:57 | **The review page's request for changes.** `tighten the opening paragraph` — the gate resolves `changes_requested` at `03:56:22.518980Z`, the repair row carries the person's words verbatim, and the repair run is created and parks. The fresh-review half is recorded as not reached. |
| 04:12–04:13 | The person arms `ac70cd70` with the form's own control (`08/29/2026 09:00`, `Europe/Berlin`) and **the armed-trigger tab** is photographed with the window's own sentence. No fill is attempted there. |
| after | Provider evidence, gate re-runs and the record. The instance's public origin was left exactly as it was found. |
