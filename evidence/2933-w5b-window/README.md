# `evidence/2933-w5b-window/` — the picture leg of cinatra#2933 (PR 2995)

Re-taken in full at head `30deb133b143f059fbd204cb5756d1496568b4f8`, on the real running app, signed in as **a run owner who is not a
platform administrator** (`role=user`, organization `member`). A real provider (OpenAI, `gpt-5.5`)
bound through the app's own setup form answered every turn; native MCP reached the instance through
its configured public origin. No stub, no seeded transcript, no edited pixel. Full window, 1440x900
at device scale 2, light and dark, through the app's own theme control. Every capture was viewed
before it was recorded.

Graded against design `458fb7ffce6cf4ab6a2c60d3ff47198135d8ea2f` — `app-artifact-review.html` §VI,
§IX and §X (masthead **v0.5.0**) and `app-lifecycle-cards.html` §X — and the setup surfaces are
additionally held to the ratified run-surface drawing (§I: a two-column frame, a step rail naming the
run's ordered steps, the selected step's surface in the run detail, the window under it). Those
sections were rendered and their pixels read before anything here was graded.

**What the window was asked, on all five mounts.** The run's owner typed **"what is this step
waiting for?"** into the window and named nothing — no run, no step, no id. **Every one of the
five answered from that run's own recorded state. None asked "which step do you mean?" and none
asked for an id.** The answers are quoted in full with their run ids and timestamps in
[`capture-records.md`](capture-records.md), together with a sha256 per file.

## The graded table

| capture | requires | shows | verdict |
|---|---|---|---|
| [`run-page__box-placeholder__light.png`](run-page__box-placeholder__light.png) | §IX — there is no panel above an empty exchange: the window is the field alone until the first message, and it is drawn only for a person who may answer the run.<br>§X — the sentence in the empty field is this surface's own, character for character: `Ask Cinatra to fill the fields above, or ask about this step…`; nothing else about the window changes from one reading to the next. | The setup gate of a `pending_approval` run — `Agentic Run Progress` with `Awaiting input`, the `Idea (optional)` field and a live `Continue` — signed in as the run's owner, who is not a platform administrator (`Rita Owner`). The window is the field alone at the foot of the page with no panel above it, and the sentence in it is `Ask Cinatra to fill the fields above, or ask about this step…`, read off the DOM and compared with the drawing character for character, the ellipsis included. | **PASS** on §IX and §X. **DEVIATION 6** on the frame: the page draws one centred column rather than the run-surface drawing's two-column frame — this run has no ordered steps yet. |
| [`run-page__box-placeholder__dark.png`](run-page__box-placeholder__dark.png) | §IX — there is no panel above an empty exchange: the window is the field alone until the first message, and it is drawn only for a person who may answer the run.<br>§X — the sentence in the empty field is this surface's own, character for character: `Ask Cinatra to fill the fields above, or ask about this step…`; nothing else about the window changes from one reading to the next.<br>As above, dark theme, through the app's own theme control. | The same gate, the same field alone with no panel above it, and the same §X sentence on the dark ground. | **PASS** on §IX and §X; **DEVIATION 6** as above. |
| [`run-page__exchange-after-reload__light.png`](run-page__exchange-after-reload__light.png) | §IX — turns, not a transcript: after a reload the exchange is there, the reader's turn right-aligned on the indigo ground in white and the assistant's left-aligned on the muted surface in ink, at most four-fifths of the panel, line breaks kept, no author label, no avatar, no timestamp.<br>§X — the field beneath the exchange still carries this surface's sentence, `Ask Cinatra to fill the fields above, or ask about this step…`. | After a browser reload the panel above the field holds `what is this step waiting for?` right-aligned in white on the indigo ground and the model's own answer left-aligned on the muted surface, naming **this run's** gate (`@cinatra-ai/agent-builder:schema-field-fallback`) and its three empty setup fields (`title`, `summary`, `outline`). No author label, no avatar, no timestamp. The field beneath still reads `Ask Cinatra to fill the fields above, or ask about this step…`. | **PASS**. |
| [`run-page__exchange-after-reload__dark.png`](run-page__exchange-after-reload__dark.png) | §IX — turns, not a transcript: after a reload the exchange is there, the reader's turn right-aligned on the indigo ground in white and the assistant's left-aligned on the muted surface in ink, at most four-fifths of the panel, line breaks kept, no author label, no avatar, no timestamp.<br>§X — the field beneath the exchange still carries this surface's sentence, `Ask Cinatra to fill the fields above, or ask about this step…`.<br>As above, dark theme. | The same two turns, the same answer and the same field sentence on the dark ground; the reader's bubble is the theme's own primary — a light ground with ink text. | **PASS** on shape; **DEVIATION 5** on the dark bubble ground. |
| [`step-by-step__box-placeholder__light.png`](step-by-step__box-placeholder__light.png) | §IX — there is no panel above an empty exchange: the window is the field alone until the first message, and it is drawn only for a person who may answer the run.<br>§X — the sentence in the empty field is this surface's own, character for character: `Ask Cinatra to fill this step's fields, or ask about the run…`; nothing else about the window changes from one reading to the next.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it. | The five-step rail with `1 Campaign setup` selected and `2 Account scope` · `3 Review recipients` · `4 Review drafts` · `5 Test & send` below it, that step's form in the run detail (`Offering company website *`, `Call to action *`, `Sender name (optional)`, `Save & start run`), and the field alone beneath the frame with the paperclip offered. The sentence is `Ask Cinatra to fill this step's fields, or ask about the run…`, DOM-verified character for character. | **PASS** on §IX, §X and the run-surface drawing. |
| [`step-by-step__box-placeholder__dark.png`](step-by-step__box-placeholder__dark.png) | §IX — there is no panel above an empty exchange: the window is the field alone until the first message, and it is drawn only for a person who may answer the run.<br>§X — the sentence in the empty field is this surface's own, character for character: `Ask Cinatra to fill this step's fields, or ask about the run…`; nothing else about the window changes from one reading to the next.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it.<br>As above, dark theme. | The same rail, form, field and §X sentence on the dark ground. | **PASS** on §IX, §X and the run-surface drawing. |
| [`step-by-step__exchange-after-reload__light.png`](step-by-step__exchange-after-reload__light.png) | §IX — turns, not a transcript: after a reload the exchange is there, the reader's turn right-aligned on the indigo ground in white and the assistant's left-aligned on the muted surface in ink, at most four-fifths of the panel, line breaks kept, no author label, no avatar, no timestamp.<br>§X — the field beneath the exchange still carries this surface's sentence, `Ask Cinatra to fill this step's fields, or ask about the run…`.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it. | After a reload the person's turn right-aligned and the answer left-aligned, naming **step 1** and the three fields that step is waiting for as `Empty`; the rail and the step's form stand unchanged behind the panel, and the field beneath reads `Ask Cinatra to fill this step's fields, or ask about the run…`. | **PASS**. |
| [`step-by-step__exchange-after-reload__dark.png`](step-by-step__exchange-after-reload__dark.png) | §IX — turns, not a transcript: after a reload the exchange is there, the reader's turn right-aligned on the indigo ground in white and the assistant's left-aligned on the muted surface in ink, at most four-fifths of the panel, line breaks kept, no author label, no avatar, no timestamp.<br>§X — the field beneath the exchange still carries this surface's sentence, `Ask Cinatra to fill this step's fields, or ask about the run…`.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it.<br>As above, dark theme. | The same exchange, the same answer and the same field sentence on the dark ground. | **PASS** on shape; **DEVIATION 5** on the dark bubble ground. |
| [`schedule__box-placeholder__light.png`](schedule__box-placeholder__light.png) | §IX — there is no panel above an empty exchange: the window is the field alone until the first message, and it is drawn only for a person who may answer the run.<br>§X — the sentence in the empty field is this surface's own, character for character: `Ask Cinatra to set the schedule above, or ask about it…`; nothing else about the window changes from one reading to the next.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it. | The setup run page of a `pending_trigger` run: the rail `1 Schedule` (selected) · `2 Recommendation` · `3 Review`, the scheduler form in the run detail (`When should this run?` · `Run right after setup` · `Schedule for later` · `Recurring` with its repeat, weekday, time and timezone rows · `Estimated run duration — Unavailable.` · `Continue`), and the field alone beneath the frame. The sentence is `Ask Cinatra to set the schedule above, or ask about it…`, DOM-verified character for character. | **PASS** on §IX, §X and the run-surface drawing. |
| [`schedule__box-placeholder__dark.png`](schedule__box-placeholder__dark.png) | §IX — there is no panel above an empty exchange: the window is the field alone until the first message, and it is drawn only for a person who may answer the run.<br>§X — the sentence in the empty field is this surface's own, character for character: `Ask Cinatra to set the schedule above, or ask about it…`; nothing else about the window changes from one reading to the next.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it.<br>As above, dark theme. | The same rail, scheduler form, field and §X sentence on the dark ground. | **PASS** on §IX, §X and the run-surface drawing. |
| [`schedule__exchange-after-reload__light.png`](schedule__exchange-after-reload__light.png) | §IX — turns, not a transcript: after a reload the exchange is there, the reader's turn right-aligned on the indigo ground in white and the assistant's left-aligned on the muted surface in ink, at most four-fifths of the panel, line breaks kept, no author label, no avatar, no timestamp.<br>§X — the field beneath the exchange still carries this surface's sentence, `Ask Cinatra to set the schedule above, or ask about it…`.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it. | After a reload the person's turn and the model's answer, read off **this run's own status**: it says the run is in `pending_trigger` and is waiting for its scheduled trigger to fire — “not for a person, approval, or input”. The scheduler form stands behind the panel and the field beneath reads `Ask Cinatra to set the schedule above, or ask about it…`. | **PASS**. |
| [`schedule__exchange-after-reload__dark.png`](schedule__exchange-after-reload__dark.png) | §IX — turns, not a transcript: after a reload the exchange is there, the reader's turn right-aligned on the indigo ground in white and the assistant's left-aligned on the muted surface in ink, at most four-fifths of the panel, line breaks kept, no author label, no avatar, no timestamp.<br>§X — the field beneath the exchange still carries this surface's sentence, `Ask Cinatra to set the schedule above, or ask about it…`.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it.<br>As above, dark theme. | The same exchange, the same answer and the same field sentence on the dark ground. | **PASS** on shape; **DEVIATION 5** on the dark bubble ground. |
| [`armed-trigger__box-placeholder__light.png`](armed-trigger__box-placeholder__light.png) | §IX — there is no panel above an empty exchange: the window is the field alone until the first message, and it is drawn only for a person who may answer the run.<br>§X — the sentence in the empty field is this surface's own, character for character: `Ask Cinatra to change this schedule, or ask about it…`; nothing else about the window changes from one reading to the next.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it. | The run's persistent `Schedule` tab selected in the strip (`Setup` · `Schedule` · `Permissions`), the armed recurring schedule as it stands (`Recurring`, `Repeat every 1 week(s)`, `On Wed`, `At 09 : 00`, `Timezone UTC`, `Save changes`), the rail beside it, and the field alone beneath. The sentence is `Ask Cinatra to change this schedule, or ask about it…`, DOM-verified character for character. | **PASS** on §IX, §X and the run-surface drawing. |
| [`armed-trigger__box-placeholder__dark.png`](armed-trigger__box-placeholder__dark.png) | §IX — there is no panel above an empty exchange: the window is the field alone until the first message, and it is drawn only for a person who may answer the run.<br>§X — the sentence in the empty field is this surface's own, character for character: `Ask Cinatra to change this schedule, or ask about it…`; nothing else about the window changes from one reading to the next.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it.<br>As above, dark theme. | The same tab, schedule, rail, field and §X sentence on the dark ground. | **PASS** on §IX, §X and the run-surface drawing. |
| [`armed-trigger__exchange-after-reload__light.png`](armed-trigger__exchange-after-reload__light.png) | §IX — turns, not a transcript: after a reload the exchange is there, the reader's turn right-aligned on the indigo ground in white and the assistant's left-aligned on the muted surface in ink, at most four-fifths of the panel, line breaks kept, no author label, no avatar, no timestamp.<br>§X — the field beneath the exchange still carries this surface's sentence, `Ask Cinatra to change this schedule, or ask about it…`.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it. | After a reload the person's turn and an answer read off **this run's own schedule row**: `Agent @cinatra-ai/blog-draft-writer-agent`, `Run Blog Draft Writer Agent (4)`, `Status armed`, `Waiting on Schedule`, `Cron 0 9 * * 3`, `Timezone UTC` — “every Wednesday at 09:00 UTC”. The window names the run it sits under; it is not listing the runs this person may see. The armed schedule stands behind the panel and the field beneath reads `Ask Cinatra to change this schedule, or ask about it…`. | **PASS**. |
| [`armed-trigger__exchange-after-reload__dark.png`](armed-trigger__exchange-after-reload__dark.png) | §IX — turns, not a transcript: after a reload the exchange is there, the reader's turn right-aligned on the indigo ground in white and the assistant's left-aligned on the muted surface in ink, at most four-fifths of the panel, line breaks kept, no author label, no avatar, no timestamp.<br>§X — the field beneath the exchange still carries this surface's sentence, `Ask Cinatra to change this schedule, or ask about it…`.<br>Run-surface drawing §I — a two-column frame: a step rail naming the run's ordered steps on the left, the selected step's surface in the run detail on the right, the window under it.<br>As above, dark theme. | The same exchange, the same answer and the same field sentence on the dark ground. | **PASS** on shape; **DEVIATION 5** on the dark bubble ground. |
| [`review__box-placeholder__light.png`](review__box-placeholder__light.png) | §VI — one decision bar with exactly three affordances: the rationale field `Add a note for the run and the audit trail…` under `DECISION RATIONALE (optional on approve, expected on reject)`, then `Comment`, `Reject` (destructive) and `Approve` (primary), with the conversational prompt window beneath it and no fourth “request changes” button.<br>§IV — the review target in its own representation; §V — a never-blank floor beneath it.<br>§IX — there is no panel above an empty exchange: the window is the field alone until the first message, and it is drawn only for a person who may answer the run.<br>§X — the sentence in the empty field is this surface's own, character for character: `Ask Cinatra about this review, or ask for changes to the work…`; nothing else about the window changes from one reading to the next. | The review page of a run that produced a `Blog Post Artifact` (`Why a Weekly Publishing Rhythm Beats a Burst of Posts`), `Review requested` · `Awaiting your decision`, the rail `1 Schedule` · `2 Review`, the decision bar exactly as §VI draws it, and the window's field beneath it with no panel above it. The sentence is `Ask Cinatra about this review, or ask for changes to the work…`, DOM-verified character for character. Above the bar the target is **not** rendered in its own representation: `review target unavailable — slot "detail", reason "no-semantic-renderer"`, falling through to the generic read-only view pull request 2983 ships (`No type renderer resolved for this artifact — showing the generic read-only view`, with `type`, `mime`, `revision` and `Preview` / `Download`). | **PASS** on §VI, §V, §IX and §X; **DEVIATION 2** on §IV. |
| [`review__box-placeholder__dark.png`](review__box-placeholder__dark.png) | §VI — one decision bar with exactly three affordances: the rationale field `Add a note for the run and the audit trail…` under `DECISION RATIONALE (optional on approve, expected on reject)`, then `Comment`, `Reject` (destructive) and `Approve` (primary), with the conversational prompt window beneath it and no fourth “request changes” button.<br>§IV — the review target in its own representation; §V — a never-blank floor beneath it.<br>§IX — there is no panel above an empty exchange: the window is the field alone until the first message, and it is drawn only for a person who may answer the run.<br>§X — the sentence in the empty field is this surface's own, character for character: `Ask Cinatra about this review, or ask for changes to the work…`; nothing else about the window changes from one reading to the next.<br>As above, dark theme. | The same page, rail, decision bar, field, §X sentence and unrendered target on the dark ground. | **PASS** on §VI, §V, §IX and §X; **DEVIATION 2** on §IV. |
| [`review__exchange-open__light.png`](review__exchange-open__light.png) | §IX — turns, not a transcript: the reader's turn right-aligned on the indigo ground in white and the assistant's left-aligned on the muted surface in ink, at most four-fifths of the panel, line breaks kept, no author label, no avatar, no timestamp.<br>§VI — “typing a change request into it is how a reviewer requests changes … on submit the gate resolves changes-requested and a repair goes in flight”, and there is no dedicated “request changes” button.<br>`app-lifecycle-cards.html` §X — a typed comment lands word for word and there is no confirmation step.<br>The answer comes from THIS run's own state, with nothing named by the person — and it must name the **open review gate** and the **work under review**.<br>§X — the field beneath the exchange still carries this surface's sentence, `Ask Cinatra about this review, or ask for changes to the work…`. | The person's turn right-aligned and the model's answer left-aligned: “This step is waiting for **your review decision** on: **Why a Weekly Publishing Rhythm Beats a Burst of Posts — Blog Post Artifact**”, followed by what the reader may do — `Approve` ends the review as approved, `Reject` ends it as rejected, `Comment` records a note and keeps the review pending, and a request for changes is typed into this window and submitted, on which “the gate resolves as changes-requested and a repair starts”. The gate header still reads `Review requested` · `Awaiting your decision` at this paint, the target card stands behind the panel and the field beneath reads `Ask Cinatra about this review, or ask for changes to the work…`. | **PASS.** The defect the previous set photographed — `Waiting on **Nothing**` for a run whose review gate was open — is gone: the answer names the gate that is open and the work under review, and it names the three decision-bar affordances §VI actually draws plus the typed request, never a fourth button. |
| [`review__exchange-open__dark.png`](review__exchange-open__dark.png) | As above, dark theme. | The same three things — the open gate header, the two turns and the same answer — and the same field sentence on the dark ground. | **PASS**; **DEVIATION 5** on the dark bubble ground. |
| [`review__exchange-after-reload__light.png`](review__exchange-after-reload__light.png) | §IX — “The window is drawn only for a person who may answer the run”: where there is nothing left to answer, no window and no field.<br>§VI — a landed change request resolves the gate changes-requested and sends a repair in flight. | After the reload the gate is resolved — `Changes requested by Rita Owner` · “The gate is resolved and the reviewed work has been turned back for repair.” — and there is no window and no field on the page. The exchange itself stands in the run's store: both rows for run `100108c3-e239-4385-8ed1-5cfa1ce145d9`, listed below, and a repair run went in flight. | **PASS** against §IX's access rule and §VI's resolve-on-submit; **DEVIATION 4** against this recipe's “the exchange open after a reload”, for this one surface. |
| [`review__exchange-after-reload__dark.png`](review__exchange-after-reload__dark.png) | As above, dark theme. | The same resolved reading and the same absent window on the dark ground. | **PASS** / **DEVIATION 4** as above. |
| [`run-page__no-respond-access__light.png`](run-page__no-respond-access__light.png) | §IX — the right to type here is the run's own access: “a person without respond access **never sees the box**, rather than a field that would be refused”.<br>Light theme. | A signed-in ordinary member who does not own the run (`Ben Bystander`) is refused the run page itself: `404 — Page not found`. No window, no exchange, no field. Byte-identical to the file recorded at `12e218f1a50d`; the behaviour was re-verified live at this head on this leg's own runs, where the box count is 0 on all five surfaces in both themes. | **PASS**. |
| [`run-page__no-respond-access__dark.png`](run-page__no-respond-access__dark.png) | §IX — the right to type here is the run's own access: “a person without respond access **never sees the box**, rather than a field that would be refused”.<br>Dark theme. | A signed-in ordinary member who does not own the run (`Ben Bystander`) is refused the run page itself: `404 — Page not found`. No window, no exchange, no field. Byte-identical to the file recorded at `12e218f1a50d`; the behaviour was re-verified live at this head on this leg's own runs, where the box count is 0 on all five surfaces in both themes. | **PASS**. |
| [`step-by-step__no-respond-access__light.png`](step-by-step__no-respond-access__light.png) | §IX — the right to type here is the run's own access: “a person without respond access **never sees the box**, rather than a field that would be refused”.<br>Light theme. | A signed-in ordinary member who does not own the run (`Ben Bystander`) is refused the stepped run itself: `404 — Page not found`. No window, no exchange, no field. Byte-identical to the file recorded at `12e218f1a50d`; the behaviour was re-verified live at this head on this leg's own runs, where the box count is 0 on all five surfaces in both themes. | **PASS**. |
| [`step-by-step__no-respond-access__dark.png`](step-by-step__no-respond-access__dark.png) | §IX — the right to type here is the run's own access: “a person without respond access **never sees the box**, rather than a field that would be refused”.<br>Dark theme. | A signed-in ordinary member who does not own the run (`Ben Bystander`) is refused the stepped run itself: `404 — Page not found`. No window, no exchange, no field. Byte-identical to the file recorded at `12e218f1a50d`; the behaviour was re-verified live at this head on this leg's own runs, where the box count is 0 on all five surfaces in both themes. | **PASS**. |
| [`schedule__no-respond-access__light.png`](schedule__no-respond-access__light.png) | §IX — the right to type here is the run's own access: “a person without respond access **never sees the box**, rather than a field that would be refused”.<br>Light theme. | A signed-in ordinary member who does not own the run (`Ben Bystander`) is refused the schedule step's route itself: `404 — Page not found`. No window, no exchange, no field. Byte-identical to the file recorded at `12e218f1a50d`; the behaviour was re-verified live at this head on this leg's own runs, where the box count is 0 on all five surfaces in both themes. | **PASS**. |
| [`schedule__no-respond-access__dark.png`](schedule__no-respond-access__dark.png) | §IX — the right to type here is the run's own access: “a person without respond access **never sees the box**, rather than a field that would be refused”.<br>Dark theme. | A signed-in ordinary member who does not own the run (`Ben Bystander`) is refused the schedule step's route itself: `404 — Page not found`. No window, no exchange, no field. Byte-identical to the file recorded at `12e218f1a50d`; the behaviour was re-verified live at this head on this leg's own runs, where the box count is 0 on all five surfaces in both themes. | **PASS**. |
| [`armed-trigger__no-respond-access__light.png`](armed-trigger__no-respond-access__light.png) | §IX — the right to type here is the run's own access: “a person without respond access **never sees the box**, rather than a field that would be refused”.<br>Light theme. | A signed-in ordinary member who does not own the run (`Ben Bystander`) is refused the armed run's `Schedule` tab itself: `404 — Page not found`. No window, no exchange, no field. Byte-identical to the file recorded at `12e218f1a50d`; the behaviour was re-verified live at this head on this leg's own runs, where the box count is 0 on all five surfaces in both themes. | **PASS**. |
| [`armed-trigger__no-respond-access__dark.png`](armed-trigger__no-respond-access__dark.png) | §IX — the right to type here is the run's own access: “a person without respond access **never sees the box**, rather than a field that would be refused”.<br>Dark theme. | A signed-in ordinary member who does not own the run (`Ben Bystander`) is refused the armed run's `Schedule` tab itself: `404 — Page not found`. No window, no exchange, no field. Byte-identical to the file recorded at `12e218f1a50d`; the behaviour was re-verified live at this head on this leg's own runs, where the box count is 0 on all five surfaces in both themes. | **PASS**. |
| [`review__no-respond-access__light.png`](review__no-respond-access__light.png) | §IX — the right to type here is the run's own access: “a person without respond access **never sees the box**, rather than a field that would be refused”.<br>Light theme. | A signed-in ordinary member who does not own the run (`Ben Bystander`) is refused the review: `Not authorized` — “You don't have access to this review. This review belongs to an agent run you're not authorized to see.” No window, no exchange, no field. Byte-identical to the file recorded at `12e218f1a50d`; the behaviour was re-verified live at this head on this leg's own runs, where the box count is 0 on all five surfaces in both themes. | **PASS**. |
| [`review__no-respond-access__dark.png`](review__no-respond-access__dark.png) | §IX — the right to type here is the run's own access: “a person without respond access **never sees the box**, rather than a field that would be refused”.<br>Dark theme. | A signed-in ordinary member who does not own the run (`Ben Bystander`) is refused the review: `Not authorized` — “You don't have access to this review. This review belongs to an agent run you're not authorized to see.” No window, no exchange, no field. Byte-identical to the file recorded at `12e218f1a50d`; the behaviour was re-verified live at this head on this leg's own runs, where the box count is 0 on all five surfaces in both themes. | **PASS**. |

## Deviations from this recipe, named

1. **CLOSED, and proved here — the per-surface sentence is the one the drawing fixes, on all five
   surfaces.** §X of `app-artifact-review.html` at design `458fb7ffce6c` draws a different sentence in
   each surface's empty field. Every capture that photographs a field shows **its own** surface's
   sentence, and each one was additionally read out of the rendered DOM and compared with the drawing
   character for character, the ellipsis included. The previous set showed one string,
   `Ask Cinatra to suggest edits to the fields above…`, on all five.

2. **The review target is not rendered in its own representation.** The card reads
   `review target unavailable — slot "detail", reason "no-semantic-renderer"` and falls through to the
   **generic read-only view pull request 2983 ships** (`#2931`, `review-target-panel.tsx` — "No type
   renderer resolved for this artifact — showing the generic read-only view", with `type`, `mime`,
   `revision` and `Preview` / `Download`). §IV draws the target's own representation. Not introduced
   here — the previous capture set photographed the same reading — and it is a renderer-resolution
   state, not a window state.

3. **CLOSED, and proved here — the review page's answer names the review gate that is open and the
   work under review.** The previous set's `review__exchange-open__*` showed `Waiting on **Nothing**`
   for a run whose artifact review gate was pending at that moment. At this head the same question on
   the same surface is answered "This step is waiting for **your review decision** on: **Why a Weekly
   Publishing Rhythm Beats a Burst of Posts — Blog Post Artifact**", with the three decision-bar
   affordances §VI actually draws and the typed request for changes, and never a fourth button.

4. **The review window's exchange cannot be photographed after a reload.** The same keystroke that
   sends the message files the review's change request (§VI: "there is no dedicated 'request changes'
   button"), so the gate resolves and the window is correctly gone on the next paint.
   `review__exchange-open__*` photographs that same surface a moment earlier. The exchange IS kept with
   the run — both rows under `message_type='window'` for run
   `100108c3-e239-4385-8ed1-5cfa1ce145d9`, quoted in `capture-records.md`.

5. **Dark-theme bubble ground.** §IX draws the reader's turn "on the indigo ground in white"; the
   window paints it with the same token in both themes, so in dark theme the ground is the theme's own
   light primary with ink text. Same token, dark value.

6. **The run page draws no two-column frame at a pre-execution setup gate.** The run-surface drawing
   draws every run-page state as a two-column frame with a step rail. `run-page__*` shows a single
   centred column: the run has no ordered steps and no gate step yet, so the screen renders the run
   detail alone. The four other surfaces all draw the rail. This is the frame's shape, not the
   window's — the window is drawn, is gated on the run's access, and answers per run.

7. **The tool-less capture is not in this set.** The platform's own sentence for a model that cannot
   operate anything is reached only on a conversation-only provider. On this instance the provider is
   committed through the app's own setup and there is no product path to a conversation-only one, so
   the state cannot be reached through the product. Named rather than faked.

8. **The warm-up turn lives on a run no capture photographs.** The first turn on a cold public-MCP
   path is refused by the runtime's own 2.5 s reachability probe. That path was warmed before the
   photographed turns were sent, and the one warm-up turn was left on a run nothing photographs
   (`868fb7b5-733b-47cb-9133-a88fa5bcdbbe`, listed in `capture-records.md`), so every photographed run
   carries exactly one real exchange.
