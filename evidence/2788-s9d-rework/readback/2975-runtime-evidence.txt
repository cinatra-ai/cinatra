THE 2026-08-26 RE-SHOOT — THE APP SERVER'S OWN LINES (cinatra#2970 / PR #2975)

Each block prints the grep that produced it, so the extraction is repeatable, and
states what its lines can and cannot say. ONE redaction is applied everywhere it
occurs: the instance's public origin, because a committed hostname is a leak and a
lane the next operator cannot reproduce. A HIT is proof of a thing happening; a
ZERO is the absence of that particular line and nothing more.

### The provider's servers calling back into the shipped MCP endpoint
grep: POST /api/mcp 200
lines matched: 8
A callback into this instance's own /api/mcp over the public origin. It proves the
hosted-MCP path was live during the round; it does not name the caller's address,
and a log line never can.

 POST /api/mcp 200 in 452ms (next.js: 4ms, proxy.ts: 16ms, application-code: 432ms)
 POST /api/mcp 200 in 206ms (next.js: 34ms, proxy.ts: 14ms, application-code: 158ms)
 POST /api/mcp 200 in 246ms (next.js: 11ms, proxy.ts: 150ms, application-code: 85ms)
 POST /api/mcp 200 in 35ms (next.js: 3ms, proxy.ts: 1889µs, application-code: 30ms)
 POST /api/mcp 200 in 44ms (next.js: 3ms, proxy.ts: 2ms, application-code: 39ms)
 POST /api/mcp 200 in 1046ms (next.js: 3ms, proxy.ts: 2ms, application-code: 1041ms)
 POST /api/mcp 200 in 122ms (next.js: 6ms, proxy.ts: 3ms, application-code: 113ms)
 POST /api/mcp 200 in 42ms (next.js: 3ms, proxy.ts: 3ms, application-code: 37ms)

### The turns REFUSED before the funnel was warm — the round's one environment limit
grep: refusing to run the turn without Cinatra tools
lines matched: 2
The runtime probes the public MCP URL with a 2.5s HEAD before every turn and
refuses the turn outright if it does not answer in time. On this host the FIRST
TLS handshake through the funnel takes about five seconds, so the first two chat
turns of the session were refused. Warming the funnel with one HEAD request is
what this round did about it; nothing was stood in for, and the two refusals are
left here rather than trimmed out.

[assistant-runtime] public MCP URL <the lane's public origin>/api/mcp is unreachable (no response within 2500ms) — refusing to run the turn without Cinatra tools (#1699)
[assistant-runtime] public MCP URL <the lane's public origin>/api/mcp is unreachable (no response within 2500ms) — refusing to run the turn without Cinatra tools (#1699)

### The scripted / deterministic model bridge
grep: CINATRA_UAT_OK|deterministic (chat|model) (reply|bridge)|scripted (runtime|provider)
lines matched: 0
Zero. The scripted provider served nothing in this session. That is the absence of
these lines, not a capture of the server's process environment — see
`2975-reshoot-readback.json`, which records that this host prints no environment
for the listening process at all.

(no line matched)

### A pre-router short circuit
grep: \[pre-router\]
lines matched: 0
Zero.

(no line matched)

### A turn refused for want of a provider
grep: no (LLM )?provider (is )?(configured|available)
lines matched: 0
Zero. The provider was configured through the app's own /setup/model form and the
app sealed the connection itself.

(no line matched)

### The hosted MCP connector's cold 424
grep: MCP tool enumeration failed \(424\)
lines matched: 0
Zero this round. Earlier rounds saw the model's hosted MCP connector fail its first
tool-list fetch on a cold OAuth path; it did not occur here.

(no line matched)

### The agent runtime resolving a provider through the shipped bridge
grep: \[llm-bridge-run-select\]
lines matched: 0
Zero, and it should be: no run EXECUTED in this round. The pictured run has
`started_at` NULL and the round's cells are all pre-execution screens.

(no line matched)
