cinatra#2274 AC3 — TWO full runs were driven. Why, and what each shows.

This lane drove the Anthropic arm END TO END TWICE, on two separate fresh lane
instances built from the same commit. Both are committed. Neither is discarded,
because the second exists to remove a caveat the first exposed, and a reader has
to be able to see the caveat.

RUN 1 — results-ac3-run1/AC3R1-*, screenshots-ac3-run1/, ledgers/egress-ac3-run1.jsonl
  wizard arm     : 18 PASS / 0 FAIL
  assistant run  : 7 PASS / 0 FAIL, answered on attempt 1, containerSkillCount=5
  CAVEAT it exposed, measured on the wire:
    26 x POST /v1/skills -> 200   (created)
     2 x POST /v1/skills -> 400   each IMMEDIATELY followed by GET /v1/skills -> 200
  Those two are the create-time DISPLAY-TITLE COLLISION reconciliation in
  packages/llm/src/tools/anthropic-custom-skills-client.ts: on a 400/409 whose body
  says the title is taken, the client lists custom skills and ADOPTS the existing
  remote identity + its latest_version rather than minting a duplicate.
  The two adopted rows were:
    @cinatra-ai/list-curation-skill:list-curation -> skill_016S7nJzFHWAjhBTG6LN7ptb
       remote created_at 2026-07-30T07:17:03Z, version 1785395823234414
    @cinatra-ai/web-research-skill:web-research   -> skill_01FMLgcvGdk8JB3TMUT5g3of
       remote created_at 2026-07-30T07:17:04Z, version 1785395824133327
  Both created_at land inside the PRE-MERGE fix-verification round's own window
  (ledgers/egress-fix-verification.jsonl: 2026-07-30T07:07:28Z .. 07:56:30Z), and
  that round uploaded 28 while reclaiming only 26 — so these are exactly the two
  it left behind in the shared org workspace.
  CONSEQUENCE, stated at the strength the evidence supports: for those two catalog
  skills run 1's sync row pointed at a remote identity+version created by a PRIOR
  round, and no POST /v1/skills/<id>/versions was recorded in run 1, so run 1 did
  NOT push its own bundle bytes for them. Whether the adopted remote content equals
  run 1's bundle is NOT VERIFIED here and is not claimed.
  It did NOT touch any acceptance item: the packaging gate refused ZERO, and all
  five references the completed turn delivered were skills run 1 itself created
  (created_at 2026-07-31T01:54Z).

RUN 2 — results/AC3-*, screenshots-ac3/, ledgers/egress-ac3.jsonl  <- THE PRIMARY RECORD
  Driven after this lane's reclaim emptied the workspace of every non-built-in
  custom skill (AC3R1-live-api-cleanup.json: 28/28 reclaimed, 0 indeterminate,
  remoteSkillsRemainingTotal=4 — Anthropic's own xlsx/pptx/pdf/docx).
  wizard arm     : 18 PASS / 0 FAIL
  assistant run  : 7 PASS / 0 FAIL, answered on attempt 1, containerSkillCount=5
  wire           : 28 x POST /v1/skills -> 200 and NOTHING ELSE on the upload path.
                   ZERO 400s, ZERO list-and-adopt. Every one of the 28 — including
                   all SIX multi-file bundles — was CREATED from this run's own
                   packaged bundle.

So the caveat is not argued away; it is eliminated by measurement, and run 1 is
kept so the elimination can be checked.

Run 1's full ledger phase histogram:
  total provider calls: 33
    2  anthropic GET /v1/skills 200  [A-readiness-run-1]
    1  anthropic POST /v1/messages 200  [A-readiness-run-2]
    1  anthropic POST /v1/messages 200  [AC3-anthropic-assistant-run-attempt-1]
    1  anthropic POST /v1/messages 200  [AC3c-anthropic-assistant-run-attempt-1]
   26  anthropic POST /v1/skills 200  [A-readiness-run-1]
    2  anthropic POST /v1/skills 400  [A-readiness-run-1]

Run 2's full ledger phase histogram:
  total provider calls: 30
    1  anthropic POST /v1/messages 200  [A-readiness-run-2]
    1  anthropic POST /v1/messages 200  [AC3-anthropic-assistant-run-attempt-1]
   28  anthropic POST /v1/skills 200  [A-readiness-run-1]
