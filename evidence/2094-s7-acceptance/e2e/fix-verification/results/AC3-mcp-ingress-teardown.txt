cinatra#2274 AC3 — ingress TEARDOWN, byte-diffed against the pre-run capture

command run: tailscale serve --https=443 --set-path=/api/mcp off

diff <pre-run state> <post-teardown state>:
  (no differences — byte-identical)

post-teardown state (redacted):
{
  "TCP": {
    "443": {
      "HTTPS": true
    }
  },
  "Web": {
    "<lane-funnel-origin>:443": {
      "Handlers": {
        "/": {
          "Proxy": "http://127.0.0.1:3000"
        }
      }
    }
  },
  "AllowFunnel": {
    "<lane-funnel-origin>:443": true
  }
}

The pre-existing ':443 /' handler this machine already carried was NEVER touched —
only the /api/mcp handler this lane added was removed.
