=== the negative run: a bad signature is refused BEFORE any live finalized row exists ===

-- the package under test was published to the lane registry signed with an UNTRUSTED key --
   (the trusted key is the one configured in CINATRA_EXTENSION_SIGNING_PUBLIC_KEYS; this one is not)

-- the application's own refusal, from its log --
[marketplace-install] install failed for @cinatra-ai/google-appointment-schedules-connector (category=unrecoverable): install of @cinatra-ai/google-appointment-schedules-connector did not finalize the real-integrity pipeline (pipeline-threw:install of @cinatra-ai/google-appointment-schedules-connector@0.1.1 was refused before anything was committed: package signature did not verify. No install-op journal, host-port grant or provenance was written, the materialized bytes were removed, and the version bundled in the image stays in service.) — the package is not anchorable; the placeholder install row was rolled back so a re-install re-runs the pipeline.

-- install rows for the package after the refusal --
        id         | version | status | is_default | source_type 
-------------------+---------+--------+------------+-------------
 iext_6e3145b0-e28 | 0.1.0   | active | t          | bundled
(1 row)

-- live marketplace-sourced rows (the count that must be zero) --
0
